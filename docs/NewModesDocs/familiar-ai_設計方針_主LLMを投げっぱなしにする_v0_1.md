# familiar-ai 設計方針：主LLM を投げっぱなしにする（環-h・v0.1）

## この文書の位置づけ

主LLM を**投げっぱなしの資源**として扱い、その返りを調べものの完了と**同じ道**（完了キュー
QC）で受ける。反復は「決める反復」と「出す反復」の2種類になり、`_iterate` は軽量LLM が
司る側だけを数える。

**課題8 の 環-f-ろ（主LLM を非同期にする）を含む。** 環-f-ろ はこの課題に吸収する。

**挙動が変わる。** 環-e（口を作り実体を切り出す・挙動を変えない）とは別に立てる。

実装はこの文書の承認後に始める。

## 1. なぜ

### いまの形

`_iterate`（271行）は、1つの反復の中で**軽量LLM の判断と主LLM の生成を同居**させている。
設計はこれを「**1つの work の内部二段**」と定め、「反復をまたがず別出力でもない」と明記して
いる（`I 内部設計根拠` 手順5）。

その結果、`_iterate` の出口は8つあり、**3つの軸**（誰が決めたか／声になるか／閉じるか）が
1つの関数に畳み込まれている。同じ結末が2か所ずつ書かれている。

| 結末 | 軽量LLM の側 | 主LLM の側 |
|---|---|---|
| 発話して閉じる | `:1274-1276` | `:1399-1401` |
| 調べものを投げて閉じない | `:1281-1293` | `:1358-1367` |
| 中止（世代ずれ） | `:1259-1261` | `:1352-1354` |

### 主LLM は既に資源である

**出-c（2026-09-04・完了）で、主LLM は `ModelResource` の型枠に乗った。** `設計図` ③-2 の
出入り口4つからも「**主LLM は 出-c で資源にしたので外れた**」と明記されている。

資源への投げっぱなしは、**既に QC を通る道がある**（`search_deferred`・`fetch_deferred`）。
主LLM だけが同期で居残っている。

### 主LLM の中で道具を使わせる形は採らない

「主LLM に道具を渡し、その内側でツールループを回す」形は、**この機体で 735 行として動いて
いて、環-c で撤去した**（`設計図` の撤去台帳）。設計も「**多段 ReAct はしない**」
（`イベント駆動ループ`）「**ターン内多段なし＝現行の主LLM↔ツールループを撤去**」
（`直近の進め方と進捗`）と名指しで排している。

戻さない理由は4つある。

| | 主LLM の内側で道具を回す | 投げっぱなしにする（本課題） |
|---|---|---|
| 打ち切り | **効かない**（`await` の切れ目が消える。実機で「打ち切った直後の反復が `fetch_deferred` を投げ、返事も1つ余計に出た」） | 効く（世代で捨てる・既存の仕組み） |
| W | **古いまま**（毎反復 O から作り直す原則が崩れる） | 毎反復 作り直す |
| `deferred` | **待つことになる**（検索は実測 平均2.5秒・最長22.1秒） | 投げっぱなしのまま |
| O の書き手 | **2つ**（ループの管理外で記録が増える） | ループだけ |

## 2. 対応

### 反復を2種類にする

| 反復 | 中身 | 軽量LLM | 数える |
|---|---|---|---|
| **決める反復** | 取込 → 想起 → 調停 → 出力（発話／調べもの投げ／**主LLM 投げ**） | 回す | **数える** |
| **出す反復** | 取込 → **主LLM の決定をそのまま実行**（発話 or 道具投げ）→ 閉じる | 回さない | **数えない** |

**`_iterations`（上限5）は軽量LLM を回した反復だけを数える。** 上限の意味が変わらない。

**「出す反復」で調停を回さない。** 回すと軽量LLM が主LLM の決定を覆せてしまい、「そのまま
出す」と矛盾する。主LLM の決定は決定として扱う。

### 主LLM へ道具を渡す

投げるときに、その反復の道具の定義（`_tools()` が組むもの）を一緒に渡す。上限に達した反復で
`say` だけに絞る現在の扱いも、そのまま持ち込む。

### 返りは QC で受ける

QC の要素は現在 `(語, 結果, 意図id, 種別, 番号)` で、**結果が文字列**である。主LLM の返りは
**道具の呼び出し**なので、QC が運べる形へ広げる。

### 記憶は残る

「出す反復」が `_speak` → `_finish` を通れば、いまとまったく同じものが書かれる。

| `_finish` が書くもの |
|---|
| `自分が答えた：…`（`direction="発話"`・役割 `答え`・親は求め） |
| やりとりの関係（`_close_exchange` で区間を切り出し、順序つきで束ねる） |
| 共起（`_record_cooccurrence(memories, 新しい id)`） |
| 要約・内省（背景タスクへ） |

**通す経路を変えない。** これが本課題の前提である。

## 3. 持ち越さないと欠けるもの（未決）

### ① `memories`（想起した W）

`_finish` が `_record_cooccurrence(memories, …)` へ渡す。**共起は「ある反復で一緒に活性した
記録の集まり」**なので、**主LLM が実際に見た W** でなければ意味がない。「出す反復」で想起し
直すと別の W になり、**主LLM が見ていない記録と共起させてしまう**。

### ② `_w_id_map`（W に出した12桁 id → 完全な id）

`_apply_memory_verdicts` が使う。主LLM は **W に印字された id** で申告を返すので、その W を
作ったときの対応表でないと引けない。いま `_w_id_map` は `_compose_workspace` が毎回上書き
する。

### ③ 危険：飛行中に別の完了が届くと、対応表が壊れる

```
調べもの2 がまだ飛んでいる
   ↓ 主LLM を投げる（決める反復）
   ↓ 調べもの2 の完了が先に届く
   ↓ 反復が起きる → 取込 → 想起 → _w_id_map が上書きされる
   ↓ そのあと主LLM の返りが届く
   ↓ 「出す反復」が、別の W の対応表で申告を引く
```

**申告が黙って別の記憶へ当たる。** 12桁で引くので当たらなければ落ちるだけだが、**当たって
しまえば気づけない**。

**塞ぎ方は未決。** 案は「主LLM の返りに `memories` と対応表を同梱して運ぶ」（求めの寿命の
状態に頼らない）だが、詰めていない。

### ④ つなぎと、機能のブロックとセットで考える（2026-09-09 の指示）

持ち越しの設計は、次の2つと**まとめて**決める。

**つなぎ。** 主LLM が考え込んでいるあいだ、何を話させるか。いまは `branch == "full"` かつ
`effort != "low"` かつ材料が届いた反復でないときに `_say_filler(decision.text)` を出す
（`I 内部設計根拠` の「同期二段生成」）。**投げっぱなしにすると、待ち時間の性質が変わる**
ので、いつ・何を出すかを決め直す必要がある。

**一部機能のブロック。** **調べものには既に同じ仕組みがある。**

```python
queues = ([self._completion_queue]
          if self._in_flight_count
          else [self._completion_queue, self._affect_queue, self._device_queue])
```

> **調査中は完了キューだけを待つ。** 飛行中の調査があるあいだに情動や人の出入りで別の連鎖を
> 始めると、1つの求めの途中に別の話が割り込む。聞いている側には、軽量LLM とフルLLM が交互に
> 喋る＝別々の人格が居るように聞こえる（実機で観測）。QA・QD は**消費せずキューに残す**ので、
> 調査が終われば順に処理される（取りこぼしではなく待たせるだけ）。代償：drive の発火と人の
> 入退室への反応が、その求めが終わるまで遅れる。

**主LLM を飛行中に数えれば、この守りがそのまま効く。** ただし代償（情動と人の出入りへの反応
が遅れる）も一緒に伸びるので、**主LLM の待ち時間ぶん遅れてよいか**を決める必要がある。

## 4. 詰めること（実装計画の前に決める）

| # | 決めること | 状態 |
|---|---|---|
| 1 | QC が道具の呼び出しを運ぶ形 | 未決 |
| 2 | `memories` と `_w_id_map` の持ち越し（③ を塞ぐ） | 未決 |
| 3 | 「出す反復」が版を書くか | **未決**（2026-09-09・これから考える） |
| 4 | つなぎをいつ・何で出すか | 未決（④ とセット） |
| 5 | 情動・機器のブロックを主LLM の待ちにも掛けるか | 未決（④ とセット） |
| 6 | 上限に達した反復で主LLM を投げてよいか | 未決 |

## 5. 完了条件

- `_iterate` が「決める反復」だけを回し、行数が減る（**数字で示す**）
- `_iterations` が軽量LLM を回した反復だけを数える
- **`自分が答えた：…`・やりとり・共起が、いまと同じに書かれる**（対で確かめる）
- 主LLM が飛行中でも、人に話しかけられたら打ち切れる（世代で捨てる）
- `ruff` / `mypy` / 全体テストが緑
- `課題8` の 環-f-ろ を、本課題へ吸収したものとして書き換える

## 6. 影響

- **環-f-ろ を含む。** 環-f は残り 環-f-い（IIF・待ち行列の統合）だけになる
- 環-e-に の に-5（`_iterate` を薄くする）は、本課題で構造的に解ける。**A（結末を束ねる）と
  B（材料を出す）は、割ってから要る分だけやる**
