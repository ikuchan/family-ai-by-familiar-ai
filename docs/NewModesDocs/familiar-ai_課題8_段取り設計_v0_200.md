# familiar-ai 課題8 段取り設計（段階的 TDD 改造の順序と依存）（v0.200）

## 1. 依存の事実（コード根拠）

- **スコアは1関数に集約**：`final_score = cosine × time_score × importance`（memory.py:70/:86）。ここが新5軸への切替点。
- **recall 系統は多数**：主 `recall`（:761）に加え、`recall_self_model`／`recall_curiosities`／`recall_semantic_facts`／`recall_behavior_policies`／`recall_day_summaries`／`recall_divergent`／`recall_on_this_day`／`recall_revisions` 等（同期・async 併せ十数個）。**1経路へ統合**が要る。
- **旧系統の規模**（gap 文書の段階移行対象・ファイル数）：workspace 15／prediction 8／interoception 7／social_policy 3／appraisal 3／concern_engine 3／attention_schema 1。
- **廃止/移管ストアの結線数**：self_narrative 15／tape 11／pending_store 11／GlobalWorkspace 6／relationship_state 4／memory_links 3／exploration_state 0（既に未結線）。
- **現行先行作業**：BUG-1（冪等化＋purge・完了）、bge-m3 移行（完了・EMBEDDING_DIM=1024）。新アーキとは独立で、再測定の前提。

## 2. 段取りの原則

- **recall 一本化が起点**：旧 cosine 純積と新5軸を**並走させない**。新スコアラへはクリーンに切り替える（フラグで旧/新を持つにしても、同時に二重採点しない）。
- **現行先行（Phase 0）は新アーキと独立**に進む（BUG-1・bge-m3）。再測定はこの後。
- **基盤（O/MI データモデル）が先、recall 一本化が次**：5軸は O の上で動くため、O モデルが無いと意味を持たない。
- **旧系統の撤去は最後**：新経路が通って実証されてから、grep 残存ゼロを完了条件に撤去する。
- **DB 変更はマイグレーション＋テスト＋ロールバック**を各フェーズに含める。カラム・ストア廃止は「旧名 grep で0件」を完了条件にする。
- **一項目ずつ**：各フェーズはさらに小ステップに割り、ステップ毎にユーザー確認。全体テストは `./scripts/run_tests.sh` で自分（Claude Code）が回す。

## 3. フェーズ順序（2026-06-29 に承認した計画。現在地は §7 の表）

### Phase 0：現行先行（並行・Claude Code 進行中）
- 内容：BUG-1（`save_async_with_id` 冪等化＋既存重複 purge）、bge-m3 移行（EMBEDDING_MODEL/DIM・e5 プレフィックス除去・situated 列 384→1024・全再埋め込み・mu 再計算）。
- 依存：相互順序＝BUG-1 完了 → bge-m3 移行。新アーキ各フェーズの前提ではないが、**再測定（c_lo/c_hi）の前提**。
- 完了条件：重複検索0件／全 situated 1024次元／旧モデル名・旧次元の残存ゼロ。
- チェックポイント：再測定を回せる状態。

### Phase 1：基盤＝O / MI データモデル（最大・最重要）
- 内容：O 一元化（observations → O・全 O＝自己体験）、MI 構造と自己認識 MI、T レジスタ、相関サブテーブル（person 別・situated 先行形を活用）、W 派生ビュー（退避/eviction/fade なし）。スキーマ＋マイグレーション中心。**recall は旧のまま温存**（まだ切替えない）。
- 依存：Phase 0 の bge-m3（O の埋め込みは bge-m3）。
- テスト観点：O への移行で既存観測が欠落・重複しない／T レジスタの読み書き／相関サブテーブルの person 別想起。
- チェックポイント：旧 recall が新 O 上でも従来どおり動く（回帰）。

#### Phase 1 の詳細ステップ（薄い縦切りの計画）

大枠の順序＝**MI 構造 → T レジスタ → 相関サブテーブル → O 一元化と撤去**（MI 構造が土台。O 一元化も PI 化も相関も「MI とは何か」が決まらないと移せない）。各ステップは1つの薄い縦切り＝pytest で通る最小の一本とし、「調査 → 設計方針（原因と対応案）→ 承認 → TDD 改造 → pytest 確認 → ユーザーの全体テスト」で進める。**未実装のステップは予定**であり、実装が済んだら各ステップに進捗を追記する。

**実機テスト（`./run-gui.sh`＝`uv run familiar --gui`・普段の運用と同じ GUI で対話）は、下表の「実機」印のステップ後に必ず挟む。** pytest が「壊れていない」を、実機が「自然に振る舞う」を担保する二重確認。全ステップでなく、記憶・感情・想起の体感が変わる節目に絞る。試すシナリオは各ステップの設計方針を詰める段で具体化する（下表は暫定案・ユースケース②〜⑥も素材に使える）。

段階A（MI 構造の確立）：
- **A-0【完了】読み出しアクセス層寄せ**＝下の実装済み進捗（最初の一本〜4本目）。`_read_observations_by_kind` に4本を寄せ、単一・複数 kind に対応済み。段階A の接続点。
- **進捗【実装済み・4本目＝読み出し層寄せの締め】**：`recent_feelings`（`kind IN ('feeling','conversation')`）を寄せるため、層の kind 引数を **`str | tuple[str,...]`** に拡張（複数値は `kind IN (...)`）。**層自体を変える初の一本**だが、str 分岐 SQL は従来とバイト同一で後方互換（既存3本は無変更・str 経路の回帰テストで保証・RED でタプル経路の失敗＝`operator does not exist: text = record` を確認）。追加テスト6件＋既存15件＋非回帰97件・全体テスト通過。**これで単純な kind 絞りの observations 読み出しは層に集約され、Phase 1 の読み出し層寄せは一区切り**（curiosities／self_model／day_summaries／recent_feelings の4本＋層の単一・複数 kind 対応）。
- **読み出し層寄せの締め＝残る recall の申し送り**：層に寄せないものを理由付きで確定。別テーブルを読む（`recall_semantic_facts`＝semantic_facts／`recall_behavior_policies`＝behavior_policies／`recall_revisions`＝memory_revisions）、日付・時刻演算（`recall_on_this_day`＝EXTRACT MONTH/DAY／時刻近傍）、集約・存在確認・dedup・複合条件（situated 近傍を含む主 recall 系）は、いずれも「kind と person_id で新しい順に読む」形から外れ、検索・絞り込みロジックを含む。これらは**Phase 2 で「層の取り出しメソッド＋5軸スコアラ」に分解**する。渡し方の骨格（確定）：層は生 SQL/WHERE/LIKE を受けず、取り出しパターンごとの専用メソッド（`by_kind` 実装済み・将来 `by_vector`＝situated 近傍・`by_date` 等）で**構造化値**を受ける。LIKE 検索は O 一元化後に situated 近傍（r 軸採点）へ置き換わる見込みで、専用メソッドを残すかは Phase 2 で再考。データモデル本体は未着手。
- **A-1【実装済み・最初の一歩】observations を器（MentalItem）として読む**（スキーマ変更なし）。基底クラス `PrimitiveMentalItem`（emotion／drive・A-1では未設定）と拡張クラス `MentalItem`（＋id／content／vector／supersedes／根づき）を `memory.py` に導入。観測行から器を組み立てる変換関数 `_row_to_mental_item` を新設。`recall_self_model` の取得列に id／superseded_by／importance を加え、器を組み立てる経路を通す。**器は返り値には使わず、返り値の辞書は従来どおり**（外部挙動を保つ・器の利用は次の一本に分ける）。emotion・drive・vector は未設定（None）で、感情のPAD化・埋め込み取り込みは後続の段階。アクセス層 `_read_observations_by_kind` は無変更（取得列を増やしただけ・本体は git diff で無差分を確認）。テスト（`tests/test_mental_item.py` 新規3件）：変換関数の組み立て／継承関係／`recall_self_model` の外部挙動保存。既存関連テスト非破壊（115 passed）・全体テスト通過。**実機テスト：不要**（内部表現のみ・体感不変）。
- **A-2【調査完了】MI 属性最小化の準備**。器の属性を最小化する段階 B に備え、既存コードが観測を読み出すときに呼び出し側へ返していた属性を洗い、それぞれが新しい器（MentalItem）のどの属性に対応するかの対応表を作る調査。この段では廃止しない。実機テストは不要（調査のみ）。

  **当初想定と実環境の食い違い（記録）**：着手前に廃止候補として挙げていた名前（state_type・source・status・target・persist・pose・meta・urgency・novelty）は、observations テーブルのカラムには存在しなかった。これらは設計 [D-MIモデル] が廃止と定めた旧属性の名だが、現行の observations の実カラムではない。また当初 A-2 の対象に含めていた date・time カラムは、マイグレーション 016 が timestamp を TIMESTAMPTZ へ正規化した際に既に削除済みで、本番データベースに存在しない。A-2 の対象は「観測を読み出すコードが返していた属性」であり、DB のカラムそのものではない。この取り違えを記録に残す。

  **観測を読み出すコードが返していた属性と、器（MentalItem）への対応表**（対象は observations を読む経路のみ。semantic_facts・behavior_policies・完了キュー等の別テーブル経路は含めない）：

  | 返り値の属性 | 由来（memory.py） | 器への対応 |
  |---|---|---|
  | memory_id／id | row["id"] | 器 `id` |
  | summary | row["content"] | 器 `content` |
  | emotion | row["emotion"] | 器 `emotion`（現状は感情ラベル文字列。PAD 化は段階 B・[D-MIモデル]） |
  | timestamp | row["timestamp"] | 器の属性ではなく store メタデータ。ここから date・time を導出 |
  | date | `_ts_to_date(timestamp)` | 器に持たない。timestamp からの派生（DB でも 016 で削除済み） |
  | time | `_ts_to_time(timestamp)` | 器に持たない。timestamp からの派生（同上） |
  | direction | row["direction"] | 器に写さない。kind の言い換え（本番で kind と行ごとに一対一を確認） |
  | kind | row["kind"] | 器は kind を持たない設計（意味は content へ・[D-MIモデル]）。ただし読み出しの絞り込みに現用のため当面残す（整理は段階 B 以降） |
  | source_kind | row["kind"] | kind の重複（同じ row["kind"] を別キーで再掲）。器に持たない |
  | image_path | row["image_path"] | 器に写さない。将来の顔認識・画像想起（DIF 知覚）まで休眠。廃止の実装はその仕組みができてから（本番で 2624 件中 1 件のみ値を持つ） |
  | 根づき の素 | row["importance"] | 器 `根づき`（現 importance の一般化）。(a0,n) 導出は A-3 |
  | supersedes の素 | row["superseded_by"] | 器 `supersedes` |
  | score／confidence／retrieval_method | 検索経路が算出 | 記憶そのものの属性でなく想起（5軸スコアリング）の産物。器に持たず Phase 2 のスコアラが付す |

  vector（器の属性）は、この読み出し経路では返していない（埋め込みは別テーブル situated_embeddings。器への取り込みは後続の段階）。drive（基底 PI の属性）も観測の読み出しにはなく、T レジスタ由来で段階 B の PI 構築で入る。

  **後続段階への申し送り**：recall_count・last_recalled_at（017 で追加）は 根づき の導出と新しさ減衰を担うため A-3（根づき の (a0,n) 導出）で役割を再編する。writer_id・subject_id・participants_json・scope（012 で追加した視点列。読み出しの絞り込みには未使用）は段階 C（person_id 所有者絞りの撤去と situated 相関整理）で相関サブテーブルへ整理する。
- **A-3【一部実装済み】根づき の (a0,n) 導出**。importance を 根づき へ一般化し、(a0,n) 保存・on-read 導出の器を作る（値の確定は課題5）。
  - **A-3-1【実装済み・A-3 の最初の一歩】(a0,n) 保存形式と導出関数**。マイグレーション `2026-07-02-021_groundedness_g0_n.py` で observations に `groundedness_g0`（REAL NOT NULL・既定1.0）と `groundedness_n`（INTEGER NOT NULL・既定0）を追加。既存行は `importance` を NULL 防御（1.0）してから `groundedness_g0 = importance` で簡易移行（`importance` 列は削除せず残す）。`memory.py` に導出関数 `_derive_groundedness(a0, n, *, floor=0.0, c=2.0, epsilon=0.001, step=0.33)` を新設（a0 を [floor,c] で正規化しロジットで無限区間へ写し、n·step を足してロジスティックで [floor,c] へ戻す・n=0 で a=a0）。**導出値は想起スコアへ未接続**で、`recall` は従来どおり `cosine × time_score × importance` で `importance` を読む（`_derive_groundedness` の呼び出しは定義とテストのみで稼働経路ゼロ）。よって**外部挙動は不変**。step の既定は課題5 で確定した 0.33（取込 a0=0.75 から評価5回で実用上限1.5 に達する効き幅・キーワード引数で差し替え可・Config 配線は後続）。テスト（`tests/test_activation_derive.py` 新規7件）：純導出5件（n=0 恒等・n に単調・両端 floor/C へ漸近・±1 対称・a0=0.75 で5回1.5到達）とマイグレーション DB2件（列追加確認・importance→groundedness_g0 移行確認）。**実機テスト：不要**（未接続で体感不変）。
  - **A-3 の Phase 1 残務【記述で完了・コード変更なし】recall_count・last_recalled_at の役割再編（対応づけの確定）**。A-2 で申し送った二列（017 で追加）を調査し、旧 recall の `_compute_final_score`（`final_score = cosine × time_score × importance`）と `_mark_recalled` の中だけで使われることを grep で確認（機械想起 conversation で `recall_count += 1` と `last_recalled_at = now()`、spontaneous で `last_recalled_at` のみ更新・system は無更新）。確定した対応づけ：`recall_count` は新しさ（t 軸）の若返り回数（`time_decay` の reinforce＝半減期倍化）、`last_recalled_at` は若返りの起点リセットに対応する。**根づき の `n`（評価由来の正味デルタ）とは別系統で、想起回数からは引き継がない**。更新トリガは機械想起からフルLLM の参照申告へ移す（[D-想起合成]「機械想起では 根づき も freshness も触らない」）。二列は旧 recall スコアリングに閉じているため、Phase 1 では現状維持（旧 recall が読む・外部挙動不変）とし、コード再編は 5軸スコアラを載せる Phase 2 で行う。詳細は活性の別紙 v0.2 §4。
  - **A-3 の残り【予定・Phase 2】**：導出値を想起スコアへ接続（Phase 2 の5軸スコアラで importance の代わりに導出 a を使う）、段2 の評価による `n` の増減機構、参照した MI の新しさ（t 軸）の若返り、解決時の a0 再測定。上の recall_count・last_recalled_at の再編コードもここに含む。テスト観点：(a0,n) からの導出が単調・可逆／既存 importance 読み出しと互換。**実機テスト：必要（節目①）**＝記憶を問う対話（ユースケース⑥）で、想起される記憶が従来と大きくずれないか体感確認（導出が想起へ効く段で行う）。

段階B（T レジスタの分離）：
- **B-1【実装済み・薄い縦切り】mood（PAD）レジスタの器**。`mood_register.py` を新設し、4軸 PAD の器 `MoodPAD`（P／Pn／A／Dom・全軸 [0,1]・中立0.5・`clipped`／`to_json_dict`／`from_json_dict`）、各軸を平静 M_rest=(0.5,0.5,0.5,0.5) へ半減期 HL_M=600秒（課題5 確定）で収束させる純関数 `decay_to_rest`（`rest+(x−rest)·2^(−経過/HL_M)`・DB 非依存）、agent_state への読み書き `load_mood`／`save_mood`（state_key `mood_pad`・self_state と同じ upsert・行が無ければ中立を返す）を置いた。収束先は各軸0.5の中点で、time_decay の floor 減衰とは別式。**emotion 文字列→PAD 写像 φ（課題11k・写像値 未確定）にも既存 mood にも未接続**＝agent.py の `_mood`／`_decayed_mood` と AffectiveState と mental_state_log は無変更で外部挙動不変（撤去は Phase 6）。φ 接続（旧データ移行と実行時 PAD 設定・評価器 LLM が P/Pn/Dom 直接出力・A は機械 A）は課題11k 確定後。新規マイグレーションなし（agent_state の新キー1つ・DDL なし）。テスト：`tests/test_mood_register.py` 新規7件（収束を上からと下からの両側・両軸独立漸近・範囲クリップ・agent_state 保存/読み出し往復・キー不在時の中立既定）。**実機テスト：不要**（未接続で体感不変・B-2 とまとめて節目②で確認）。
- **B-2【実装済み・接続済み】drive（5欲求）レジスタ**（2026-09-10 確認。器を置いた当時は「薄い縦切り・器のみ」だったが、`loop/tonic.py` が `core/drive_dynamics.py` を回すようになった）。`drive_register.py` を新設し、5欲求（SEEKING／REST／BOND／SAFETY／ESTEEM・集約は課題6-2 確定）の器 `AiDrivers`（各軸 float・[0,1]・静止（既定）0.0・`clipped`／`to_json_dict`／`from_json_dict`）、agent_state への読み書き `load_drives`／`save_drives`（state_key `drive5`・self_state と同じ upsert・行が無ければ全0.0 を返す）を置いた。**器と永続化だけ**で、蓄積と放電と mood 変調（dynamics）は作っていない。**既存の生きた15欲求 `DesireSystem`（agent_state["desires"]）にも `as_coalition` 経路にも未接続**＝desires.py と既存の "desires" キーは無変更で外部挙動不変（撤去は Phase 6）。旧15→新5 のデータ移行はしない（マッピングは課題6-2 で記録済み・移行は挙動切替の段）。新規マイグレーションなし（agent_state の新キー1つ・DDL なし）。テスト：`tests/test_drive_register.py` 新規5件（静止既定＝全0.0・範囲クリップ・JSON 往復・agent_state 保存/読み出し往復・キー不在時の全0.0 既定）。**実機テスト：不要**（未接続で体感不変）。
  - **B-2 の残り【予定・後続段】**：蓄積 dynamics（[D-活性] の `drive_i ← clip(drive_i + rate·mult·learn·g_{D,i}(M)·P_T, 0, 1)`・共通レートとバイアス b_i と変調行列 C_{ij} は課題5 B と発火・mood 別紙の暫定値）、全放電（発火時 −放電量）、mood M による変調 g_{D,i}(M)、PI.drive への surface、旧15→新5 のデータ移行と挙動切替。dynamics は mood レジスタ（B-1）の値と発火に依存するため後続。**実機テスト：必要（節目②）**＝数回の対話で、感情が自然に変化し平静へ戻るか・欲求が極端に振れないか体感確認（dynamics と surface が効く段で行う）。
- **B-3【実装済み・薄い縦切り】PI 構築と PI→MI 拡張（構築関数のみ）**。`tif.py` を新設し、`build_primitive(emotion: MoodPAD, drive: AiDrivers) -> PrimitiveMentalItem`（発火ペイロード構築・emotion←M・drive←D）と `expand_to_mental(pi, *, id, content, vector=None, supersedes=None, 根づき=None) -> MentalItem`（I 取り込みでの PI→MI 拡張・pi の emotion と drive を引き継ぎ id 等を付与）の二つの純関数を置いた。emotion に B-1 の `MoodPAD`、drive に B-2 の `AiDrivers` を値として流用し、A-1 の器（`PrimitiveMentalItem`／`MentalItem`／`_row_to_mental_item`）は無変更（注釈 `object | None` のまま）。**実際の発火とループには未接続**で、両関数は稼働経路から呼ばれない＝外部挙動不変。DB 不使用の純関数（マイグレーションなし）。テスト：`tests/test_tif.py` 新規3件（`build_primitive` が M と D を運ぶ・`expand_to_mental` の引き継ぎと付与・`MentalItem` が `PrimitiveMentalItem` のサブクラス）。**実機テスト：不要**（未接続で体感不変）。
  - **B-3 の残り【予定・後続段】**：I→T Nudge を MI として発し T が emotion をフィルタ（[D-T境界]）、N_PAD（W の MI の emotion を 根づき 重みで合成）、発火とループへの `build_primitive`／`expand_to_mental` の接続。いずれも想起（W・Phase 2）と MI.emotion の PAD 化（課題11k）と発火に依存するため後続。

段階C（相関サブテーブルの整理）：
- **C-1【実装済み】person_id 所有者絞りの撤去**。代替の相関経路を先に作り、そのあと所有者絞りを外す二段で進めた。第一段：situated 相関の読み出し層 `_read_observations_by_situated(person_id, n, columns, *, kind=None, keywords=())` を新設（`situated_embeddings s` を `observations o` に JOIN し `s.person_id` で紐づける・所有者に依らない母集合・timestamp DESC・ベクトル類似度は使わない・未接続・テスト10件）。第二段：`recall_day_summaries` をこの層へ付け替え、`observations.person_id` 所有者絞りを撤去（母集合が在席者相関へ変わる・戻り値の形は不変・既存 day_summary テスト4件を相関の意味論へ更新）。フォールバック二関数 `_recall_keyword_fallback`／`_recall_recency_fallback` は C-1 対象から外した（主 situated 経路が0件のときだけ発火し、その0件は「その person の situated 行が無い」ときに限るため、同じ situated 相関へ寄せると恒常的に空になる。所有者絞りのまま「situated 行を持たない観測」を拾う役目を残す）。「situated 行を持たない観測のフォールバック扱い」は別課題へ申し送り。完了条件（達成）：`recall_day_summaries` から `observations.person_id` 所有者絞りが消えた（書き込み側 `delete_day_summaries_for_date` とフォールバック二関数は対象外）。**実機テスト：不要**（C-2 とまとめて節目③）。
- **C-2【実装済み・設計整理のみ・実行時の挙動不変】situated の役割整理**。situated_embeddings（obs_id, person_id, vector）を MI×person 相関の先行形として、二役割で整理・固定した。役割1＝視点シフト検索（`s.person_id=問う人`・本人・その視点に寄せた母集合とスコア）、役割2＝在席者相関 p（W 想起の第5軸・在席**他者**・**自分除外**・noisy-OR・[D-在席相関]）。C-2 の段では役割1（視点シフト検索）のみが生きていた。**その後、役割2＝在席者相関 p を実装した**（slice-1＝score 軸で `_score_breakdown` に第5軸 p/w_p、slice-2＝候補集合拡張で `recall_presence_expand` と在席他者視点の候補 union・r 補完）。想起経路は `_presence_correlation` と `_score_breakdown(..., w_p=recall_w_p, p=…)` を通る5軸合成である（薄い包みの `_compute_final_score` 単体は今も p を渡さない4軸）。AGENT_SELF の situated 行は自己の中立視点（役割1の自己スコープ・`perspective_vec` 不在なら素の記憶ベクトル）で、p 軸の自分除外とは別物。**実機テスト（節目③・在席者に応じた想起変化）は、顔登録＝S を伴うため R と D の後へ置く（戦略・v0.24）**。

段階D（O 一元化の残りと撤去・最後）：
- **D-1【予定】旧テーブルの O 統合**。episodes・episode_memories・memory_events・pending_speech・unfinished_business 等を O へ統合、semantic_facts・behavior_policies は O 外の昇格処理へ。1テーブルずつ薄く。テスト観点：統合で既存データが欠落・重複しない（移行の要）。マイグレーション＋ロールバック。**実機テスト：必要（節目④・テーブルごと）**＝統合したテーブルに関わるシナリオ（例：記憶を問う対話・②〜⑤の該当機能）で、統合前と挙動が変わらないか確認。
- **D-2【予定】旧テーブル・旧クラスの撤去**。memory_links・exploration_state・self_narrative_log・relationship_state と関連クラス（DefaultModeProcessor・ExplorationTracker・SelfNarrative・RelationshipTracker・TAPE 等）を撤去。**新経路が通って実証されてから**。完了条件：旧名・旧クラスが grep で0件。**実機テスト：必要（節目⑤・Phase 1 総合）**＝主要シナリオを一通り試し、Phase 1 前と体感が劣化していないか総合確認。

**TUI 実機チェックポイント一覧（節目のみ）**：

| 節目 | 直前ステップ | 試すシナリオ（暫定案） | 見るポイント |
|---|---|---|---|
| ① | A-3 導出の想起接続（A-3-1 は未接続で対象外） | 記憶を問う対話（UC⑥） | 想起される記憶が従来と大きくずれないか |
| ② | B-2（感情・欲求が動く） | 数回の対話 | 感情が自然に変化し平静へ戻るか・欲求が極端に振れないか |
| ③ | p 軸（在席者相関・実装済み） | 在席者を想定した対話 | 想起が在席者に応じて変わるか（p 軸は slice-1 score＋slice-2 候補集合拡張とも実装済み。実機確認は顔登録＝S の後・戦略上 R と D の後に置く） |
| ④ | D-1（O 統合・テーブルごと） | 統合テーブル該当シナリオ | 統合前と挙動が変わらないか |
| ⑤ | D-2（撤去・Phase 1 総合） | 主要シナリオ一通り | Phase 1 前と体感が劣化していないか |

**A-1・A-2 完了、A-3-1 実装済み、A-3 の Phase 1 残務も記述で完了、B-1・B-2・B-3 も実装済み**（段階B の Phase 1 スライス完了＝B-1 `mood_register.py`／B-2 `drive_register.py`（`AiDrivers`）／B-3 `tif.py`（`build_primitive`・`expand_to_mental`）・いずれも器または構築関数で未接続）。A-3 の残りは Phase 2、B-2 の残り（蓄積 dynamics ほか）と B-3 の残り（Nudge・N_PAD・発火接続）は後続段。**C-1（person_id 所有者絞りの撤去）実装済み**（`_read_observations_by_situated` 新設＋`recall_day_summaries` 付け替え・フォールバック二関数は別課題へ申し送り）。**C-2（situated の役割整理）実装済み**、加えて**役割2＝在席者相関 p も実装済み**（slice-1 score＋slice-2 候補集合拡張・`_presence_correlation`／`_score_breakdown`／`recall_w_p`／`recall_presence_expand`・実機テストは顔登録＝S を伴うため R と D の後）。**MI 集約段の設計は一通り確定**（系統A＝self_model→自己認識 MI 自己エピソード部／curiosity→cue O、系統B＝キーレス supersede＋content 注記の信念 MI、situated V2＝型つき関係エッジ、自己認識 MI＝核/Config/自己エピソード/policy＋プロンプトキャッシュ整合の構築規約・MIデータモデル §7／[D-在席相関/V2]／[D-自己認識分離]）。**いずれも更新機構が REST 内省に依存するため実装本体は Phase 2 寄り**（REST 詳細＝周期・閾値は課題10）。**Phase 1 で入ったのは situated V2 の schema 器のみ**＝スライス1（`relation_key` 列・2026-07-12-022）とスライス2（UNIQUE を relation_key 込みへ・2026-07-12-023・`_upsert` キー化）で、いずれも生成 presence のみ・挙動不変。**slice-3 以降（視点列から presence/speaker/subject の関係生成・person_id 削除・旧 `_remember` 撤去）は、書き込みが視点列を実質埋めていない＝在席検出・話者帰属（[D-知覚]）が入る Phase 2 に依存するため申し送り**。着手時は実環境の最新ソースを確認してから、調査 → 設計方針 → 承認 → TDD 改造の順で進める。

#### Phase 1 実装済み進捗（読み出し層寄せ＝段階 A-0）

- **進捗【実装済み・最初の一本】**：薄い縦切りの起点として、読み出しアクセス層の最初の実体を実装。`memory.py` に `_read_observations_by_kind(kind, person_id, n, columns)` を新設（observations を kind／person_id で絞り新しい順に n 件読む機械的読み出し・採点や想起判断は持たない＝課題13c の dumb な層）し、最も単純な `recall_curiosities` をこの層経由へ付け替え（生 SQL 除去・外部挙動不変・スキーマ変更なし）。新規テスト7件＋既存110件 pass・全体テスト通過。次の一本＝emotion を含む `recall_self_model` を同じ層へ寄せ、`columns` が emotion 込み経路でも機能するか確認（layer の再利用性を1本広げる）。データモデル本体（O 一元化・T レジスタ・相関サブテーブル・emotion の PAD 化）は未着手。
- **進捗【実装済み・次の一本】**：`recall_self_model`（emotion を返す）を `_read_observations_by_kind` 経由へ付け替え。`columns=("content","timestamp","emotion")` を渡すだけで**層のコードは無変更**（git diff で層ゼロ差分を確認）＝emotion 込み経路で層の再利用性を実証。外部挙動不変・スキーマ変更なし。追加テスト4件（emotion の distinct 値が変わらず返ることを含む・実スキーマは `emotion TEXT NOT NULL DEFAULT 'neutral'` のため NULL ケースは検証対象外と判断）＋既存97件 非回帰・全体テスト通過。次の一本は `recall_day_summaries` 等の残る単純 recall。
- **進捗【実装済み・3本目】**：`recall_day_summaries` を層経由へ付け替え。person_id が固定の AGENT_SELF_ID でなく **`self._person_id`（在席者スコープ・実行時に決まる値）**でも**層のコードは無変更**（git diff で層ゼロ差分）＝層の再利用性を「AGENT_SELF_ID 固定・emotion 込み・在席者スコープ」の3パターンで実証。外部挙動不変・スキーマ変更なし。追加テスト4件（在席者スコープの分離テストを含む・person_id は persons への FK があるため既存の予約 ID＝DEFAULT_PERSON_ID/AGENT_SELF_ID を使用）＋既存97件 非回帰・全体テスト通過。次の一本は `recall_semantic_facts`・`recall_on_this_day` 等、kind 単純絞りでない recall（クエリ検索・日付演算を含む）で、層をそのまま使えるか別途検討が要る。

### Phase 2：recall 一本化＝5軸スコアラ（起点の切替）
- 内容：旧 cosine 純積（:86）→ 新5軸（埋め込み平均中心化 → r 段階的関連係数〔ハード veto なし〕／t 新しさ／e 感情一致／a 根づき／p 在席者相関 noisy-OR／加重平均 M／min_score 足切り）。recall_* 群を1経路へ統合。**旧スコアと並走させずクリーン切替**。
- 依存：Phase 1（O モデル）＋ Phase 0（bge-m3 ベクトル）。p の在席入力は Phase 3 まで暫定（在席ゼロ扱い可）。
- テスト観点：5軸スコアの単調性・min_score 足切り・r 段階化（関連を veto しない）・noisy-OR の在席ゼロ時の分母処理・recall_* 統合後に各用途が従来近い結果を返す。
- チェックポイント：**ここで再測定の min_score を確定**（c_lo/c_hi は Phase 0 後に測れるが、min_score は5軸実装後）。

#### Phase 2 の締め：境界の切り出しと生存確認の不変条件〔決定・未着手〕

Phase 2 を閉じる前に、`agent.py`（4,024行）と `tools/memory.py`（2,593行）を、**一度に読める大きさ**へ分解する。目的は二つある。バグを減らすことと、人と LLM の双方が全体を把握できる状態に戻すことである。

**この段でやること／やらないこと**

- **やる**：副作用の境界を切り出す（永続化・時刻・発話・ストアアクセス）。これらは新ループになっても残る概念なので、Phase 5 で捨てる投資にならない。
- **やる**：端から端まで通る少数の不変条件をテストに置く。
- **やらない**：ターン内イテレーションの入れ子（`run()` の制御構造）の作り替え。[D-反復出力] で撤去が決まっており、撤去される構造へ投資しない（Phase 5 のまま）。

**根拠**

2026-07-20 に3件の不具合を見つけた。`say()` で話したターンが永続化されない（`run()` 795行の約590行目に埋もれた条件式）、感情語の一致を辞書サイズで割っていて値踏みゲートを越えない（`appraisal.py`）、観測 `timestamp` が9時間ずれる（`memory.py` の1行）。関数の長さが直接の原因なのは1件目だけで、3件に共通するのは**誰も端から端まで確かめていなかったこと**である。最後の書き込みが 2026-06-29 で、3週間気づかなかった。よって不変条件を先に置く。

置き場所を Phase 2 の締めにするのは、Phase 3 と Phase 4 が同じ2ファイルへ積み増すためである。Phase 3 は話者帰属を書き込み経路（`memory.py`）へ供給し、在席ゼロ時の発話ゲート（`agent.py` の `run()`）に触る。Phase 4 は `say()` の呼び出し口に触る。分解しないまま積むと同じ失敗を繰り返す。

なお `memory.py` の分解は新しい作業ではなく、**v0.6 で Phase 2 へ申し送った宿題の履行**である（「残る複雑な recall は Phase 2 で『取り出しメソッド＋5軸スコアラ』へ分解」）。`agent.py` の副作用境界の切り出しは、どのフェーズにも割り当てが無かった作業として、ここに新設する。

**分割の単位**：`モジュール分割設計 v0.1` に切り出した。判断の基準は「設計が定めたコンポーネント（[D-I内部]／[D-周期]／[D-B分離]）に名前を合わせる」と「変わりそうな判断を隠す単位で切る（Parnas 1972）」の二つで、依存は内側へ向ける（Ports and Adapters）。目標構成は `core/`／`store/`／`loop/`／`io/`／`legacy/`。撤去予定を `legacy/` へ隔離しておくことで、Phase 6 の撤去をディレクトリの削除に近づける。

**順序**

1. **生存確認の不変条件**。分解の安全網。これ無しに2ファイルを切り刻むのは、壊れても分からない状態で大工事をすることになる。粒度は着手時に詰める。
2. **`store/` の切り出し**。O とアクセス層（`by_vector`／`by_date` 等の専用メソッド・構造化値で受ける）、および時刻の一元化（`clock.py`）。
3. **`core/recall_score.py`**（W 構築の5軸スコアラ）→ **`loop/evaluator.py`**（設計に名前のある実体）→ **`loop/persistence.py`**（永続化の呼び出し口）→ **`legacy/` への隔離**。
4. **`min_score` の是正**（生コサインの閾値 → 合成スコアの床）。Phase 2 のチェックポイント。境界が整ってから行う。

**実機テスト**：必要。分解の前後で体感が変わらないことを確認する（挙動不変が完了条件）。

### Phase 3：知覚入力
- 状態：**認識の載せ替えは実装済み**＝InsightFace(ArcFace) 顔＋ECAPA-TDNN 声、GPU 実行（onnxruntime CUDA/cuDNN 解決済み）、presence_watcher が person_arrived/left へ結線。**残るは登録の入口（GUI/CLI）と実際の登録＝S**で、戦略上 R と D の後。p 軸のスコアラ結線は実装済み（在席入力が埋まればそのまま効く）。
- 内容：在席（InsightFace・DINOv2）、声紋話者帰属（ECAPA-TDNN）、VAD（silero・**512 サンプル@16kHz 単位**）＋発話バッファ → STT（faster-whisper int8/medium）と話者同定へ分配、顔×声＝融B。
- 依存：p 軸（実装済み）。登録＝S は R と D の後。
- テスト観点：VAD のフレーム条件（512）／発話バッファの終端確定／話者同定の閾値（enrollment 後）／在席ゼロ時の発話ゲート。
- チェックポイント：「誰が話したか」が相関サブテーブルへ供給される。

### Phase 4：音声出力
- 内容：TTS＝Style-Bert-VITS2（jvnv-M2-jp）、PAD→(style, style_weight) 写像、voice_guard 維持。STT/TTS の provider 抽象（`tools/tts.py`／`tools/stt.py`）。GPU 利用時 BERT float32。
- 依存：Phase 1（感情 PAD が MI/T にある）。Phase 3 とは独立に進められる。
- テスト観点：PAD→style 写像の代表点（平常/喜び/落ち着き/ささやき）／voice_guard の自発話聞き返し防止／合成失敗時のフォールバック。

### Phase 5：挙動（反復抑止・REST・拡散想起）
- 内容：[D-反復出力]（1反復1出力）、REST（近重複統合・内省・自己エピソード supersede・per-person 蒸留）、WR 拡散想起（memory_links 代替）。
- 依存：Phase 1（O/T）＋ Phase 2（recall）。BUG-1 の冪等化と整合（恒久の反復抑止）。
- テスト観点：同一意図の再発火が止まる／REST が日次 O を正しく統合／WR の seed と想起 MI 選択。
- **WR 拡散想起は W 想起の再帰化として設計し、旧 ToM ツールの第二 W を吸収する**：W が想起した MI が、その場にいない人の話を含むとき、その人を種にさらに想起して MI を W へ足す。1回の想起で閉じず、想起が想起を呼ぶ再帰である。旧 ToM ツール（撤去済み）は内部で独自クエリの想起を叩いており、これはターンの W とは別の第二の W を立てているに等しかった。この第二 W は WR の再帰想起が担うべきもので、新ループでは不在の人の考慮を WR へ畳む（ToM 自体は撤去済みで、現行は一人称 CoT が W の在席者と想起 MI の人を想像する）。
- **ToM のもう一方の働き（他者の心の想像）は「W を消費する一人称 CoT」に置き換え、WR と相互再帰で噛み合わせる**：応答の前に、いま W の文脈に出ている人それぞれの気持ちと望みを一人称で想像し、その上で自分として一人称で答える（三人称の視点分析レポートで応答を置き換えない）。この CoT は W の**下流**に置き、対象を固定の人リストでなく「W の文脈に出る人」で定義するので、W の作り方に依存せず、WR で W が深まればそのまま対象が増える。**想像が新たな想起を呼ぶ相互再帰（想像→その人を種に再帰想起→増えた人も想像）は上流の WR 側に置く**。この一人称 CoT 自体（対象は当面 W の在席者＋想起 MI の人）は Phase 2 の知覚が入った後に先行実装でき、WR が入れば自動で深まる（先行実装しても作り直しにならない）。

### Phase 6：旧系統の撤去
- 内容：**環-c（完了）**＝旧経路そのものの撤去。旧 `run()`、アイドル自発系、`EVENT_LOOP`、呼ばれなくなった層（interoception／social_policy／appraisal／attention_schema／meta_monitor／default_mode／mental_state と `GlobalWorkspace` クラス）。**環-d（未着手）**＝廃止ストア（tape／memory_links／self_narrative／relationship_state→案A／pending_store→O open 意図）の移管完了と撤去。
- 依存：環-c は 知-a。環-d は移管先が要るので 記-a・知-e・環-b の後。
- 完了条件：各旧名を grep して**残存ゼロ**（数えた件数でなく0件を証明）。**`getattr(self, "_x", None)` の形は `self._x` の検索に出ないので、判定に含める。**
- テスト観点：撤去後に新経路だけで全機能が成立する回帰。
- 撤去できなかったもの：`prediction`／`concern_engine`／`exploration` は `_run_post_response_pipeline` 経由で新経路が使う。`self_state` は後に**撤去した**（書き込みだけが生きており、読み手が居なかった）。`workspace.py` は `Coalition` が生きているのでファイルを残す（2026-09-10 に **`coalition.py`** へ改名。`loop/workspace.py` と名前が重なったため）。

## 4. 並行可否

- **並行可**：Phase 0（現行先行）は全体と並行。Phase 3（知覚入力）と Phase 4（音声出力）は Phase 2 後なら相互に並行可。
- **直列必須**：Phase 0(bge-m3) → Phase 2／Phase 1 → Phase 2 → (3,4,5) → Phase 6。撤去（6）は最後。

## 5. リスク

- Phase 1（O 移行）と Phase 2（スコア切替）が最大の山。データ移行の欠落・次元/型不整合・旧新スコアの取り違えに注意。各々マイグレーションのドライラン→確認→実行の2段。
- 旧系統の結線が深い（workspace 15・self_narrative 15）。撤去時に隠れ参照が残ると壊れるため、grep 0件を厳守。
- 現行先行（Phase 0）が未完だと Phase 2 の再測定が回せない。順序を守る。

## 6. 決定事項（承認済み・2026-06-29）

Phase 0（BUG-1・bge-m3 移行）は実機で完了。残る段取りの判断は次のとおり確定。

1. **着手フェーズ＝Phase 1（O/MI データモデル）から**。5軸は O の上で動くため基盤が先（Phase 2 は Phase 1 の後）。
2. **Phase 1 の刻み＝薄い縦切り**。最小の O＋MI で一通り通してから広げる（O 移行の欠落・不整合を小さく検出するため）。
3. **スコア切替＝フラグで旧/新を排他切替（並走しない）**。旧 cosine 純積と新5軸を同時に二重採点しない（取り違え防止）。
4. **min_score 確定＝Phase 2 実装後に再測定**。5軸スコアが動かないと測れないため、Phase 2 のチェックポイントで確定（c_lo/c_hi は暫定 0.25/0.50 を先行使用）。

---

承認済みにつき、最初に着手するフェーズ＝**Phase 1（O/MI データモデル）の TDD 改造方針**を、原因と対応案＋テスト＋マイグレーション＋完了条件の形で起こす。Phase 1 は薄い縦切りで、さらに小ステップに割って一項目ずつ確認しながら進める。

---

## 7. 残課題と順序（現在地）

### 全課題一覧（一つの表・着手順・2026-09-13）

**これが課題の正本である。** 束は「**何があれば進められるか**」で切り、**依存の鎖は束ごとに切り出す**（v0.178・2026-09-17）：
**A は机上で閉じる**（依存なし・2026-09-18 の実機で 5 件）、**B は机上で作って実機で確かめる**（依存なし・作ったら C の行になる・2026-09-18 時点で空）、**C は次の実機セッションで
確かめる**（互いに独立。起動 → 会話 → 無人の順に並べ、1 回のセッションで上から消化する）、**D は普段使いで貯まるのを待って測る**、
**E は見回りの鎖**（知-c の実機確認 → 知-i）、**F は人の登録の鎖**（知-c の後・人が揃ってから：知-d → 環-b → 環-d の残り／知-d → 知-e）。
束をまたぐ依存は **E-1（知-c）→ F-1（知-d）の 1 本だけ**で、A〜D は互いに独立。束の中は上から順に着手する。
完了したものは末尾の束 0 に残す（消さない）。未了の課題の詳しい中身は
下の各節と、`直近の進め方と進捗` の申し送りにある。**完了した課題の詳細（当時の設計方針・段取り・実測）と
旧番号との対応は `課題8_記録_完了した課題` に移した**（2026-09-17）。実機確認の段取りは
`実機確認シナリオ_ループと記憶の口` が正本で、この表は「何が未確認か」だけを持つ。

| 順 | 束 | 課題 | 項目 | 状態 | 待っているもの・中身 |
|---|---|---|---|---|---|
| A-1 | **A. 机上で閉じる**（依存なし） | **出-v** | **調べものの結果が W に届かない**（2026-09-18 新設・実機 12:35・quiet）：「よく考えて答えて、金木犀はいつ咲く？」→ 調停 `search_deferred` → 1 秒で成功（2,456 字・O に書かれた）→ 帰りの調停へ渡す W に**結果の本文が無く「検索を始めた」だけ** → 調停が 4 回同じ検索を選び（`すでに調べた語なので投げない`）、つなぎを 3 回言い、上限 5 で主LLM が「調べきれなかった」。09-16 16:54 は同じ経路で答えられていた | **実機確認済み**（2026-09-18 14:48）：`/speaker パパ` →「金木犀はいつ咲く？」→ 検索 1 回・帰りの W に `わたしが調べていたこと…結果が…`（パパの `present` 面・適合度 1.00）→ 154 字で答えた。根は知-t | いちばん先。会話が成り立たない |
| A-1b |  | **知-t** | **話者の指定に寿命**（2026-09-18 新設・出-v の根）：「分かっている相手」は `agent.speaker_known()` の 1 箇所——`/speaker` から 60 秒・その人に返事してから 60 秒（会話中は切れない）・顔照合。どれも無ければ T の tick が「不明」に戻し、想起は共通の面・system 文に名前を出さない・面に立てない・話者ゲートも空。09-17 の「話者の指定は残す」を撤回 | **実機確認済み**（2026-09-18 14:39・14:49）：`/speaker` から 60 秒で `話者の指定が切れた：パパ`。切れたあとの自発は独白 | 出-v と一緒に実機 |
| A-2 |  | **情-k** | **「沈黙が明けた」の求めが、誰も見えないのに立つ**（2026-09-18 新設・実機 12:38:07）：入口で止めた 0.1 秒後に `駆動体が機器を受領（沈黙が明けた）` → 調停が回り → 返事が `pending_speech` に溜まる。`check_silence_lifted` が「器に溜まりあり・黙っていない」で機器を積むが、**溜まったのは不在で聞いたもの**（理由「誰も見えなかった」）で沈黙とは無関係 | **完了**（2026-09-18・`イベント駆動ループ` v0.80）：`check_silence_lifted` は器の中で理由が「黙っていた」のものが 1 件でもあるときだけ積む。不在の分は人が映った求めに載る | 出-v の次 |
| A-3 |  | **知-r** | **退室の知らせに、居ないのに話す**（2026-09-18 新設・実機 12:36:22）：`在席の変化：誰か → （なし）` の求めで「お部屋から出ていかれたんですね」（`結末=発話` 48 字）。退室の瞬間はセンサの滞留窓（最後の検出から 180 秒）がまだ効いていて出口が通る | **完了**（2026-09-18・`イベント駆動ループ` v0.80）：機器「退室」の求めの発話は出口で常に独白（O には「考えたが言わなかった」） | 情-k と一緒 |
| A-4 |  | **知-s** | **`/speaker` 直後に在席表が失効**（2026-09-18 新設・実機 12:32:28）：打った 0.5 秒後に `在席表を失効：1 人（誰も居ないが 340 秒）`。失効の数え始めが `/speaker` より前 | **完了・実機確認済み**（2026-09-18 14:39：打ってから 60 秒で失効） | 小 |
| A-5 |  | **出-w** | **調停の `light` で text 空を「読めなかった」とフルへ倒す**（2026-09-18 新設・実機 12:28:13）：`'{"branch": "light", "text": ""}'`——黙る、の正当な返事なのに `調停の返事を読めなかったのでフルへ倒す`。主LLM が無駄に回る | **完了・実機確認済み**（2026-09-18 14:43：自発の調停 light 空 → `結末=沈黙`） | 小 |
| A-6 |  | **情-l** | **タイマー由来の沈黙が、在席表の失効で解ける**（2026-09-18 新設・実機 14:51）：「3 分測って」→ 掛かって沈黙依頼（`reason=timer:12`）→ 24 秒後に在席表が失効（`/speaker` から 60 秒・カメラは誰も見ていない）→ `is_silenced` の「頼んだ人が在席表に居る」が偽 → 「こんにちは」に返事した（「静かにしているね」と言いながら喋る）。09-16 の「退室で解ける」が、在席表が「誰か」だけの層になった今は失効と区別できない | **完了**（2026-09-18・`イベント駆動ループ` v0.82・`設計方針_タイマー` v0.8）：`is_silenced(req, now, nobody_since)`——タイマー由来は期限まで真、明示はセンサが誰も居ないを 60 秒見続けたときだけ解ける。在席表の名前照合は撤去（`_present_names` も）。実機未確認 | 先にやる |
| A-7 |  | **出-x** | **調停が「確かめて」の返りを読んで、聞き返さずに `set_timer` をもう一度**（2026-09-18 新設・実機 14:50〜51）：道具が「まだ掛けていない。本人に一度聞く：「…」」を返したのに、帰りの調停（Gemini）が `{"branch":"action","text":"3 分ですね…よろしいですか？","action":"set_timer","query":"パパの頼み"}`——確認文を text に書きつつ action も付けた。`_parse` は `query` から入力を組めず（「パパの頼み」に分数が無い）None → full → 主LLM が同じ見出しで呼び直し `すでに調べた語なので投げない` → 調停がまた `set_timer` → 上限 5 で主LLM が確認文を言った。**掛かりはした**が 4 反復の空回り。金木犀（09-18 12:35）と同じ形 | **A を実験で減らした・完了**（2026-09-18・`根拠台帳` §35・`イベント駆動ループ` v0.83）：実機と同じ W で 4 案 × 8 回。返りを W の最上部に `[いま道具から返った]` 1 行載せる（案あ）で 8/8 light の確認文（base は 8/8 full・先導文だけは 6/8 で action を誘発）。`Request.just_returned`。案あ は**汎化しなかった**（実機 15:28・15:42 で再発）。3 種の実機 W × 8 回（`根拠台帳` §35）：いまの形 22/24 が `action`、原因は想起の列（同じ道具の過去の帰りをいまの頼みとして読む）。**完了**（2026-09-18・`a55ba03`・`イベント駆動ループ` v0.84）：タイマー・アラームの帰りは想起なし（W＝返り＋直近）・返りを受ける先導文（実験 new2 の文・失敗の記録があっても道具で取り返さない）・返った道具を候補から外す → 24/24 light。調停は残す（あいまいな指示の聞き返しは LLM）。B は不要になった。**実機未確認** | 情-l の次 |
| A-8 |  | **出-y** | **主LLM が聞く前に `confirmed=true` を付けて掛けた**（2026-09-18 新設・実機 15:42・id=13）：「確かめて」の後の掛け直しを LLM の引数（`confirmed`）に任せる限り、聞かずに掛ける穴は閉じない | **完了**（2026-09-18・`a55ba03`・`設計方針_タイマー` v0.9 §10・`設計方針_アラーム` v0.2）：確認を機械の状態に——道具は預かり（`core/confirm_state.py`・`agent._pending_confirm`・寿命 `CONFIRM_TTL_SEC` 300 秒〔仮〕）を置いて確認文だけ返し、`confirmed` は道具の定義から撤去（grep 0）。次の求めで W の最上部に `[確認待ち]`、候補と道具に `confirm`（機械が預かった入力で掛ける）／`decline`（捨てる）。別の `set_timer` は上書き。**実機未確認**（会話 4：3 分測って → 確認 1 回 → いいよ → 掛かる） | 出-x の次 |
| A-9 |  | **出-x-ろ** | **`needs_tools` の守りが帰りの反復にも効く**（2026-09-18 新設・実機 18:31・「1１分測って」）：出-x の直し（想起なし・返りを受ける先導文）は効き、調停は light で確認文を選んだが、`arbiter.py:671` の守り（「測って」を含む発話は light で答えない・09-15 の「セットしましたと言うだけ」対策）が帰りの反復にもそのまま効いて full へ倒し、主LLM が `set_timer` → 確認待ち（上書き）→ 帰り light → 守りで full …と上限 5 まで空回り。実験（`根拠台帳` §35）は守りより手前の生の返事を数えていて見落とした | **完了**（2026-09-18・`9d2ff79`・`根拠台帳` §35）：`arbitrate(tool_return=True)` の反復では守りを掛けない。初回の反復の守りは残す。実験を守りの後で数え直し：直す前 0/4 light・直した後 24/24 light（3 種の実機 W）。**実機未確認**（会話 4a の 2 番） | 出-y の次 |
| A-10 |  | **出-z** | **上限到達の答えで、していないことを「した」と言う**（2026-09-18 新設・実機 18:31:37）：出-x-ろ の空回りが `考えた回数 5/5` に達し、主LLM に答えさせたら「はい、1分のタイマーかけたよ。18時32分に鳴るから待っててね」——道具は呼んでおらず（`道具=say`）、1 分も 18:32 もどこにも無い（頼みは 11 分）。整合チェックは `違反=なし`。本人は黙る時間だと思い、SEEKING の自発発話（18:32:25）を「黙るはずなのに話した」と受け取った。09-15 の light 版「セットしました」と同じ症状が full 側に残っている | **未着手**（設計で決める）。候補：上限到達の system 文に「道具の返りが無いものを『掛けた』と言わない・できなかったと言う」／整合チェックに「道具の返りに無い完了報告」の規則 | 出-x-ろ の次 |
| A-11 |  | **知-u** | **止めた返事が分からない・古いストップウォッチが日をまたいで残る**（2026-09-18 新設・実機 18:37）：`/timer stop` の返事「2 本止めた：「何のために時間を測るか不明」・「パパのタイマー」」。止めたのは 09-16 08:59 と 17:00 に主LLM が始めたストップウォッチ 2 本（id=2・10・DB 読み取りで確認）で、今日の分ではない。返事に種類（ストップウォッチ）と経過（2 日）が無く、名前は主LLM が `label` に書いた文のまま（機械が検めない）。18:31:25 の `start_stopwatch` は id=2 が動いていて断られていた | **未着手**（設計で決める）。候補：止めた返事に種類と経過を添える／`label` が文なら機械で「測る」に／ストップウォッチの上限時間〔仮〕で自動停止・枠に「N 日動いている」 | 出-z の次 |
| A-12 |  | **環-n** | **設定画面の保存が `FAMILIAR_ENV_FILE` の上書き file に設定一式（鍵を含む）を書く**（2026-09-18 新設・実機 18:27）：`SettingsDialog` の保存先は `resolve_env_path()`（`gui.py:253`・`d4aa868`）で、`FAMILIAR_ENV_FILE=.env.quiet` のときは `.env.quiet` を指す。`.env.quiet` は「上書きしたい数行だけ。鍵は素の `.env`」の雛形（`471a010`）で git 管理下。読む側（`bootstrap`）は雛形の形に直したが書く側は直しておらず、保存で 26 行（`API_KEY`・`ELEVENLABS_API_KEY`・`UTILITY_API_KEY`・`CAMERA_PASSWORD` と、画面の既定値 `BASE_URL=localhost:11434`・`TOOLS_MODE=prompt` など `.env` に無い 9 行）が書き出された。コミットはされず、雛形に戻した（鍵は `.env` に同じ値で既にあった）。知-o で「声」タブの保存を手順に入れて踏んだ | **未着手**（設計で決める）。候補：保存先を素の `.env`（`bootstrap._base_env_path()`）に固定し上書き file には書かない／`.env.*` を無視して雛形は `.env.quiet.example` に。直すまで quiet 構成では保存を押さず `.env` を手で直して `/reload` | 知-u の次 |
| A-13 |  | **情-m** | **タイマー中に本人の「一時停止」「止めて」が入口で落ちる**（2026-09-18 新設・コードで確認・会話 4b の予想）：沈黙の入口（`silence_hold.lifts`）は止める言葉を「頼んだ本人」だけ通し、本人かは話者の指定（60 秒・知-t）で見る。タイマー中は返事をしないので掛けて 60 秒後には本人が「分からない」になり、顔照合が無ければ `黙っているので聞くだけ` に落ちる。声の門（`TIMER_MIC_CLOSE`）は操作の言葉を誰でも通すのに入口だけ本人性を求めていた | **完了**（2026-09-18・`1d8b8ba`・`設計方針_タイマー` v0.10 §12）：タイマー由来の沈黙（`reason=timer:`）では操作の言葉（`is_control_word`・12 字以内）を誰の言葉でも通す。人の依頼の沈黙は本人だけのまま。案イ（タイマー中は本人の指定を切らさない）は知-t を崩すので不採用。**実機未確認**（会話 4b の 6 番：掛けて 70 秒後の「一時停止」が通る） | 出-x-ろ の次 |
| A-14 |  | **環-o** | **GUI の Enter で入力の末尾に「・」が付く**（2026-09-18 新設・実機 20:51〜20:55・3 回とも `/timer resume・` 14 字）：Enter を押す前は見えず、押した後の `text()` に入っている。GUI のコードに文字を足す経路は無い（`_on_send` は `text().strip()`）。打鍵は Mac の Chrome（リモートデスクトップ）→ 機体の X11 → **ibus-mozc** → Qt の順で、mozc のローマ字表は `/` を「・」にする。09-15 の `/speaker・`（`agent.py:127`）と同じ現象。命令として読めず会話入力へ落ち、タイマーの沈黙中は `黙っているので聞くだけ` に飲まれた。**GUI を再起動すると起きない**（20:5x 報告）——Qt と ibus のあいだに未確定文字「・」が描画されずに残り、Enter のたびに一緒に確定されていた、と読める（3 回とも同じ 1 文字）。原因の機構は未特定 | **命令側で吸収・完了**（2026-09-18）：`/speaker`・`/timer`・`/alarm`・`/mic`・`/reload` は末尾の「・」「。」「、」「．」と空白を無視する（`agent._command_text`）。回避：機体側の mozc を英数にしてから打つ。原因の追跡は次に起きたときの生の文字列（符号位置）から | 情-m の次 |
| A-15 |  | **情-n** | **道具の帰りの反復で調停が沈黙の依頼を読む**（2026-09-18 新設・実機 20:46:07）：`set_timer` の帰り（確認文「3分のタイマーですね。その間は黙って待機します」）で調停が `silence_minutes` を書き、`黙っているよう頼まれた：パパ（60 分）`。発話「パジュ、3分測って」は名前入りなので `names_me` を通った。タイマーが掛かる前に 60 分の沈黙が掛かり「いいよ」「こんにちは」が飲まれた | **完了**（2026-09-18・`18aafed`・`イベント駆動ループ` v0.86）：`arbitrate(tool_return=True)` では `silence_minutes`／`lift_silence` を機械で 0／false に。**実機未確認** | 環-o の次 |
| A-16 |  | **情-l-ろ** | **明示の沈黙が退室で解けても記録が残る**（2026-09-18 新設・実機 20:46:45〜47）：情-n の 60 分の沈黙が「誰も居ないを 60 秒」で解けて（打っている人が映っていない）まとめの求めが立ち `confirm` で掛かったが、記録が `until` まで残るので `hush_for_timer` が「人の依頼が生きている」と負け、タイマーの沈黙が掛からなかった（`タイマーが鳴るまで黙る` の行が無い）。人が映れば沈黙が復活する形でもある | **完了**（2026-09-18・`18aafed`）：`check_silence_lifted` が明示の依頼で `is_silenced` が偽なら `clear_silence()`（器が空でも）。タイマー由来は消さない。**実機未確認** | 情-n の次 |
| A-17 |  | **出-aa** | **「再開」を調停が light で受け流す**（2026-09-18 新設・実機 20:47:47）：一時停止中に「再開」→ `調停=light`「はい、分かりました。再開しますね。」——`resume_timer` は呼ばれず、タイマーは止まったまま（09-15 の「セットしました」と同じ型）。同時に「再開」を `lift_silence` とも読んだ | **完了**（2026-09-18・`18aafed`）：`[タイマー]` の枠があるとき（`timer_active`）、操作の言葉（`is_control_word`）の light は full へ倒す（主LLM が枠と道具で決める・機械で道具は選ばない）。帰りの反復には掛けない。**実機未確認** | 情-l-ろ の次 |
| C-1 | **C. 次の実機セッションで確かめる**（互いに独立・**起動 → 会話 → 無人**の順に上から消化する） | （起動） | 起動ログ：ウォームアップ（温め・TTS・Whisper・YOLO）が 1 回だけで、発話のたびに走らない（環-k）。09-16 の変更（環-m・情-h・知-m・タイマーの沈黙と声・`/reload`・入力ゲイン 2.5・ElevenLabs flash）が入った版（`24bb367` 以降）で立ち上がり、起動直後の発話が止まらない（環-m・12:40 の 88 秒止まりの再発なし） | **確認済み**（2026-09-18 12:26〜32・quiet・`v0.f5a99`）：066・067 が本番に当たった。温めは各 1 回（「準備完了 14s」）。「こんにちは」に 1.6 秒で返事 | A-9 の直し。ログの見どころ：`_take_trigger`・`Microphone capture … gain=2.5` |
| C-2 |  | （会話） | **知-m**：「窓のほう見て」→「右向いて」で首が右（pan 負）へ回り、向き終わってから撮った写真で答える（`押入で見えたもの`）。ログ：`調停=action … action=look`・`調べもの look … 右を見に行く`。帰りの調停が `look` を選び直さない | 未確認（①②は 09-16 12:4x に確認済み・③の再起動後の再確認） | C-6（旧）の直し |
| C-3 |  | （会話） | **知-n・沈黙・声**：「3 分測って」→ 掛けているあいだ黙る（`TIMER_SILENCE`）→ 鳴ったら `[届いた知らせ]` の求めで話し、`DIF 声の倍率 1.50` が DEBUG に出る・鳴った知らせでタイマーを掛け直さない。「6 時半に起こして」→ 確かめられる →「いい」→ 掛かる。「止めて」→ 止まって沈黙も解ける | 未確認 | C-7（旧）・`設計方針_タイマー` 追記 |
| C-4 |  | （会話） | **情-h**：名前を呼んで「黙って」→ 誰が話しかけても返事しない（`黙っているので聞くだけ`）→ 本人の「話していいよ」で 1 つの求めが立ち、W に `黙っていたあいだ（…）に届いたもの` が載って 1 回で答える。名前を呼ばない「待てぃ」では掛からない | 未確認 | A-10（旧）・`イベント駆動ループ` v0.73 |
| C-5 |  | （会話） | **出-q**：`/speaker パパ` の挨拶のあと `seeking`／`safety` の自発ターンが挨拶を繰り返さない・見回りの帰りに黙る | 未確認 | D-0f（旧）・`イベント駆動ループ` v0.66〜v0.67 |
| C-5b | （会話） | **情-i** | **「パジュ、静かにして」が名前の聞き違いで落ちる**（2026-09-17 新設・実機 15:31〜15:32）：STT が「パジュ」を 体重／パチュー／はじゅ と書き、名前の守り（`names_me`）が 3 回とも弾いた（1 回は喋っている最中で `tts_active` に捨てられた）。直し：守りをゆるい読み（カナ・長音・濁点を無視・1 文字違いまで）に、`ME.md` の名前に聞き違いの綴りを並べ、先頭 1 語を STT の `hotwords` に | **机上は完了・実機未確認**（2026-09-17・`イベント駆動ループ` v0.74・`音声入力からGUIへの経路` v0.6）。実機で見るもの：再起動後に「パジュ、静かにして」で `名前で呼ばれていないので受けない` が出ずに黙る・`書き起こした` の本文で「パジュ」がどう書かれるか（`hotwords` の効き）・喋っている最中に言ったときは `gated transcript (tts_active)` で捨てられる（**これは別の課題**・案エ） | — |
| C-5c | （会話→無人） | **知-p** | **`/speaker` の後、カメラに誰も映らなくても自発が出る**（2026-09-17 新設・実機 15:44）：在席ゲートは 3 つの証拠の OR で、`/speaker パパ` が在席表（PMM）に永久に残り（出る口が無い）、カメラが 2 分「誰も居ない」でも自発が出た。直し：**マイクで拾った声は在席の証拠にしない**（17:03 にカメラが誰も見ていないのに「こんにちは」の書き起こしへ返事した・テレビと物音を拾う）。居るかは**センサ（滞留窓 180 秒）＋自分の発話・`/speaker` から 60 秒**、在席表は「誰か」だけでセンサが「誰も居ない」を 60 秒見続けたら失効（話者の指定は残す）。**誰も見えないときの会話入力は入口で止め**（情-h の器・返事を作らない）、人が映った最初の求めに列挙 | **一部確認**（2026-09-18 12:37〜38・quiet）：自発 → `聞く相手が居ない ので独り言は言わずに残す`、映らない位置から「パジュ、聞こえる？」→ `誰も見えないので聞くだけ`・`seeking を +0.50（いま 0.57）`・返事なし・在席表の失効も出た。**残り**：映ったら列挙が載るか。**見つかった穴** → 情-k・知-r | — |
| C-5d | （会話） | **知-n-ろ** | **タイマーは声でなく音で鳴る**（2026-09-17 新設）：自作の `timer_alarm.wav` を `TIMER_RING_SEC`（30 秒〔仮〕）繰り返す。止めるのは「止めて」・`/timer stop`・時間切れ。マイクは閉じない | **机上は完了・実機未確認**（2026-09-17・`設計方針_タイマー` v0.2 §5a）。実機で見るもの：「1 分測って」→ 音が鳴る（`DIF タイマーの音を鳴らす`）→「止めて」で止まる（`DIF タイマーの音を止めた`）・鳴っている間の `書き起こした` が幻聴の門で落ちるか（拾って求めが立つなら `STT_NO_SPEECH_MAX` を見直す）・音量が `TIMER_VOICE_GAIN` で足りるか | — |
| C-5e | （会話→無人） | **情-j** | **声がしたが応じられないとき、見回す情動が上がる**（2026-09-17 新設・案ア）：返事が「聞く相手が居ない」で保留になるたび SEEKING に発火閾値の半分〔仮〕を足し、2 回目で発火して情動の求めが立つ。行き先は `[いま]` の「見ていない順」＋機械の既定（知-c の穴：`stalest_pose` に呼び手が無く、`look` の pose は LLM 任せだった） | **一部確認**（2026-09-18 12:38）：押し上げ +0.50 は効いた。2 回目の声で発火して `look` へ行くかは未確認 | E-1 と同じ場面 |
| C-5f |  | **知-o** | **指定された時間、マイクを閉じる（音を聞かない）**（2026-09-16 新設）：「10 分マイク閉じて」で、頼まれた直後に**一度確かめ**（「10 分マイクを閉じるね、いい？」）、OK が出たら常時集音（`RealtimeSttSession`）に音を流さない。時間が来たら自動で開く。閉じているあいだも**パジュは話す**（自発・タイマーは鳴る＝ア）。声で「開けて」は届かないので GUI の `/mic on`〔仮〕で手動で開ける口を持つ。閉じた・開いたは O に残し、GUI に 🔇 と残り時間を出す。「タイマーと同時にその時間は喋らない」は `TIMER_SILENCE`（2026-09-16・`設計方針_タイマー` 追記）で別に実装済み | **机上は完了・実機未確認**（2026-09-18・段 1〜5・`設計方針_タイマー` v0.7 §8〜§12）：設定 3 つ（画面・保存で効く・`TIMER_CONFIRM` 既定 true）・確かめる・同時 1 本・一時停止／再開（066）・聞かない（書き起こしの門で操作の言葉だけ通す・`/mic on`・🔇）。実機で見るもの：設定画面で「聞かない」を切って保存 → 次の `set_timer` で確認文が変わる／「3 分測って」→ 確認 →「いい」→ 掛かる・黙る・🔇 → 「こんにちは」は `聞いていない（タイマー「…」）` で捨てられ「一時停止」は通る → `止めておく`／「再開」→ 鳴る → 音 → 「止めて」 | — |
| C-5g | （会話） | **知-q** | **タイマーとアラームを別物に**（2026-09-18 新設）：`set_timer(at=…)` の引数の違いだけで同じ表・規則を通っていた。タイマーの規則（黙る・聞かない・1 本・一時停止）をアラームに当てると「7 時に起こして」で翌朝まで黙って聞かない | **机上は完了・実機未確認**（2026-09-18・`設計方針_アラーム` v0.1・067）。表 `alarms`・`set_alarm`／`cancel_alarm`（調停と主LLM の両方）・`alarm_watch`・`/alarm stop`・`[アラーム]` の枠・確認は静穏時間だけ・同時 5 本。実機で見るもの：「21 時に薬って言って」→ `掛けた：id=1 「薬」 21:00`・`[アラーム]` に「今日 21:00」・黙らない／「明日 6 時半に起こして」→ 静穏時間の確認 →「いい」→ 21:00 と 06:30 の 2 本／`/alarm stop 1` | — |
| C-6 |  | 記-a-へ・情-f | **翌朝のシステム文**（`--debug` の `調停へ渡す W` 前）：`[守っている決まり]` の次に `[いまの自分]`（望み 8・気がかり 8・価値 10 行）、`[内部状態]` が言葉（気分 4 軸 × 5 段・欲求は発火した軸を明示、ほかは p70 超だけ弱く）で、生の小数が無い。調停の system にも同じ `[いまの自分]` | **未確認** | 09-14 の机上の変更。会話中に `measure.log` へ `気分 …`／`欲求 …` の行が増える |
| C-7 |  | 出-p | **「明日の予定わかる？」**：`調べもの family_schedule … 家族の予定を見る（2 日ぶん）（days=2）` が 1 行で済み、明日の行を含めて答える。調停が数字を書かなければ `days=1` の後に主LLM の `days=2` が**別の求めとして通る**（「すでに調べた語」が出ない） | **未確認**（09-14 19:49 に 4 回空回りした形の再現） | 出-o と同じ確かめ方：「家の決まり」も 1 回で答える |
| C-8 |  | 出-o | **道具の失敗**：カレンダー MCP を止めた状態（`~/.familiar-ai.json` から外す・または ICS の URL を壊す）で「明日の予定は？」→「…は道具が使えず失敗した」が W に載り、同じ道具を呼び直さず、失敗を言って閉じる（空回り無し）。検索は Brave を止めても Tavily で続く | **未確認** | 生のエラー文は WARNING のログにだけ |
| C-9 |  | 記-i・環-k | **計測ログと起動**：会話の続きで `measure.log` に `続き先 結末=続き／途切れ／未判定` が 1 発話 1 行。起動ログでウォームアップ（温め・TTS・Whisper・YOLO のダミー推論）が**1 回だけ**で、発話のたびに走らない | **確認済み**（2026-09-18 12:32）：`続き先 結末=未判定`・`気分`・`欲求` が 1 発話ごとに | `rest_logs/measure.log` は 09-14 の全体テストで 2 回改名された（19:53・20:33）。次の行から新しい file ができる |
| C-10 |  | — | シナリオ H：DEBUG で `OIF write/recall/link` が出て、記憶へ直のログが無い（環-e-い） | **確認済み**（2026-09-18・`OIF write/health` が DEBUG に） |  |
| C-11 |  | — | 2026-09-12 の変更：写真つき主LLM（v0.43）・調停 see（v0.44）・写真つき調停（v0.46）・返事の予算と effort（出-k-ろ・Sonnet 5）・生成時間のログ・声（ElevenLabs） | **一部確認**（2026-09-18 12:34）：「何が見える？」→ 調停 `see` → 写真つき 67 字（effort=low）。「よく考えて」は **出-v** で判定不能 | 「何が見えますか？」で `調停=light`／`主LLM … effort=low` 40 字前後／「よく考えて」で high／調べた後 160〜240 字／`DIF 声` が字数に見合う。済：see 1 回・返事に写真の中身・GUI の吹き出しログ |
| C-12 |  | **記-j** | **REST 内省の 1 パスを実機で 1 回確かめる**（2026-09-14 新設・**4 層**に広げた）：誰も居ない晩に REST が発火し、①層 1 が前回以降の出来事を畳む（`measure.log` の `層1 …`・`記憶`／`人物` が書かれ元が `畳み込み` で隠れる）、②層 2 が最大 3 行を出典つきで変える（`層2 …`・`agent_state.self_image`・翌日の `[いまの自分]`）、③層 3 が計測ログを集計して値を 1 刻み動かし `measure.log` を改名する（`設定値 名前=… 前=… 後=…`・`config_overrides`・`measure.log.<時刻>`）、④層 4 が要約を作り直す（`層4 一覧=… 要約=…`・初回は要約が無いので必ず作る・`agent_state.capability_summary`・翌日の `[あなたは誰か]`）。見送りなら理由（字数・出典・家族に無い名前・形の検査）を `内省` の記録で読む | **一部確認**（2026-09-16 04:28・v0.0f841）：層 1 計測 I=69,994（目標 131,072・減り・固めなし）、同一 25 件→1、①材料 2,956・10 回・書いた 19・畳んだ 247・見送り 2・持ち越し 65・141 秒。層 2・4 は思考で予算切れ（環-l 追記で直した）。設定値は標本不足で動かず（記-k どおり） | 起動条件：`PresenceSensor` あり・REST 発火・在席ゼロ。誰も居ない時間に起動したままにする。**先に再起動**（層 2・4 の予算切れを直した `4aa42c0` 以降のコード） |
| D-1 | **D. 普段使いで貯まる・測る**（依存なし） | **出-n（5）** | **整合チェックの見直しの効果の実測**（出-n の 5）：規則別の違反数・チェック回数・差し戻しの秒数を、直し（2026-09-13・1〜4）の前後で比べ `根拠台帳` に置く | 未着手（普段使いで貯まる） | 直す前の実測（2 日間・チェック 60 回・違反 31 回・`declare-memory-use` 43 文）と 1〜4 の中身は `イベント駆動ループ` v0.58 と、束 0 の 出-n の行 |
| D-2 |  | — | シナリオ G：キャッシュ（5 分以内 3 往復で `cache_read`・6 分後 `cache_write`・終了時に例外なし）（出-i） | 未確認 | 主LLM を Sonnet 5 に替えたので前提が変わる |
| D-3 |  | — | 想起の e 軸（`実機確認シナリオ_想起` スライス 3） | 未 | 依存なし（記-g は済み）。会話が貯まってから `実機確認シナリオ_想起` スライス 3 の手順で |
| D-4 |  | **知-l** | **ICS の更新遅れの実測**（2026-09-13 新設）：カレンダーを変えてから `get_family_schedule` に映るまでの遅れを測り `根拠台帳` に置く | 未着手 | 依存なし（知-j は済み）。カレンダーを変えた時刻と `get_family_schedule` に映った時刻を記録する |
| E-1 | **E. 見回りの鎖**（依存あり・順に） | **知-c** | 昼夜の見回り——**首が実際に回る**（シナリオ A） | 実装済み・**未確認** | `look` は 09-16 に名前（正面）と相対（右）で回った（旧 C-6 ①②）。残るのは**見回り（自発）が定点を巡ること**——無人で待つか、昼夜の発火を起こす |
| E-2 |  | **知-i** | **カメラの向き（定点）を名前で管理し、向きごとの前回の見えと比べて何が変わったかを会話にする**（2026-09-13 新設） | 未着手 | **E-1（知-c）の実機確認の後**。定点ごとの前回の見えと比べるには、見回りが定点を巡っていることが要る。 依存なし。**ある部品**：定点レジストリ（`poses.py`・`CAMERA_POSES` かカメラのプリセットから・いまは env が空でプリセット頼み）、`look`（定点へ向く）、定点別の見えの「普通」（`pose_norms`・DINOv2・3 定点分・在席の驚きに使う）、見た印の定点名（`出入り口を見た。`）、VLM の実体差分（`scene.SceneTracker._diff_entities`——**どこからも呼ばれていない**）。**無いもの**：向きごとの前回の写真を残す場所、「前回とここが違う」を言葉にして W へ載せる経路、人が「窓側を見て、前と何が違う？」と頼める形。知-c（見回り）は実装済みで実機未確認 |
| F-1 | **F. 人の登録の鎖**（依存あり・順に・**E-1 の後**・人が揃うのを待つ） | 知-d | 顔と声の登録 | 未着手 | E-1（知-c）。登録に応じられる人が揃うこと（外部条件） |
| F-2 |  | 環-b | 保留していた発話を配る起点（配ったものが人に届く） | 起点は動く | F-1（知-d） |
| F-3 |  | 知-e | D-rel（関係の加工）／p 軸の実挙動・大人と子どもの口調・一定時間で忘れる在席 | 未着手 | F-1（知-d） |
| F-4 |  | 環-d | 廃止ストアの移管と撤去（`self_narrative` は移管しない） | **一部完了**（2026-09-14・061・`設計図` v0.99）：読み手の無い 5 表（`self_narrative_log`・`semantic_facts`・`behavior_policies`・`memory_links`・`memory_revisions`）を DROP し、書き手（`self_narrative.py`・`concern_engine.py`・`intervention_policy.py`・`reflect.py`・`legacy/`）を撤去。`desires.py`（旧 15 欲求・063）と `relationship_state`（移管でなく撤去・064・`設計図` v0.101）も撤去（2026-09-15）。**残り**：`pending_store`（環-b 待ち）だけ | F-2（環-b）。`pending_store` は不在の保留を持ち、環-b が配れるようになるまで残す |
| 0-0 | **0. 完了**（2026-09-17） | **出-u** | **`pyproject.toml` の `torch` が CPU の索引を指している**（2026-09-17 新設）：`[tool.uv.sources]` は `torch = { index = "pytorch-cpu" }` だが、実物は `2.10.0+cu128`・`cuda True`（RTX 3060）。知覚のスライス S4 の前に「実物へ合わせる」と書いたまま残った。別の環境で `uv sync` すると CPU 版が入り、YOLO・DINOv2・faster-whisper が GPU で回らない | **完了**（2026-09-17）。調べると **死んだ宣言**だった：torch は直接の依存でなく `tool.uv.sources` は直接の依存にしか効かない。`uv.lock` の torch は PyPI の 2.10.0（Linux は CUDA 12.8 版・`+cu128`）で実物と一致。宣言と索引を撤去し、理由を `pyproject.toml` に書いた。`uv lock --check` で lock は不変 | — |
| 0-1 | **0. 完了**（2026-09-13〜16 の分・消さない。旧 A・B・C・D の完了行） | **出-o** | **失敗した調べものの再試行**（2026-09-13 新設）：`isError`・実行できなかった結果も「調べた」と数え、主LLM が正しい引数で呼び直しても「すでに調べた語」で止められる（J で 4 回空回り・25 秒） | **完了**（2026-09-13・`イベント駆動ループ` v0.59）。失敗は印で運び、文は「道具が使えず失敗した」だけ、失敗した道具は求めのあいだ候補から外す、検索は片方が失敗したらもう片方で。再試行はしない（同日決定） | 依存なし。失敗した器は同語の再投げを許す（結果を失敗として区別する） |
| 0-2 |  | **出-p** | **期間を持つ道具の見出しと調停の日数**（2026-09-14 新設）：「明日の予定」に調停の `family_schedule` は入力 `{}`（今日だけ）で引き、主LLM の `days=2` の呼び直し 4 回が固定の見出し「家族の予定を見る」で二度投げ禁止に止まり、上限まで空回り（19:49 実機） | **完了**（2026-09-14・`イベント駆動ループ` v0.62）。見出しは `家族の予定を見る（N 日ぶん）`、調停の候補文に範囲 1〜14 を書き `query` の数字を `days` に、`調べもの` のログ行に入力を載せる | 出-o と同型（止めるのは同じ入力の繰り返しだけ） |
| 0-3 |  | **記-i** | **続き先の判定の結果を記録する**（2026-09-13 新設）：継起の辺の有無しか残らず「判定して途切れ」と「未判定（入室・情動が起点）」「判定が落ちた」を区別できない。W には効かないが判定の健全性を数えるために残す（`MI間の関係` v0.16 段 4 改訂・未決） | **完了**（2026-09-14・`MI間の関係` v0.17）。結末 5 通りを計測ログ `rest_logs/measure.log` へ 1 行ずつ（`core/measure.py`）。累計は持たない | 記-h |
| 0-4 |  | **環-k** | **起動時のウォームアップが 2 回走る**（2026-09-13 新設）：`_start_background_services` が `_ensure_event_loop` の末尾で呼ばれ、起動時 2 回に加えて**人の発話のたび**にも走っていた（温め・TTS 起こし・Whisper は再入を止めていなかった。YOLO のダミー推論が発話ごとに 1 回） | **完了**（2026-09-14）。一度でよいもの（温め・TTS・Whisper）を `_services_primed` で 1 回に。MCP とワーカーは自分の状態で守られているので従来どおり | 依存なし |
| 0-5 |  | **情-f** | **内部状態（気分・欲求の定性ラベル）が効いていない**（2026-09-14 新設）：気分は `LABEL_PAD` 12 点への最近傍だが、記-g で測り直した PAD のスケールでは `neutral` から最寄り（`curious`）まで距離 0.55 あり、09-12〜14 の 15 回の更新が**すべて `neutral`**（情報量 0）。表は不快側が `sad` だけ・英語・025 の凍結写しと対で記-g 後に合わせていない。欲求の低／中／高の閾値（0.5／0.75）は `課題5` に無い仮値で、情-d（倍々の遅れ）と無関係。何を渡すかから決め直す（表を記-g のスケールに合わせる／閾値を `課題5` に載せる／渡す形） | **完了**（2026-09-14・`課題5` C2 節・`core/inner_state.py`）。気分 4 軸 × 5 段（実測分位）、欲求は発火した軸を明示しほかは p70 超だけ弱く、境目は `InnerStateConfig`（層 3・DB > 既定・`.env` を読まない最初の適用）、数値は計測ログへ。旧 `drive_snapshot`／`qualitative_level`／`drive_level_*` は撤去 | 依存なし。層 2（自己像・日単位）とは別の、分単位の内部状態 |
| 0-6 |  | **情-g** | **自発ターンが「人と話した」扱いになり、ひとりの回数が毎回 0 に戻る**（2026-09-15 新設・実機 11:27〜13:08）：`seeking` が 5 分おきに 18 回・毎回「ひとり 1 回目」。`_run_post_response_pipeline` が `if user_input:` で `_last_human_at` と `record_conversation()` を毎ターン書き、ループは自発ターンにも cue を `user_input` として渡す。以前の `not is_desire_turn` はループが常に False を渡していたので効いていなかった（09-12 の「1 時間に 16 回」が直っていなかった理由） | **完了**（2026-09-15・`イベント駆動ループ` v0.64・実機未確認） | pipeline は `human=trigger_kind=="発話"` のときだけ `record_conversation()`。`_last_human_at` は入口に一本化 |
| 0-7 |  | **環-l** | **`AnthropicBackend.complete()` が思考ブロックで空を返す**（2026-09-15 新設）：Sonnet 5 の返りが `[ThinkingBlock, TextBlock]` のとき `content[0]` だけを見て空文字（`anthropic.py:337`・`:381`）。REST 層 1 ①が 69 回すべて「返りを読めなかった」（806 秒・書いた 0）、層 4 も「capabilities が空」。本番の 06-13 の 60 件で再現 | **完了**（2026-09-15・`イベント駆動ループ` v0.64）。**追記 09-16**：思考ブロックだけで `stop=max_tokens`（本文ゼロ）が層 2・3・4 で再発——`complete()` は思考の指定が無く Sonnet 5 の適応的思考が予算を使い切っていた。`complete()`／`complete_with_image()` は `thinking={"type": "disabled"}` を明示 | `_text_of(resp)` が `TextBlock` を全部つなぐ（`complete_with_image` も）。`complete()` を使う全経路（層 1〜4・②）に効く |
| 0-8 |  | **記-k** | **層 3 が標本 4 件で境目 9 件を動かした**（2026-09-15 新設）：改名された計測ログ 121 行のうち `気分`／`欲求` は 4 行。規則ごとに最小の標本数〔仮〕を置き、足りなければ動かさない | **完了**（2026-09-15・`設定値を調整する` v0.3）。`MIN_SAMPLES`＝内部状態 50・窓 20・時間切れ 20・関連 20〔仮〕 | 情-g・環-l の後 |
| 0-9 |  | **記-l** | **層 1 ①の初回が全履歴（2,677 件・69 回・14 分）を対象にした**（2026-09-15 新設）：前回の `内省` が無いと全期間が材料になる。1 晩の回数の上限〔仮・10 回〕を置き、残りは翌晩へ | **完了**（2026-09-15・`出来事を畳む` v0.6）。材料は「まだ畳まれていない」（`OIF.fold_materials`）、1 晩 10 回〔仮〕・超過は持ち越し | 環-l の後（書けるようになってから） |
| 0-10 |  | **環-m** | **列から取り出す口は 1 つ・会話入力は保留箱より先**（2026-09-16 新設・実機 12:40）：「おやすみなさい」が 88 秒返らず STT の入力が 6 件溜まった。(A) `_take_trigger` が保留箱を列より先に返し、列の会話入力より打ち切りで回した入室が先に走った (B-1) `_intake` が列を種類を見ずに空にし（3 本キュー時代の drain が `a57cf94` で名前だけ残った）会話入力を完了として取った (B-2) 意図の無い物を無言で捨て、Future が解決されなかった | **完了**（2026-09-16・`イベント駆動ループ` v0.69）：会話入力は調査中でも保留箱より先、取込は完了箱だけ、意図の無い完了は warning・会話入力が紛れたら error＋Future に例外。**実機で見るもの**：起動直後に話しかけたとき返事が詰まらない（`GUI turn end` が数秒で出る） | — |
| 0-11 |  | **情-h** | **沈黙は入口で止め、明けたら 1 つの求めにまとめる**（2026-09-16 新設）：出口で止めていたため、発話ごとに求めが立って LLM が回ってから止まる・他人への返事を通す例外（案イ）・溜めた返事の配り時が限られる、の 3 つの歪み | **完了**（2026-09-16・`イベント駆動ループ` v0.73・`core/silence_hold.py`）：黙っているあいだ会話入力・機器・情動は O に残すだけで求めを立てない（誰の声でも・案イ撤回）。通すのはタイマーが鳴る・本人の「話していい」・本人の止める頼み。明けた求めの W 作業状態の枠に列挙（上限 `SILENT_HEARD_MAX_CHARS`〔仮 4,000〕・想起から除外）、主LLM が 1 回で答える。**実機で見るもの**：黙っているあいだ `黙っているので聞くだけ` が出て返事が無い・明けた求めの W に `黙っていたあいだ（…）に届いたもの` が載り 1 回で答える | — |
| 0-12 |  | 記-a-ろ | 層 1 **出来事を畳む**：記-a-ろ-い（前提・完了）→ **ろ-は ①日次の畳み込み＋専用ループの最小形** → ろ-ろ（$\Delta$ と $I$ の計測）→ ろ-に ②核の固め | **設計済み**（`出来事を畳む` v0.3）。ろ-い・**ろ-は 完了**（2026-09-14・実機未確認）。**ろ-ろ・ろ-に 完了**（2026-09-15）。層 1 の 4 段が揃った（実機未確認） | 記-a-い は完了。4 層の定義は `用語一覧` v0.70 |
| 0-13 |  | 記-a-へ | 層 2 **自己像を抽象化する**（望み・気がかり・価値・字数上限） | **完了**（2026-09-14・`自己像` v0.3・実機未確認） | 記-a-ろ |
| 0-14 |  | 記-a-に | 層 3 **設定値を調整する**（計測ログ・登録制・DB > 既定・n×2 と調停の時間切れ・層 1 の $I^\*$ 等を登録） | **完了**（2026-09-14・`設定値を調整する` v0.4・実機未確認） | 記-a-へ |
| 0-15 |  | 記-a-と | 層 4 **能力を再定義する**（`capabilities.yaml` と要約） | **完了**（2026-09-14・実機未確認）。一覧は DB（`agent_state.capabilities`・`capabilities.yaml` は既定）、7 日〔仮〕に 1 度書き直し、要約は一覧か自己像が変わった晩だけ | 記-a-に |
| 0-16 |  | ~~記-b~~ | ~~D-mi（MI 畳み込み・蒸留）~~ | **消す**（2026-09-15）。07-29 の「蒸留の設計」の名残で、中身は層 1 の②（核の固め・記-a-ろ-に）に吸収された | — |
| 0-17 |  | **知-f** | 話者ゲート（名前に人が入っている道具を、その人のターン以外で出さない） | **完了**（2026-09-14・`家の記録との接続` v0.3・`イベント駆動ループ` v0.63）。FAMILY.md の `英字`・`core/tool_gate.py`・`_gated()` の 1 出口（主LLM の道具と調停の候補）。実機で見るのは知-g-い で `ask_vault_yusuke` を載せたとき | `ask_vault_yusuke` を繋ぐ前に要る |
| 0-18 |  | **知-g-い** | 聞かれたとき Vault の中身を引きに行く（`ask_vault_yusuke` を主LLM と調停の候補へ・数十秒なので投げっぱなし＋つなぎ） | **完了**（2026-09-15・`家の記録との接続` v0.4 §9・実機未確認）。動作 `vault`・話者の英字で道具を解く・120 秒〔仮〕・素性の印を落とす | 知-f |
| 0-19 |  | **知-g-ろ** | Obsidian を定期的に読み、新しいメモ（指示・予定）を拾って自分から動く | **完了**（2026-09-15・`家の記録との接続` v0.4 §10・実機未確認）。相手側に `get_notes_for_paju` とページ `10_Wiki/参照/パジュへ.md`（本人の許可で直接足した・向こうの HANDOVER §9）、こちらは T が 1 時間に 1 回〔仮〕読み `agent_state.paju_notes` と比べて `機器` の求めを積む | memo_mcp は別リポジトリ（ObsidianMemo の 2 コミット・未 push） |
| 0-20 |  | **知-g-は** | 情動で起きた求めで、調停が家の記録（`obsidian-memo`）を見に行く（調停の候補に `house_rules`・see と同じ形） | **完了**（2026-09-15・実機未確認）。知-j（動作の表）と出-p（候補文）で通っていた——ループは起点に関わらず `extra_actions` を渡し、`_BRANCHES_SELF` の候補にも載る。証拠の試験 `test_an_affect_origin_can_choose_a_synchronous_mcp_tool` | — |
| 0-21 |  | **知-n** | **タイマー**（アラーム・タイマー・ストップウォッチ・2026-09-15 新設）：途中で停められることを軸に。静穏時間・沈黙の依頼に掛かるものは登録せず一度確かめ、確かめたものだけ鳴るときに配信ゲートを通り抜ける | **完了**（2026-09-15・`設計方針_タイマー` v0.1・065・実機未確認）。表 `timers`・道具 3 本・`/timer stop`・T の tick・`[タイマー]` の枠 | 実機：「3 分測って」→ 3 分後に「タイマー：…」の求め→発話。「6 時半に起こして」→ 確かめられる→「いい」→ 掛かる。「止めて」→ 止まる |
| 0-22 |  | **知-m** | **相対の向き（右／左／上／下）で定点を選ぶ**（2026-09-14 新設）：「右向いて」に応えたい。相対の首振りは 2026-07-28（`34805aa`・S5a）に撤去し、`look` は定点の名前でしか呼べない | **①完了・③実装済み**（2026-09-16）。①：Tapo のプリセット 5 件（出入口 −0.55・押入 −0.25・正面 −0.08・窓 +0.19・テレビ +0.49）。③：`look` に `direction`（`poses.pose_toward`——その方向で最も近い定点へ絶対移動・無ければ「知っている場所」を挙げる）。**右＝pan 負**（実機 09-16 12:41：仮置きの正で「右向いて」が左を向いた → `RIGHT_IS_POSITIVE_PAN=False`）。②（名前で回る）は 12:4x に正面へも回って確認。**実機 15:11**：首は 0.5 秒で回るが帰りの調停が `look` を選び直して 18 秒 → `look` の帰りを `see` と同じ扱いに（向いた先をその場で撮る・`イベント駆動ループ` v0.71）。**実機 09-16 11:34**：「右見れる?」「もっと右を見て」に調停が `see` しか選べず首が動かなかった（`look` は主LLM の道具にしか無かった）→ 調停の候補に `look`（`イベント駆動ループ` v0.68）。あわせて「待てぃ」が沈黙依頼と読まれ 60 分止まった件は、本人が「もう話していいよ」と言ったら解く形にした（話しかけただけでは解かない） | 実機：再起動→「窓のほう見て」→「右向いて」（正面から右なら窓）。ログの見どころ：`調停=action … action=look`・`調べもの look … 右を見に行く` |
| 0-23 |  | **出-q** | **自発ターンが、済んだ入室にもう一度「おかえりなさい」と言う**（2026-09-15 新設・実機 15:45〜16:11）：`/speaker パパ`（15:46・PMM「Arrived」→ 機器の求め）で挨拶は済んだのに、`seeking`／`safety` の自発ターン（15:56・16:03）の light 分岐が再び「パパ、おかえりなさい」。`see` は人 0 件・机と椅子。会話の最後はパパの「いや、もういいや」（15:48）で、その 5 分後・12 分後に話しかけている。【推測】W か `[直近のやりとり]` に入室の記録が上位で残る／自発の light 分岐に「済んだ出来事に反応しない・話を切り上げられたら黙る」の材料が無い | **机上は完了・実機未確認**（2026-09-16）。09-16 09:16 の `--debug` の W で原因を確定：自発の求めに「[入室] 誰か が来た」と自分の挨拶が『直近のやりとり』として載り、`_LEAD_SELF` に済んだ出来事へ反応しないという材料が無かった。`_LEAD_SELF` に「直近のやりとりは済んだこと。入室・挨拶・終わった話に改めて反応しない。切り上げられていたら黙る」を足した（`イベント駆動ループ` v0.67）。あわせて自分から見に行った帰りの但し書き（v0.66・「静かにしていますね」）と light の「黙るのが基本」。**実機で見るもの**：`/speaker` の挨拶のあと `seeking`／`safety` の自発ターンが挨拶を繰り返さない・見回りの帰りに黙る | 実機。情-g（発火の間隔）とは別の問題——間隔は伸びている |
| 0-24 |  | **出-r** | **主LLM が答えを関数呼び出し風の文で返し、沈黙になる**（2026-09-15 新設・実機 20:12）：パジュへのメモの初回で、Sonnet 5 が `say(text="パパ、今日は体調崩して…", memory_verdicts=[…])` を**本文として**返し、ループは「道具なし・542 字」→ 結末＝沈黙・O に「考えたが言わなかった：say(text=…」。09-13 の拾い直しは `<invoke>` の形だけだった | **完了**（2026-09-15・`core/tool_text._call_style`）。先頭が `名前(` で末尾が `)` の本文を `key=値`（JSON か単引用符）で読み `ToolCall` に直す。実機未確認 | 知-g-ろ の確認はこの直しの後（メモの差分は 20:11 に積まれていた） |
| 0-25 |  | **出-s** | **人が話しかける前の自発の発話が画面に出ない**（2026-09-15 新設・実機 21:50）：メモの求めで主LLM が 66 字を話した（`DIF speak`）のに吹き出しに無い。ループの出口（`_on_text`／`_on_action`）は人の発話（`push_utterance`）で初めて結ばれていた | **完了**（2026-09-15）。GUI が `start_autonomy()` の直後に `agent.set_output(None, on_action=…)` で出口を結ぶ。出口が無いまま話したら WARNING「表示先が無いまま話した」 | 知-g-ろ はここまで通した（メモの差分→求め→発話→画面） |
| 0-26 |  | **出-t** | **調停の light が「タイマーをセットしました」と言うだけで道具を呼ばない**（2026-09-15 新設・実機 22:47）：「３分のタイマーをかけて」に light が答え、`timers` は空のまま。light は道具を使えない | **完了**（2026-09-15）。道具が要る頼み（`arbiter.needs_tools`・タイマー・アラーム・測る・止める・覚えて・予定）は light でも full へ倒す（沈黙の依頼は保つ）。候補文にも明記 | 知-n の実機確認はこの直しの後 |
| 0-27 | **0. 完了**（2026-09-12〜13 の分・消さない） | **出-m** | **検索と取得の MCP を繋ぎ直す**（2026-09-13 新設） | **完了**（2026-09-13） | `search_deferred` は MCP の `brave_web_search` を呼ぶが、`~/.familiar-ai.json` に `obsidian-memo` しか無い（開発機の故障で `brave-search`／`fetch` の設定と鍵が失われた）。調べものが 0.00 秒で「道具が無い」と返り、主LLM が「検索の道具が無い」と答える（実機 2026-09-13 00:22）。直し方は `.env.example:217-238`。2026-09-13 に `~/.familiar-ai.json` へ `brave-search`・`tavily`（`tavily-mcp@0.2.22`）・`fetch`（`uvx mcp-server-fetch`）を追記し、3 つとも起動して `brave_web_search`／`tavily_search`／`fetch` を出すことを確かめた（仮の鍵）。鍵は同日中に人が置き換えた。**同日 16:27・17:19・17:36 の `search_deferred` は `brave_web_search` で走り、結果（tenki.jp の守谷の予報など）が返った**（実機ログ・失敗 0 件）。調停が投げる `search_deferred` は `source` を渡さず既定の brave へ行くので、Brave が使えないあいだ調停経由の検索は失敗する（主LLM が `source='tavily'` を指定したときだけ通る）。既定を tavily に変える案は出したが、いまは変えないと決めた（同日） |
| 0-28 |  | **出-n** | **整合チェックの見直し**（2026-09-13 新設）— 1〜4 | **1〜4 完了**（2026-09-13・`イベント駆動ループ` v0.58）。**5（効果の実測・前後比較）は後で** | 依存なし。【事実】整合チェック（`evaluator.check_response_coherence`・軽量LLM）が受け取るのは **返事の文（300 字まで）・機械の事実（見たか／画像／記憶の件数と日付／申告した記憶の中身＝同日追加）・直近のやりとり**で、システム文に規則 `(rules …)` の全文が入る。返事は OK か違反の一文。**2 日間の実測：違反 31 件のうち `declare-memory-use`／`memory_verdicts` 絡みが 21 件**——申告（`say` の引数）はチェッカーに渡っておらず、文だけを見て「申告が足りない」と言っている（判定できないはずのものを判定している）。ほか `no-past-comparison-without-memory` 2 件（比較していない部屋の描写に）、`no-fake-perception` 3 件（写真を見て語った返事に）、`no-invented-knowledge` 2 件（記憶で答えた天気に）。違反のたびに主LLM を 5〜7 秒かけて呼び直し、言い直しは「確証はない」と後退しがち。**直し**：(1) 文から判定できない規則（`declare-memory-use`・`voice-only-from-say`・`no-tts-tags`）はチェッカーの規則から外し、機械で見る（申告の件数と W の件数の突き合わせは既に `apply_memory_verdicts` がログに出す）。(2) 材料が要る規則には材料を渡す（申告した記憶の中身・写真の有無は入れた）。(3) 違反の型ごとに件数を取り、正しかった違反が無い規則はチェックから外す。(4) 言い直しの指示を「違反の一文」でなく「どの事実と食い違うか」に変える **2026-09-13 Q の実測**：7 回中 3 違反（`declare-memory-use` 空振り 1・`workspace-is-notes-not-script`「調べた結果が届いた」の言い回し 1・`no-invented-knowledge` 気温 1）。差し戻し後の 2 度目は検査されず、「明日の東京は晴れ 30℃/22℃」（直前 16:28 の守谷・雨のち曇・25℃ と食い違う）が出た。本番 DB には同日 09:46 東京・11:03 大阪・16:27 守谷の天気の検索結果が並んでおり、都市の混在も材料 |
| 0-29 |  | **記-g** | **PAD の測り直し**（057 の後始末・2026-09-13 新設） | **完了**（2026-09-13） | `--apply` で 6,313 行を全部測れた（外した 0・9 分 32 秒・`gemini-3.1-flash-lite`）。本番の未測定（A ≥ 0.25）は 0 件、測定済み 7,088／7,648。分布は neutral 80%・curious 12%・relieved 2%…（P 中央値 0.25。大半がループ自身の記録のため中立が多い）。想起の e 軸が本番で意味を持つ状態になった。実機確認の段取り 4 |
| 0-30 |  | — | シナリオ D：`light` で閉じた反復の後にも申告が出る・返事は待たない | 未確認 | **確認済み**（2026-09-13 16:32・light の 1.2 秒後に `記憶の判定`・返事は 0.01 秒） |
| 0-31 |  | — | シナリオ E：W に `ゆうすけが言った`／`わたしが答えた` が分かれて出る（記-f） | 未確認 | **確認済み**（2026-09-13・O は `自分が答えた：`／人の言葉で分かれ、W の直近の枠は `相手：`／`わたし：`／`きっかけ：`） |
| 0-32 |  | — | シナリオ F：調べもの中に入室 → 待たされて取りこぼさない（環-f-い-1） | 未確認 | **確認済み**（2026-09-13 17:37・入室は答えまで待たされ、消えなかった） |
| 0-33 |  | **記-h** | **直近のやりとりを W の枠へ一本化**（2026-09-13 新設）：時系列の最新 $n$ 往復（無条件）＋継起の鎖を `compose()` が組み、調停と主LLM は同じ作り方の W を見る。$n$ は Config に 2 つ（軽量 3・主 6）、REST 内省で統計的に見直す（記-a-に の最初の対象）。段 4 の「続きでなければ載せない」は撤回 | **完了・実機未確認**（2026-09-13・`MI間の関係` v0.16・`イベント駆動ループ` v0.57・`課題5` v0.41）。REST 内省での見直しは 記-a-に 側 | **完了・実機確認済み**（2026-09-13 21:21・`MI間の関係` v0.16・`イベント駆動ループ` v0.57）。REST 内省での見直しは 記-a-に 側 |
| 0-34 |  | **知-h** | **「誰かがいる」と「知っている人がいる」を分ける**（2026-09-13 新設） | **完了**（2026-09-13・`知覚在席` v0.18 §3-2b） | 依存なし。設計は二層（在/不在＝T(G)・YOLO／誰か＝I・InsightFace・自己申告）だが、**配信ゲート**（`agent._social_presence_permission`・「聞く相手が居ない」）は PMM の人物 id（顔の照合＝知っている人）と直近 5 分の発話しか見ず、顔が未登録だと目の前に人が居ても「誰も居ない」になる（docstring 自身が暫定と明記・2026-07-28 の申し送り）。情-c・環-i はこの判定の上に乗る。**在/不在は `PresenceSensor.room_occupied()`（YOLO）で判定し、顔の照合・自己申告・直近の発話は補助**にする。`_present_ctx`（主LLM に渡す在席）にも YOLO の「誰か」を載せる。分かれているもの（入退室・REST の内省・沈黙依頼の宛先＝名前）は変えない。話者ゲート（知-f）は身元の層のままで正しい |
| 0-35 |  | **情-c** | 独り言は相手が居なければ発話しない（積まずに捨てる・独白として O に残す・不在なら写真も添えない） | **完了**（2026-09-13） | `_speak` が起点 `情動`＋ゲート閉で結末 `独白`（積まない）、`_user_content` が不在の独り言に写真を添えない。`イベント駆動ループ` v0.48 |
| 0-36 |  | **環-i** | つなぎを出したなら本応答も出す | **完了**（2026-09-13） | `_speak` が `said_fillers` の在る求めでは「居ない」「静穏時間」で止めない（「黙っていて」だけ守る）。`イベント駆動ループ` v0.49 |
| 0-37 |  | **情-d** | 欲求の発火間隔を倍々に伸ばす（REST・ESTEEM を除く・会話で基準へ戻る）／沈黙依頼の既定 60 分 | **完了**（2026-09-13） | `core/solitude.py`・`accumulate(solitude=)`・T の tick が数えて `agent_state` へ。ついでに tick の書き込みが commit されていなかった欠陥を直した。`設計詳細：発火・mood 機構` v0.3 §2-b・課題5 v0.40 |
| 0-38 |  | **情-e** | **情動で起きた求めを「自分がしたくなったこと」として渡す**（2026-09-13 新設） | **完了**（2026-09-13・実験 §33 → 修正・`イベント駆動ループ` v0.53） | 【事実】欲求で起きた求めは `utterance` が空なので、調停には `[人の言葉]` の見出しの下に求めの版（`[内的な促し:seeking] 探索したい気持ちが募っている…`）が入り、主LLM にも同じ文が人の発言と同じ形で渡る。「自分の中から湧いた」という前提はどこにも書かれていない。**実験**：同じ W で、いまの渡し方と「[自分の中から湧いたこと]」の見出し＋「これは自分がしたくなったことで、誰かに頼まれたのではない」の一行を付けた渡し方を、調停（分岐・つなぎの文）と主LLM（返事）で比べ、根拠台帳に残す。**修正**：起点が `情動` の求めでは、調停・主LLM の見出しと但し書きを変え、つなぎを出さない（つなぎは相手への返事の前置き）ことも含めて決める |
| 0-39 |  | **環-j** | 返事の途中で停止ボタンが効く（cancel を駆動体の求めへ届ける） | **完了・実機確認済み**（2026-09-13 17:36・つなぎ直後の停止で何も出ずに終わった） | 求めの開閉を通知（`set_request_state_listener`）、GUI は開いているあいだ停止を有効、押すと `agent.interrupt()`→`abort_current()`。鳴り始めた声は止められない（別課題）。`イベント駆動ループ` v0.52 |
| 0-40 |  | ~~出-l~~ | ~~see の画像を GUI に出す~~ | **取り下げ**（2026-09-13） | 在席フレームの定期表示で足りると決めた |
| 0-41 |  | **知-j** | **予定の確認**（2026-09-13 新設） | **実装済み・実機未確認**（2026-09-13・`設計方針_家の予定との接続` v0.1・ICS の URL は人が置く） | **完了・実機確認済み**（2026-09-13 16:26・J） |
| 0-42 |  | **知-k** | **Notion を読む口**（2026-09-13 新設） | **実装済み・疎通済み・実機未確認**（2026-09-13・`設計方針_Notionとの接続` v0.1） | **完了・実機確認済み**（2026-09-13 16:26・K／L） |
| — | **0. 完了**（それ以前） | 情-a／情-b／記-c／記-d／記-f／知-a／知-b／環-a／環-c／環-e（い・ろ・は・に）／環-f／環-g／環-h／出-a／出-b／出-c／出-d／出-e／出-f／出-h／出-i／出-j／出-k（い・ろ・は） |  | 完了 | 中身は `課題8_記録_完了した課題`。出-g は取り下げ、記-e は吸収 |

### 番号の付け方

課題は**群を表す漢字1文字＋英小文字**で識別する（`記-a`・`知-c` など）。数字を使わないのは、設計図の「課題 N」と混ざるためである。英数字（`A-1` 等）を避けたのは、本書の「段階A」「B-1」と衝突するためである。

| 群 | 意味 |
|---|---|
| **記** | 記憶と内省 |
| **情** | 感情と自発 |
| **知** | 知覚と在席 |
| **環** | ループの構造と撤去 |
| **出** | 出力と基盤 |

課題をさらに段へ切るときは、**いろは**を足す（`記-a-い`・`記-a-ろ`）。ここでも数字を避けるのは同じ理由である。

**着手順に番号は振らない。** 完了するたびに番号が意味を失い、順序が変わるたびに振り直しが要るためである。着手順は下表の並びで示し、行を入れ替えるだけで済むようにする。

**設計図の「課題 N」は別の体系で、ここでは触らない。** あちらは設計文書全体から参照される固定 ID である。

### 実機で確かめる宿題（出-e で溜まったもの）

**どれもテストは形しか見ていない。** 実機で人と話して確かめる。

| 何が変わったか | 何が変わり得るか |
|---|---|
| 主LLM のシステムプロンプトの書式（`---` → 見出し） | 自己認識や家族の記述をモデルがどう重く見るか |
| PAD 評価が一人称になった | 感情の値そのもの。**記憶に永久に残る** |
| 軽量LLM の6仕事に立ち位置と文脈が乗った | 判定の質と、1ターンの費用（トークンが増えた） |
| 調停の自己規定が一人称になり、安定部がシステム文へ移った | 分岐の選び方と、待ってもらう一言の口調 |

### 知覚のスライス（知-a・知-b・知-c の中身）

`知覚在席` §3-2 の二層のうち、**人物判定（誰か・InsightFace）を外した残りすべて**を、次の5スライスで進める。人物判定は 知-d（顔と声の登録）が持つ。

| スライス | 内容 | 課題 |
|---|---|---|
| S1 | 見る・首を振るをループへ繋ぐ。見た画像を VLM で言葉にする | 知-a・**完了**（意味づけは 2026-08-01 に修正） |
| S2 | 絶対 pan/tilt と定点 | 知-c の土台・**完了** |
| S3 | 在/不在（YOLO・定点別 presence マップ・滞留窓）→ QD | 知-b・**完了（実機で通し確認済み）** |
| S4 | 見えの普通（DINOv2 の定点別 EMA）→ $\widehat{S}_{景色}$ | 知-b・**完了** |
| S5 | 見回り | 知-c・**実装済み・実機未確認** |

**S1 の意味づけは長らく成立していなかった**（2026-08-01 に修正）。`extract_entities` が `json.loads` を直接呼んでおり、返答が Markdown のコードフェンスで包まれると先頭で落ちて `[]` を返していた。`see` の完了は「撮った。以上」で終わり、何が写っていたかが載らない。実機の llava:7b は**同じ画像・同じプロンプトでも回ごとに変わり**、包む回と包まない回が混ざる（1回目は素の JSON で `chair`・`person`、2回目はフェンス付きで `child`・`adult`・`chair`）。通る回と通らない回があるため、症状が間欠的に見えていた。剥がす処理は `core/helpers.py` の `strip_code_fence` に置き、`capability_state.save_manifest` にあった同じ処理もそこへ寄せた。先頭と末尾だけを見るので、返答の本文に含まれるコード片は壊さない。

**あわせて、ターン後の場面更新を止めた。** `_run_post_response_pipeline` を呼ぶのは `loop/event_loop.py` の1箇所だけで、そこは `camera_image=None`・`action_name=None` を渡す。つまり `SceneTracker.update` に渡るのはカメラ画像ではなく自分の発話テキストであり、そこから拾ったラベルが `scene_entities` に入る。`person` を含めば `_react_to_scene_events` が `greet_companion` を +0.6 した。在/不在は S3 の YOLO が担い、`知覚在席` §3-2 も在/不在を G（T 側・連続）の担当と定めるので、この呼び出しと `_react_to_scene_events` を撤去した。`SceneTracker` クラスそのものは残している。

**S4 で入れたもの**：`visual_norm.py`（EMA・コサイン距離・育つまで5回）、`recognition/visual_encoder.py`（DINOv2 small・384次元）、`store/pose_norms.py` と マイグレーション 034（専用テーブル `vector(384)`）。実測は、同じ画像で 0.0000、別の定点で 0.4167、同じ向きで高さ違い 0.4485。**tilt の 7.5° が pan の 22° より見えを大きく変える。** 推論は初回 4.9 秒、以降 19 ミリ秒。

**隔たりの分布（138 件・実機）**：最小 0.0020、中央 0.0060、90% 0.1360、95% 0.2200、最大 0.4510。「いつもどおり」は 0.006 前後に集中し、変化があると 0.1〜0.45 へ跳ねる。**min-max 係数は、在席側の $\widehat{S}_{在席}$ が揃ってから決める**（$\max$ で合成するので片方だけでは決められない）。

**S5 で入れたもの**：`look` を**定点名で呼ぶ形**へ（絶対 pan/tilt・道具定義の `enum`・定点が無ければ道具ごと出さない）。相対の首振りは撤去した（定点から外れて振動中ゲートに落ちるため）。`see` の完了に**どの定点を見たか**を残す（`ユースケース③` の「見た定点の印」）。**別の記録を足さない**のは、同じ出来事に2件残すと想起でどちらも上がって W の枠を食うため。移動中は定点名を付けない。

`ExplorationTracker`（相対移動の積算）は `look` から外したが、器の撤去は 環-c／環-d で旧 `run()` のプロンプトごと落とす。

**S2・S3 で入れたもの**：`poses.py`（定点の読み込み・プリセットとの統合・最寄り判定）、`tools/camera.py` の絶対移動と現在位置とプリセット読み、`presence_map.py`（定点別の在席）、`recognition/person_detector.py`（YOLO）、`recognition/motion_events.py`（ONVIF PullPoint）、`presence_sensor.py`（束ねる常駐）、`agent.start_autonomy()`（起動時に自律を回す）。実機で確かめた事実は `設計詳細_活性_O書込_知覚在席` §3-6。

**この過程で直したもの**：定点の距離が軸で意味が違っていた（pan 換算へ）、自律が人の発話まで回らなかった、ONVIF が繋がる前に購読を諦めていた、動体イベントの連打で 0.15 秒ごとに GPU を回していた。**カメラ側の動体追尾**は、切らないと定点が成立しない（アプリの設定）。

**在席の情報源を二層に分けた**：在/不在は `PresenceSensor`（YOLO・登録不要）、名前は PMM（顔の照合・`/speaker`）。照合が済んでいなければ「誰か」として、居ることだけ伝える。旧 知-b が挙げていた「退室が実機で起こせない」は、登録を待たずに動くようになった。

S1 は独立している。S2 が S3・S4・S5 の土台で、S5 は S2 から S4 が揃って意味を持つ。

**定点は ONVIF のプリセットから始める。** 加えて、置きたい角度を Config で持てるようにする（プリセットに無い向きを足せるように）。

**DINOv2 を採用する。** 新しい依存は要らない（`transformers`・`huggingface_hub`・`torchvision` は導入済み、`torch 2.10.0+cu128`・`cuda True`・RTX 3060 12GB）。`facebook/dinov2-small`（ViT-S/14・384次元）から始め、S3 の YOLO と同居させて足りなければ上げる。

**$\widehat{S}$ の min-max 係数は実測で決める**（`課題5` §I が「係数のみ課題7」としている箇所）。S4 で両系統の生の値をログに残し、その分布から出す。

**`pyproject.toml` の `torch = { index = "pytorch-cpu" }` は死んだ宣言だった**（出-u・2026-09-17 に確認して撤去）。torch は直接の依存でなく `tool.uv.sources` は直接の依存にしか効かないため、`uv.lock` の torch は PyPI（Linux x86_64 の 2.10 は CUDA 12.8 版）で、実物 `2.10.0+cu128` と一致していた。別の環境で `uv sync` しても CUDA 版が入る。

### 記-a（REST 内省）

**REST 内省の目的は自己認識の変革で、仕事は 4 層を順に更新すること**（2026-09-14 決定・`用語一覧` v0.70）。
1 パスは **出来事（畳む）→ 自己像（抽象化する）→ 設定値（調整する）→ 能力（再定義する）** の順である。
2026-07-29 の折衷型（読み込み → 蒸留 → open 棚卸し → Config 自己調整）はこの 4 層に置き換えた。棚卸しは
検討して消す方向、`self_narrative` は層 1 で代替する。

**4 層の外形**（2026-09-14 決定）：**状態はすべて DB、ファイルは既定と人の入力だけ**。層 1＝DB、層 2＝
`agent_state.self_image`（種は repo の `defaults/self_image.yaml`）、層 3＝`agent_state.config_overrides`
（既定は `config.py`）、層 4＝再生成した一覧と要約を DB に（`capabilities.yaml` は既定の一覧で、**実行時に
書き換えない**——いまの `save_manifest` が git 管理の file を上書きしているのはズレ・層 4 の実装で直す）。
`.env` は機密と機体固有のものだけで、4 層の値は `.env` に置かず読まない（登録した設定値の優先順位は
「env > DB > 既定」から「DB > 既定」へ改める・層 3 の実装で）。

| 段 | 層 | 内容 | 状態 |
|---|---|---|---|
| **記-a-い** | — | 起動条件と骨格 | **完了**（実機確認済み・2026-07-29） |
| 記-a-ろ | 1 出来事 | **畳む**：①日次の畳み込み（閾値以下）・②核の固め（$n \ge 1$・$I^\*$ 超過分）。前提の変更は 記-a-ろ-い（$HL$ 10 日・強化B 廃止・時間軸の一次絞りの撤去）、$n$ の減りは 記-a-ろ-ろ | **完了**（4 段とも・2026-09-15・`出来事を畳む` v0.7・実機未確認。段は下の小表） |
| 記-a-へ | 2 自己像 | **抽象化する**：層 1 の結果から抽象を取り出し、望み・気がかり・価値を更新する。字数に厳密な上限 | **完了**（2026-09-14・`自己像` v0.3・実機未確認）。種は `defaults/self_image.yaml`。中期的にいちばん大事 |
| 記-a-に | 3 設定値 | **調整する**：計測ログを読み、登録済みの範囲で値を動かす。読んだ計測ログを改名する | **完了**（2026-09-14・`設定値を調整する` v0.4・実機未確認）。DB > 既定 に改めた。`distill_min_a0`・$HL$ の LLM 規則は層 1 の②の後に接続 |
| 記-a-と | 4 能力 | **再定義する**：実装の docstring・`.env`・MCP 設定から一覧を書き直し、`ME.md` に「できること」を足した要約を作り直す（旧「アイドルの自己更新」を吸収） | **完了**（2026-09-14・`loop/rest_capabilities.py`・実機未確認）。一覧の現在値は DB（`agent_state.capabilities`）で `capabilities.yaml` は既定（実行時に file を書かない・書くのは `scripts/generate_capabilities.py` だけ）。一覧は 7 日〔仮〕に 1 度、形の検査（YAML・id の重複・有効条件・件数が半分未満に減らない）に通ったものだけ置く。要約は一覧か自己像（層 2）が変わった晩か、まだ無いときだけ作り直し、`ME.md` がそのまま先頭に残り 1,000 字〔仮〕以内のものだけ保存する |
| ~~記-a-は~~ | — | ~~open 棚卸し~~ | **検討して消す方向**（2026-09-14） |

**起動条件と骨格を先に置いたのは、起動が正しく掛かることを先に確かめるためである。** この機構はこれまで一度も動いたことがなく、中身を作り込んでから起動の誤りが見つかると切り分けが難しい。

**在/不在は `PresenceSensor` で見る。** `_social_presence_permission()` は PMM（顔の照合）と直近5分の発話しか見ないので使わない（環-b の欠陥）。センサが無い構成では内省へ回さず従来どおり QA へ積む（「センサが無い」を「誰も居ない」と扱うと、カメラ無しで常に内省へ落ちる）。誰か居るときの REST は自発ターンのままにする（「休みたい」と人に伝えるのは自然な振る舞いである）。

#### 記-a-ろ の段（層 1「出来事を畳む」・2026-09-14 整理）

設計の正本は `設計方針_REST内省_出来事を畳む` v0.2。2026-07-29 の「蒸留の設計」は下の表で**残すもの・置き換えたもの**に
分けた。段は上から順に着手する。

| 段 | 内容 | 状態 |
|---|---|---|
| **記-a-ろ-い** | 前提の変更：$HL$ 10 日（層 3 の設定値）・強化B 廃止・作られた日と思い出した時の役割分け・関連想起の 2 並び | **完了**（2026-09-14） |
| 記-a-ろ-は | **①日次の畳み込み**：前回の内省以降の出来事のうち核でない（$n<1$）ものを、自己エピソード（日）と関係のまとめ（人ごと）に要約し、元を supersede。固定手順のパス（道具持ちのループは要らなかった） | **完了**（2026-09-14・`出来事を畳む` v0.3 §3b・`rest_fold.py`・実機未確認） |
| 記-a-ろ-ろ | $n$ の減り $\Delta$（$\Delta \ge 0$・$n \ge 1$ を保つ）と、$I$ の計測（毎晩 1 行を計測ログへ） | **完了**（2026-09-15・`出来事を畳む` v0.4 §4b・`rest_info.py`・062 `last_referred_at`・実機未確認） |
| 記-a-ろ-に | **②核の固め**：$n \ge 1$ の記録の情報量が $I^\*$ を超えた分を、まとめ知識（週・月・人ごとの「いつもの」）に固めて supersede | **完了**（2026-09-15・`出来事を畳む` v0.5 §3c・`rest_core.py`・`core/bundling.py`・実機未確認）。同一→束ね（τ の覆い＋容量）→ 1 束 1 文 |
| 記-a-ほ | 面の段②（意味役割 `about`／`addressee` を本文から抽出し既存にもさかのぼって足す） | 後回し（層 1 の補助） |

**2026-07-29 の決定のうち残すもの**：内省は道具を持ったエージェントとして専用ループで回る（`event_loop.py` は
発話前提なので流用しない）／フル LLM を使う／可逆性は `supersede` のみ（戻す操作は作らない）／件数の上限は
設けない（軽い記録を外す）／終了時の日次要約（`_generate_day_summary`）は撤去し、過去日の backfill だけ残す。

**置き換えたもの**：

| 旧（2026-07-29） | 新（2026-09-14） |
|---|---|
| 畳む対象は LLM が選ぶ | **対象は機械（$u$ の閾値・核は $I^\*$ 超過分）、まとめ方は LLM** |
| 第 2 段の絞り $a_0 \ge$ `distill_min_a0` | 材料の選別として残すかは ろ-は の実装計画で決める（未決） |
| 前回の内省は `direction='休息'` で引く | 実物は `direction='内省'`（`rest.py`）。これで引く |
| 材料の `direction`：会話・観察・発話・好奇心 | 実物に合わせる：`発話`（人・自分）・`会話`（要約）・`観察`・`独白`・`情動`・`機器`。`求め`（版）・`保留`・`内省`・`記憶` は材料にしない（`好奇心` は今の実物に無い） |
| 道具 5 つ（読む・畳む・書く・調停の秒数・Config） | 4 層に合わせる：層 1＝読む・書く・畳む（固める）／層 2＝自己像を書く／層 3＝計測ログを読む・改名する・設定値を動かす／層 4＝能力を書き直す。**ろ-は では層 1 の 3 つだけ** |
| 1 パス＝読み込み→蒸留→棚卸し→Config | 1 パス＝層 1→2→3→4（棚卸しは消す方向） |

#### Config 自己調整の器（記-a-に の一部・完了）

値の優先順位は **DB（`agent_state.config_overrides`・内省が調整）> Config の既定** の 2 段で、`.env` は読まない（層 3 の実装・2026-09-14 で「env > DB > 既定」から改めた。4 層の値は `.env` に置かない）。

**範囲を登録した値だけを調整できる。** 登録が無ければ拒否する。`rate` や `theta_fire` のような式の骨格を内省が動かすと、蓄積や発火が壊れる。**接続情報は常に拒否する**（API キー・ホスト・認証・URL・デバイス id の 18 フィールド）。書き換えられると機器へ繋がらなくなり、`agent_state`（DB）に入るので `.env` を見ても原因が分からない。

登録は `core/settings.py` の `REGISTRY`（2026-09-17 時点で 11 件＋内部状態の分位 21 件。一覧は `課題5` §C3）。

**調停の時間切れも自己調整の対象にする**（記-a-に・2026-07-29 決定）。`AgentConfig.arbiter_timeout_sec` を **範囲 1.0〜10.0** で登録する。1.0 未満だと普通の会話（0.93〜1.10 秒）すら間に合わず常にフルへ倒れ、10.0 を超えると時間切れのときの遅延が実用に耐えない。

**材料は計測ログ（`rest_logs/measure.log`・記-i・2026-09-14）から拾う。** `app.log` は起動ごとに回転して消えるが、計測ログは REST が読むまで溜まる。**読み終えたら REST が同じ dir で `measure.log.<読んだ時刻>` へ改名する**（回転するのは REST）。いま入っているのは続き先の判定の結末だけで、調停の秒数などをここへ移すかはこの項目で決める。

**値は LLM が判断する。** ただし材料は機械が渡す：ログから「調停 X.XX 秒」「調停が遅れて返った：X.XX 秒」を拾い、**集計した数字**（件数・中央・p90・最大・時間切れの回数）をツールが返す。生のログ行を渡さないのは、数百行から分位点を数えるのが LLM に向かないためで、設計の「機械が測り LLM が提案する」切り分けにも沿う。**ログは 14 日で消える**ので、それより長い傾向は追えない。**内省が値を提案する部分は記-a-に で足す。** 蒸留を回して分布が見えてから決めるほうが確実で、いま作ると根拠のない値を根拠なく動かすことになる。

### 環-b（保留していた発話を配る起点）

在席がゼロから立ち上がった瞬間だけ配る。在/不在（知-b・`PresenceSensor`）は動いているが、配った直後の発話を配信ゲートが「聞く相手が居ない」で積み直す（ゲートが見るのは PMM の顔の照合と直近 5 分の発話）。直すのは 知-d（顔と声の登録）の後（F-2）。

### 知-c（昼夜の見回り）

ユースケース③ が定める、定点を巡って様子を見る動き。首を振ること（知-a）と、定点ごとに人が居るかを見ること（知-b）の両方が要る。

### 知-d（顔と声の登録）

登録に応じられる人が揃うまで着手できない。この課題が済むまで、**誰か**の層と、それに依存するものは動かない。

**声紋の現状**（2026-09-17 確認・記録）：`recognition/voice.py`（`VoiceIdentifier`・ECAPA-TDNN／speechbrain 1.1.0・
インストール済み）は実装があるが**呼び手が 0 件**（常時集音は書き起こしだけで照合を呼ばない）、登録された声も無い
（`~/.familiar_ai/voice_embeddings.pkl` は存在しない）。保存形式が pickle ファイルで、開発ルール「保存は PostgreSQL のみ」に
反する（顔の `embedding_store.py` も同じ）——知-d の実装時に PostgreSQL へ移す。着手順は F-1 のまま（前倒ししない・
2026-09-17 決定）。繋がれば「マイクは在席の証拠にしない」（知-p）に穴を開けずに**登録済みの家族の声だけ**を別扱いにできるが、
ECAPA の照合は 2〜3 秒以上の発話が要り、「はい」「止めて」のような短い声には効きにくい。

- **登録の入口**：顔と声の登録を GUI／CLI から行えるようにする。認識の載せ替え自体は実装済み（InsightFace(ArcFace) 顔＋ECAPA-TDNN 声・GPU 実行・`presence_watcher` が `person_arrived`／`person_left` へ結線）で、残るは登録の入口と実際の登録。
- **p 軸（在席者相関）の実挙動**：スコアラの結線は実装済みで、在席入力が埋まればそのまま効く。相関の相手が特定できないと実挙動を確かめられない。
- **`unconfirmed` のときに丁寧語にならない**：`ME.md` は「相手が分からないときは大人として扱い丁寧に話す」と定めるが、話者が `unconfirmed` のときは効かず、入力の文体を真似る。名前が入っているときは効く。原因は未特定。この症状自体は 知-b で扱えるが、直ったことの確認には相手を変えられることが要る。
- **人に合わせた発話の実機確認が未了**：`ME.md` は丁寧さを相手で決める（大人にはですます、子どもには打ち解けて、分からなければ丁寧に）と定め、話者は `_present_ctx` 経由でプロンプトへ渡り、`/speaker` はイベントループ経路でも効くようになった。**ただし、相手を変えて口調が実際に変わることは実機で確認できていない**。大人と子どもの双方で確認する。
- **一定時間で忘れる在席**：MI の想起に駆動される在席（活性の派生ビュー）は、記憶が人に紐づくこと（D-rel＝知-e）に依存する。知-e の後に、どちらの層が寿命を持つかを決める。

## 更新履歴

> v0.200：情-n・情-l-ろ・出-aa 新設→完了（`18aafed`・実機未確認）（2026-09-18）。
> v0.199：環-o 新設（GUI の Enter で末尾に「・」・命令側で吸収）（2026-09-18）。
> v0.198：情-m 新設→完了（タイマー中の操作の言葉は誰でも・`1d8b8ba`）（2026-09-18）。
> v0.197：出-x-ろ 完了（`9d2ff79`）・環-n 新設（設定画面が上書き .env に鍵を書く）（2026-09-18）。
> v0.196：出-x-ろ（守りが帰りにも効く・方針決定）・出-z（上限到達の答えで無いことを言う）・知-u（止めた返事・古いストップウォッチ）を新設（2026-09-18 実機 18:31〜37）。
> v0.195：出-x 完了（帰りは想起なし・new2 の先導文・24/24）・出-y 新設→完了（確認を機械の状態に・`confirmed` 撤去）（2026-09-18）。
> v0.194：出-x：B は入れず実機で様子を見る（経過は根拠台帳 §35）（2026-09-18）。
> v0.193：出-x の A を実験で減らして完了（案あ・2026-09-18）。B は次。
> v0.192：情-l 完了（2026-09-18）。
> v0.191：**14:38〜51 の実機（quiet）を書き戻した**——出-v・知-t・知-s・出-w 実機確認済み。新設：**情-l**（タイマー由来の沈黙が在席表の失効で解ける・方針決定済み）・**出-x**（調停が「確かめて」の返りで聞き返さず掛け直す・A を減らす方法を先に考える）。
> v0.190：知-s・出-w 完了（2026-09-18）。束 A は出-v（実機待ち）と知-t（実機待ち）だけ。
> v0.189：情-k・知-r 完了（2026-09-18）。
> v0.188：出-v の原因（誰か分からない相手をパパとして扱った）を書き、知-t（話者の指定に寿命）を A-1b に新設・机上完了（2026-09-18）。
> v0.187：**2026-09-18 12:26〜38 の実機（quiet）を書き戻した**——C-1・C-9・C-10 確認済み、C-5c・C-5e・C-11 一部。見つかった 5 件を束 A に新設：**出-v**（調べものの結果が W に届かない）・**情-k**（沈黙が明けたの求めが不在で立つ）・**知-r**（退室に居ないのに話す）・知-s（`/speaker` 直後の失効）・出-w（light の空 text）。環-b の起点が動いて配れた（在席表の失効で 0 → 1 が起きる）。
> v0.186：知-o（段 1〜5）と知-q（アラームの分離）を机上完了として C-5f・C-5g に（2026-09-18）。束 B は空。
> v0.185：出-u 完了（死んだ宣言の撤去）→ 束 0 へ。束 A は空（2026-09-17）。
> v0.184：知-d の節に声紋の現状（部品はあるが呼び手 0・登録なし・pickle）を記録（2026-09-17・着手順は F-1 のまま）。
> v0.183：知-p：不在の会話入力は入口止め（情-h の器）に揃えた（2026-09-17）。C-5c の見るもの。
> v0.182：情-j（声で見回す情動が上がる）を C-5e に新設・知-p の直しを「マイクは証拠にしない・自分の発話 60 秒」に・知-c に行き先の仕組み（2026-09-17）。
> v0.180：知-p（在席ゲート・`/speaker` が永久に立つ）と 知-n-ろ（タイマーの音）を C-5c・C-5d に新設・机上は完了（2026-09-17）。
> v0.179：情-i（「パジュ、静かにして」が名前の聞き違いで落ちる）を C-5b に新設・机上は完了（2026-09-17）。
> v0.178：**束を切り直した**（2026-09-17）——依存の鎖を束ごとに切り出す：A 机上（出-u）／B 机上→実機（知-o）／C 次の実機セッション（起動→会話→無人・09-16 の変更の確認 5 行を起こした）／D 貯まる・測る／E 見回りの鎖（知-c → 知-i）／F 人の登録の鎖（知-d → 環-b → 環-d の残り・知-e）。完了行 26 本を束 0 へ。あわせて「記-a」表の記-a-ろ（未実装→完了）・「Config 自己調整の器」（優先順位は DB > 既定・登録件数）・「環-b」（知-b 待ち→知-d 待ち）の古い記述を直した。
> v0.177：出-u（`torch` の索引が CPU のまま・実物は cu128）を A-11 に新設（2026-09-17・「知覚のスライス」の文中に残っていた宿題を表へ）。
> v0.176：B-2b と記-a-に の節の参照を `設定値を調整する` v0.4 に直した（2026-09-17）。
> v0.175：**完了した課題の詳細節を `課題8_記録_完了した課題` v0.1 へ移した**（2026-09-17・骨格の整理）——旧番号との対応・「完了した課題（記録）」・2026-08-01 の「次にやること」・旧表・出-e／出-h／記-f／出-i／出-g／記-e／出-j／出-f／記-d／小さな片付け／環-a／環-c／知-a／知-b／「掘り当てた不具合」。§7 に残るのは全課題一覧・番号の付け方・出-e の宿題・知覚のスライス・記-a・環-b・知-c・知-d。§3 の「提案」の表記と §7 の 07-29 の日付を落とした。

> v0.126 追記 5（2026-09-13）：`obsidian-memo` の資料一式を読み、**知-j を「カレンダー MCP をこちらで作る（ICS 直読から）」と決め、知-k（Notion を読む口・全部家族ティア）を立てた**。
> v0.126 追記 4（2026-09-13）：**出-n**（整合チェックの見直し）を束 1 に立てた。2 日間の違反 31 件のうち 21 件が、チェッカーに渡っていない申告についての判定だった。
> v0.126 追記 3（2026-09-13）：**情-e**（情動で起きた求めを「自分がしたくなったこと」として渡す・実験→修正）を束 3 に立てた。出-l（see の画像を GUI に）は**取り下げ**（要らないと決めた）。
> v0.126 追記 2（2026-09-13）：**知-i**（カメラの向きを名前で管理し、前回の見えと比べて会話）・**知-j**（予定の確認・中身は要確認）を束 4 に立てた。
> v0.126 追記（2026-09-13）：**知-h**（「誰かがいる」と「知っている人がいる」を分ける——配信ゲートを在/不在の層で）を束 3 の先頭に立てた。知-g（Obsidian）は後回し。
> v0.174：情-h（沈黙は入口で止め、明けたら 1 つの求めにまとめる）を A-10 に新設し完了（2026-09-16）。
> v0.173：知-o（指定された時間マイクを閉じる・確かめてから）を C-8 に新設（2026-09-16）。
> v0.172：知-m——`look` の帰りを `see` と同じ扱いに（`イベント駆動ループ` v0.71）（2026-09-16）。
> v0.171：環-m（列の取り出し口を 1 つに・会話入力は保留箱より先）を A-9 に新設し完了（2026-09-16）。
> v0.170：知-m の実機の穴（調停が `see` しか選べず首が動かない）を塞いだ・沈黙依頼は本人が話していいと言ったら解く（2026-09-16・`イベント駆動ループ` v0.68）。
> v0.169：出-q を机上で閉じた（自発の求めでは直近のやりとりを済んだこととして渡す・`イベント駆動ループ` v0.67）（2026-09-16）。
> v0.168：知-m ①（定点 5 件）・③（`look` の `direction`）（2026-09-16）。実機で②と符号を確かめる。
> v0.167：層 1 ①に $a_0$ を効かせた（`出来事を畳む` v0.7・2026-09-16）。`distill_min_a0` が初めて読まれる。
> v0.166：一晩の実機（09-16 04:28 の REST）を反映——層 1 は通った、層 2・4 は `complete()` の思考で予算切れ（環-l 追記・思考を切る）。自発は静穏時間で保留・間隔は 2h→4h→8h と伸びた。
> v0.165：出-t（light が道具の要る頼みに「できました」と言う）を新設・完了（2026-09-15）。
> v0.164：出-s（人が話しかける前の自発の発話が画面に出ない）を新設・完了（2026-09-15）。
> v0.163：出-r（関数呼び出し風の文で返った say を拾う）を新設・完了（2026-09-15）。知-g-ろ は実機で求めが積まれるところまで確認。
> v0.162：知-n（タイマー）を新設し完了にした（2026-09-15）。
> v0.161：**出-q を新設**（2026-09-15 実機）——自発ターンが済んだ入室に再び挨拶する。情-g の発火の間隔は伸びていることを確認（1 回目→2 回目・次は約 20 分後）。
> v0.160：知-g-い・知-g-ろ を完了にした（2026-09-15）。実機で見るもの：ゆうすけのターンで「コーチングの返事どうなった？」→ `調べもの vault …`・数十秒後に答え／「パジュへ」のページに 1 行足す→ 1 時間以内に `機器 メモ` の求め。
> v0.159：知-g-は を完了にした（2026-09-15・既に通っていたことを試験で示した）。
> v0.158：記-b を消し（記-a-ろ に吸収）、環-d の `relationship_state` を撤去にした（2026-09-15・064）。環-d の残りは `pending_store` だけ。
> v0.157：記-k・記-l を完了にした（2026-09-15）。
> v0.156：情-g・環-l を完了にした（2026-09-15）。次に実機で REST を 1 回（記-j）。
> v0.155：**2026-09-15 午前の実機ログから 4 件を新設**——情-g（自発ターンがひとりの回数を戻す）・環-l（`complete()` が思考ブロックで空）・記-k（層 3 の最小標本数）・記-l（層 1 ①の 1 晩の上限）。先に 情-g・環-l を直す。
> v0.154：環-d の旧 15 欲求（`desires.py`・GUI／TUI の受け渡し・pipeline の死んだ分岐・`agent_state.desires`）を撤去（2026-09-15）。残りは relationship_state／pending_store。
> v0.153：記-a-ろ-に（②核の固め）を完了にした（2026-09-15）。層 1 は 計測→減り→同一→束ね→固め→①。
> v0.152：記-a-ろ-ろ（$I$ の計測と $\Delta$ の減り）を完了にした（2026-09-15）。次は ろ-に（核の固め）。
> v0.151：環-d の今回分（読み手の無い 5 表と書き手の撤去・061）を完了にした（2026-09-14）。残りは desires／relationship_state／pending_store。
> v0.150：知-f（話者ゲート）を完了にした（2026-09-14）。
> v0.149：知-m を 3 段（①定点を足す→②名前で首が回る→③相対の向き）の一つの作業にまとめた（2026-09-14）。
> v0.148：知-m（相対の向きで定点を選ぶ）を新設・設計方針を承認（2026-09-14）。前提は定点 2 件以上（Tapo のプリセットを足す）。
> v0.147：束 D（実機）を更新（2026-09-14）——記-j を 4 層に広げ、机上で閉じた 09-14 の変更（記-a-へ・情-f・出-p・出-o・記-i・環-k）の実機で見るものを D-0b〜e に足した。
> v0.146：出-p（期間を持つ道具の見出しと調停の日数）を新設し完了にした（2026-09-14）。
> v0.145：記-a-と（層 4）を完了にした（2026-09-14）。REST の 1 パスは層 1 ①→層 2→層 3→層 4 と回る。旧 `save_manifest`／`should_regenerate_*`／`_regenerate_capability_manifest`／`_refresh_capability_summary` は撤去。
> v0.144：記-a-に（層 3）を完了にした（2026-09-14）。REST の 1 パスは層 1 ①→層 2→層 3（改名）と回る。
> v0.143：**記-j（REST 内省の 1 パスを実機で 1 回確かめる）を新設**（2026-09-14・D-0・実機セッションの先頭）。
> v0.142：記-a-へ（自己像）を完了にした（2026-09-14）。REST の 1 パスは層 1 ①→層 2 と回る。
> v0.141：記-a-ろ-は（①日次の畳み込み）を完了にした（2026-09-14・固定手順のパス・終了時の日次要約と日記を撤去）。次は ろ-ろ。
> v0.140：**記-a-ろ の段を整理**（2026-09-14）——ろ-は ①日次の畳み込み（専用ループの最小形を含む）→ ろ-ろ（$\Delta$・$I$ の計測）→ ろ-に ②核の固め。2026-07-29 の「蒸留の設計」を「残すもの／置き換えたもの」に分けた。
> v0.139：B-1 記-a-ろ-い を完了にした（2026-09-14）。
> v0.138：A-4 情-f を完了にした（2026-09-14）。
> v0.137：層 3 の設計方針を新規文書に書いた（2026-09-14）。これで 4 層のうち 1・2・3 が設計済み。層 4 は実装時に。次は 情-f の実装へ。
> v0.136：**情-f（内部状態の定性ラベルが効いていない）を新設**（2026-09-14・A-4）。4 層の文書化のあとに最初に実装する。
> v0.135 追記 2：層 1 の設計方針を新規文書に書いた（2026-09-14・使われる情報量 $I=\sum b_i u(t_i,n_i)$・蒸留の 2 段・$I^\*$ と $\Delta$ は層 3 の設定値）。
> v0.135 追記：4 層の外形（状態は DB・既定は GitHub・`.env` は機密のみ）を決め、記-a-へ の設計方針を新規文書に書いた（2026-09-14）。
> v0.135：**記-a を REST 内省の 4 層（出来事・自己像・設定値・能力）に切り直した**（2026-09-14）。記-a-へ（自己像）・記-a-と（能力）を新設、記-a-は（棚卸し）は消す方向。束 B の行も同じ並びに。
> v0.134：A-3 環-k を完了にした（2026-09-14・発話ごとにも走っていたことが分かった）。
> v0.133：A-2 記-i を完了にし、記-a-に に計測ログを読む・回転する約束を書いた（2026-09-14）。
> v0.132：A-1 出-o を完了にした（2026-09-13）。
> v0.131：**§7 の表を「何があれば進められるか」で束を切り直した**（2026-09-13）——A 机上・小さな穴（出-o・記-i・環-k）／B 机上・記憶の加工と自己調整（記-a-ろ→は／に→記-b→環-d）／C 机上で作り実機で確かめる（知-f→知-g→知-i）／D 実機セッション（H・出-n の 5・09-12 の変更・G・知-c・e 軸・知-l）／E 人が揃ってから（知-d→環-b・知-e）／0 完了。旧 3・4 束は全部完了か取り下げだったので 0 へ。
> v0.130：出-n の 1〜4（規則を 7 つに絞る・届いた結果を切らず渡す・言い直しに根拠・申告の母数は過去の列）を完了にした。5（実測）は後（2026-09-13）。
> v0.129：**2026-09-13 の実機確認を表へ反映**——出-m（Brave の検索が走っている）・D／E／F・記-h・環-j・知-j／知-k を完了に。新設：出-o（失敗した調べものの再試行）・知-l（ICS の更新遅れ）・環-k（ウォームアップ 2 回）・記-i（続き先の判定の記録）。出-n に Q の実測を足した。
> v0.128：記-h を**完了・実機未確認**にした（2026-09-13）。
> v0.127：**記-h（直近のやりとりを W の枠へ一本化・$n$×2・REST で見直し）を立てた**（2026-09-13・§7 表 2-9）。同日の実機 F で、こうきと話した直後の入室の反復に直近が渡らず挨拶した件が起点。
> v0.126：**全課題を一つの表に束ねた**（2026-09-13・§7 冒頭「全課題一覧」）。束は 6 つ——1 いま壊れているもの（**出-m** 検索と取得の MCP・**記-g** PAD の測り直し。ともに新設）、2 実機で確かめていないもの（シナリオ A・D〜H と 09-12 の変更）、3 自発と在席（情-c → 環-i → 情-d）、4 GUI と操作（環-j・出-l）、5 外を見に行く・誰と話しているか（知-g-ろ／は → 知-f → 知-g-い → 知-d → 環-b → 知-e）、6 記憶の加工（記-a-ろ → は／に → 記-b → 環-d）。旧「これからやること」「完了した課題」は記録として残す。
> v0.125：**環-j（停止ボタン）・出-l（see の画像を GUI に）・知-g（Obsidian を見に行く）を立てた**（2026-09-12）。前 2 つは実機確認の申し送り。知-g は 3 段——い＝聞かれたとき Vault を引く、ろ＝定期的に読んでメモを拾う、は＝情動で起きた求めで調停が家の記録を見に行く。
> v0.124：**情-d（欲求の発火頻度を下げる）を立てた**（2026-09-12）。昼の SEEKING は中立で 5 分に 1 回で、情-b は夜しか下げていなかった。実機で 1 時間に 16 回起き、費用の大半になる。REST・ESTEEM を除く欲求は、人と会話しないかぎり間隔が倍々に伸びる（会話で基準へ戻る）。沈黙依頼の既定を 60 分へ。
> v0.123：**3つの課題を立てた**（2026-09-12・実機確認で露見）。**情-c** 独り言は相手が居なければ黙る（在席ゲートが閉じているあいだは発話しない。求めは起きる）、**環-i** つなぎを出したなら本応答も出す（つなぎと本応答で配信ゲートの判断が食い違う）、**出-k** 返事の長さと速さ（い＝測る、ろ＝`max_tokens` を直前の会話の長さとネット調査の有無で決める、は＝Sonnet／Haiku）。着手順は 情-c → 環-i → 出-k-い → 出-k-ろ。
> v0.122：**公開面を絞り、線を口へ寄せきった**（2026-09-12・環-e-い）。公開面 73 → **65 種**
> （役割の終わった8面を落とした——`link` に置き換わった2つと、旧 ReAct 経路の
> `format_*_for_context` 6つ）。記憶へ直につながる線は 24 → **12 箇所**（`agent.py` は
> 16 → 4）。口に欠けていた4つ（書くときの強さ・その日づけ・種別の取りこぼし・同じ月日）も
> 埋めた。**寄せなかったのは旧 semantic layer の4箇所**で、D-2 で撤去する。

> v0.121：**想起も口を通した**（2026-09-12・環-e-い）。器を `Recalled` へ移し、`confidence`
> を採点として足した。**W の1行は核（`workspace._lines`）が組む**——記憶に置いたままだと
> 記憶が OIF の器を知ることになる（依存が逆向き）。主体を引く口（`OIF.actors`）も足した。
> `agent` 側の呼び手は辞書のままなので `format_for_context` は残し、主体の文面は module の
> 関数へ出して両方から使う。**残るのは公開面を絞ることと、`loop/rest.py` の `kind`**（記-a）。

> v0.120：**読むときの視点を口が受け取れるようにした**（2026-09-11・環-e-い）。`View` は
> 「読むときの視点」と名乗っていたのに**誰の面かの欄が無く**、そのまま呼び替えると想起が
> 話者の面から `__self__` の面へ移って記-f の直しを壊すところだった。`viewpoint` を足し、
> **人ごとの実体は `pmm` が持つもの**を口へ渡す（作り直さない）。**呼び替えはしていない**
> ——器の移行（`Recalled` に `confidence`・`date`／`time`・`actor_name` が無い）と5つの
> 読み手の付け替えは別の段である。**v0.119 で「残っているもの」の節を二重に作っていたので
> 1つへ畳んだ。**

> v0.119：**関係とベクトル埋め込みも OIF へ通した**（2026-09-11・環-e-い）。関係の**書く口は
> 1つ**（`link(kind, members)`）で、読む口は2つ（`exchanges`・`latest_origin`）。
> **`RelationStore(self._memory._ctx)` と記憶の私的属性を掴んでいた1箇所が消えた**——
> `ObservationMemory` すら通っていなかった。埋め込みは `agent` の2行を `OIF.health()` へ。
> **残るのは想起（器が違う）と `rest`（`kind` の食い違い・記-a で決める）、公開面を絞ること**。

> v0.118：**書き込み側を OIF へ通した**（2026-09-11・環-e-い）。ループの9箇所
> （`write` 6・`supersede` 2・`append` 1）で、**器は変わらないので挙動も変わらない**。
> 通したことで**書き手が9箇所すべてで明示された**（`OIF.write` は `writer_id` が必須）。
> **環-e-い は完了ではない**——関係は OIF に口が無く（058〜060 で入った機構に設計が
> 追いついていない）、想起は器が違い、`rest` は `kind` の食い違いを抱えている。
> 役割のうち「ベクトル埋め込みを隠す」「公開面を絞る」は一歩も進んでいない。

> v0.117：**O へ書く 15 箇所を網羅し、書き手の抜けを1つ塞いだ**（2026-09-11・記-f）。
> 抜けていたのは `OIF.write` で、`writer_id` に既定（`None`）を置いていた。必須にし、
> 空文字も弾く。OIF はまだ呼ばれていないので、**通し始めた瞬間に効く穴**だった。
> 同名だが O へ書かない2件（見えの普通の保存・PIL のサムネイル作成）は理由を書いて外した。
> 網羅はテストが AST で保つ。

> v0.116：**記-f（誰の記録かを言う）を立てて完了した**（2026-09-11）。`発話` の衝突を
> 掘ると、根は **`actor` の面**にあった——人の言葉が `__self__` の面に立っており、話者の面
> には**その人が言ったことが1件も無かった**。本番 DB（読み取り）では 7〜8月の 484件が正しく
> 話者に立っており、イベント駆動ループへ移したときに落ちた配線だと分かった。あわせて W の
> 1行を**言葉**にした——`(会話・ゆうすけ)` のように種別と名前を並べても読みにならない
> （`actor` の意味は記録ごとに変わり、`会話` では**その会話の相手**であって書き手ではない）。
> **`direction` の値を分ける案は取り下げた。**

> v0.115：**環-f-い-2（会話入力も列へ）を完了し、環-f を全段完了にした**（2026-09-11）。
> **IIF の待ち行列に4つのきっかけが揃った。** 直す前に名前の食い違いを2つ見つけた——
> **`発話` が3つの別物を指していた**（人が話しかけたこと・パジュのつなぎ・パジュの答え。
> `direction` として DB にも入っている）ことと、**`begin_request` が返すのは求めの終わりでは
> なく最初の反復の出力**だったこと。種別は用語一覧の語 `会話入力` を使い、口は
> **`push_utterance()`** へ改めた。呼び手の約束（`Future` でその反復の出力を返す）は変えて
> いない。**`direction="発話"` の衝突は別の課題として残した**——本番 DB の値であり、
> `RECALL_WEIGHTS` の鍵と環境変数名にも触るため。

> v0.114：**環-f-い-1（待ち行列を1本にする）を完了した**（2026-09-11）。3本のキューを1本に
> し、`Completion` を **`Trigger`（きっかけ）**へ改名した。調査中の情動・機器は**保留箱**へ
> 避ける——3本だったころ「キューに残す」ことで待たせていたのと同じ意味である。**順序づけが
> `_take_trigger()` の1箇所に集まった。** 機器と情動が同時に届くと情動を黙って捨てていた
> 取りこぼしも消えた。あわせて、**装置が event loop より長生きしたときに待ち行列を作り直す**
> ようにした（旧コードでは空回りとして隠れていた不具合が、1本にしたことでハングとして表に
> 出た）。残るのは 環-f-い-2（会話入力も列へ）だけで、**GUI と CUI の約束が変わる**。

> v0.113：**出-i を完了にした**（2026-09-11）。**本体（366円）は既に実物へ入っていた**——
> 安定部と道具の `cache_control` も、`warm`／`forget`／`aclose` の3口も在り、
> **無かったのは呼び手だけ**だった。終了時の `agent._close_backends()` を足し、心拍は
> 入れないと決めた（**温めると書き込みが丸ごと上乗せになる**：1.148円 対 1.063円）。
> 作られることのない `_cache_heartbeat_task` を畳む3行も撤去した。

> v0.112：**出-h-ろ（軽量LLM）を実装し、出-h を全段完了にした**（2026-09-11）。申告は
> **調停とは別に、答えたあと背景で1回聞く**（案ホ）。調停の JSON へ足すと `light` を選ぶ側へ
> 判断が寄ったためで、切り離せば調停のプロンプトは変わらない。あわせて、**申告が当たる面を
> 想起した面へ揃えた**（③）——想起は話者の面を通るのに申告は `__self__` の面へ行っており、
> 話者が同定されている場面では0行に当たっていた。`Decision` が `mem` を運ぶ。

> v0.111：**出-h-い（主LLM）を実装し、`enum` の案を取り下げた**（2026-09-10）。直したのは
> `say` のスキーマ2箇所——**申告を必須にする**（`required` に `memory_verdicts`）ことと、
> **4つの判定に別々の引き金を書く**こと。`id` の `enum` は入れない。**道具は安定部と同じ
> キャッシュ範囲にあり**（出-i）、W の id を入れると想起のたびに定義が変わって毎ターン
> 書き直しになる（366円 → 738円）。実測でも `enum` の有無で成績は変わらなかった。
> 保留していた3論点も測って閉じた。**軽量LLM で同じ指示が効くかは未確認**なので、
> 出-h-ろ ではそのまま持ち込まない。

> v0.110：**出-h の切り方を「どちらの LLM か」で見出しへ出した**（2026-09-10）。段の名前が
> `light` の経路という**機構の名前**で始まっており、要になる語（**主LLM**・**軽量LLM**）が
> 見出しに無かった。症状も並べた——**主LLM は口があるのに使わない／軽量LLM は口そのものが
> 無い**。

> v0.109：**出-h を 出-h-い と 出-h-ろ に切った**（2026-09-10）。ろ は今日見つけたもので、
> **軽量LLM が `light` で答えて閉じる経路に申告が無く、記憶が育たない**。`apply_memory_verdicts`
> の呼び手はリポジトリに1箇所だけで、それは主LLM の返りを実行する側である。混ぜると
> 「構造で求めたのに出ない」のか「通らない経路がある」のかが切り分けられなくなるので分けた。

> v0.108：**群B の点検で、完了表示を実物へ合わせた**（2026-09-10）。**出-f と 出-j は
> どちらも完了しているのに「立てた」のままで、依存表も未着手として書いていた**。あわせて
> 環-e（AIF・DIF・環-e-に が完了。OIF だけ呼ぶ側が残る）と 環-f（環-f-ろ は 環-h へ吸収して
> 完了）も直した。

> v0.107：**環-h の完了を反映した**（2026-09-09）。未決6つは同日すべて決まり、段い・段ろ・
> 段は-1・段は-2・段に を実装した。ループの中に同期の主LLM 呼び出しは無くなった。実測で
> 学んだこと（リセットで「何回目か」の手がかりが3つとも消えた／`_iterate` は薄くならず、
> 引き継ぎ先は に-5）も書いた。

> v0.90：**記-e を取り下げた**（2026-09-06）。立てたときの前提が二つとも実コードと違って
> おり、実際に壊れていたのは「問いは残るのに答えの逐語だけが畳まれる」ほうだった。閉じる
> 操作を置かない形で `設計方針_MI間の関係` に吸収した。

> v0.89：**心拍を定期実行しないと決めた**（2026-09-05・計算した）。安定部＋道具で 5,671
> トークン。5分ごとに温め続けると**1か月 735円**かかり、キャッシュで浮く額（1000ターンで
> 372円）を食い潰す。**月2000ターンを超えないと心拍のほうが高い。** そもそも会話中は送る
> たびに TTL が延びるので、心拍が要るのは切れ目だけであり、**「待つ間ずっと温める」と
> 「来たときに書き込む」がほぼ同額**である。`warm` の口は残す（起動直後の1回に使える）。

> v0.88：**出-g（Gemini に明示キャッシュ）を取り下げ、出-i（キャッシュの寿命を持つ口）を
> 立てた**（2026-09-05）。出-g は**そもそも作れなかった**——軽量LLM の安定部は1,012トークンで、
> 明示キャッシュの最小長 1,024 に12トークン足りない。作れても**保存料が入力単価の4倍**
> （$1.00/1Mトークン・時）で、**1日30ターン未満なら損**になる。家庭で使う頻度では元が取れない。
> 出-i は、§27 で確定した「`haiku` ＋キャッシュ」を実物にするための口である。**この API に
> セッションという保持物は無く、あるのはキャッシュだけ**——毎回1発言だけを送っており、
> 会話履歴はサーバに残らない。鍵の単位は**安定部ごとで3本**（調停と軽量の4仕事が1本を共有）。
> **中身を持つのは `anthropic` だけ**で、他5つは「何もしない」を既定とする。

> v0.87：**出-h に「構造そのものの再検討が要る」を追記した**（2026-09-05）。場面を40へ
> 増やした実験Aで、**申告だけが構造で縛られていない唯一の指標であり、そこだけモデル差が
> 出た**（`haiku` 30.0% 対 `sonnet` 88.9%）。他の4指標では `haiku` が勝っている。ただし
> **案イ（`required` と `enum`）だけで足りるかは分からない**——必須にすると出力が増えて
> 遅くなり、`_apply_memory_verdicts` が守っている「申告漏れと本当に使わなかったことの
> 区別」が消える。**出させることと、正しく出させることは別である。**

> v0.86：**出-h（記憶の申告を構造で求める）の設計方針を決めた**（2026-09-05）。実装はまだ
> しない。主LLM の実測で、W に記憶が並んだ9回のうち `memory_verdicts` を出したのは `haiku` が
> 2回、`sonnet` が7回だった。**申告しなければ記憶は忘れられていく。** 原因は3つ——構造で
> 求めていない（`required` に入っていない）、規則が14個の制約に埋もれている、12桁の id を
> W から写させている。**案イ**（記憶がある反復だけ `say` のスキーマを差し替え、`required` に
> 入れ、id を `enum` にする）を採る。**現状の入力から構築でき、関数構成は変えない**——
> `_tools()` は毎反復その場で組まれ、`self._w_id_map` はその前に確定している。
> **この差はモデルの優劣ではない可能性がある**ので、出-h は主LLM の選定より前に置く。

> v0.85：**出-g（Gemini に明示キャッシュを入れる）を型枠として置いた**（2026-09-05）。実装は
> しない。出-e で軽量LLM へ文脈を渡すようにしたので、毎ターン同じ安定部が4回以上流れる。
> **暗黙キャッシュは当てにできない**——仕様が「当たれば渡す」としか書かず、実測でも最小長を
> 超えて当たらないモデルが4つ、当たった1つも36%だけだった。採用した `gemini-3.1-flash-lite`
> は効かない。明示キャッシュなら見積もれるが、**保存時間にも課金される**ので、1,020トークンを
> 常時保持する費用と毎回送る費用のどちらが安いかを実装の前に計算する。

> v0.84：**記-e（応答した発話を閉じる）を立てた**（2026-09-05）。`[D-発話]` が定める
> 「応答で supersede」が実装されていない。本番で人の発話 521 件のうち 174 件が閉じられておらず、
> 閉じている 347 件もほとんどが**日次要約による偶発**である（応答で閉じたのは8件）。
> **紐づけの材料は既にある**——応答の `parent_id` が起点を指し、97.4% が親を持つ。
> **つなぎで畳む恐れも無い**（`action` 分岐は `_finish` を通らず、つなぎは O に書かない）。
> 足りないのは閉じる1行で、`close_parent_id` は渡されているのに使われていない。
> 情動・機器の起点をどう扱うかは未決である。

> v0.83：**出-e を完了した**（2026-09-05・全4段）。出-e-に で、**調停器はパジュの心そのもので
> ある**と決めて自己規定を一人称へ書き直し、構造も寄せた。1本の文字列だったのは `complete()` に
> システム文の口が無かったからで、安定をシステム文へ分けると交互に並ぶ問題が消えた。
> **一度「寄せられない」と誤って判断した**（制約の由来を確かめずに、制約を設計の前提として
> 扱った）。あわせて、出-e で溜まった**実機で確かめる宿題4件**を節に起こした。

> v0.82：**出-e-は の完了を反映した**（2026-09-05）。軽量LLM の6仕事すべてに立ち位置と部品が
> 届いた（PAD 評価・気分の分類・一言要約・欲求の列挙はパジュとして、整合チェックと同一意図は
> 外から測る）。PAD プロンプトも一人称へ差し替えた。残るは 出-e-に（調停を寄せる）で、
> `.env` の切り替えより前に行う。実装の詳細は `モジュール分割設計` v0.16 へ移した。

> v0.81：**出-e に段を切り、い・ろ の完了と に の位置を記録した**（2026-09-05）。
> 出-e-い（文脈を組む口）と 出-e-ろ（主LLM を寄せる）が完了。**当初の「挙動不変で寄せる」は
> 成り立たなかった**——2箇所は並びは同じでも書式が違い、寄せるにはどちらかへ揃えるしかない。
> 見出し側へ揃えたので、**主LLM のシステムプロンプトが実際に変わった**（実機で確かめる対象）。
> 出-e-に（調停を寄せる）は 出-e-は の直後・`.env` の切り替えより前に置いた。まだ変わる口へ
> 事故の多い経路を寄せないためである。**飛ばさない**と明記した。

> v0.80：**出-e（文脈を組む口）と 出-f（整合チェックの再接続）を立てた**（2026-09-05）。
> 出-e は出-d の対で、「何を求めるか」に対する「何を与えるか」を揃える。同じ組み立てが
> `build_event_system_prompt` と `ARBITER_PROMPT` の2箇所にあり、同じ不変条件を別々に守って
> いた。**感情を作るのはパジュである**と決め、立ち位置を仕事ごとに割った（PAD・欲求・要約・
> 気分は一人称、整合チェックと同一意図は外から測る）。出-f は、整合チェックの**呼び手が
> 環-c で消えて一度も動いていない**ことが分かったため。さらに、旧経路が渡していた
> `agent.messages` に**追記する箇所が1つも無く、ターンをまたぐ会話履歴が実質失われている**
> ことも分かった。繋ぎ直す前にそこを回復する。状態で分かる違反は機械で落とし、意味の判断
> だけ軽量LLM へ回す。あわせて 環-d の `self_narrative` を**移管しない**と決めた。

> v0.79：**出-d を完了した**（2026-09-04）。形のある答えを求める口へ7箇所を通し、
> `backend.py`（1,790 行・6つのバックエンドが同居）を `backends/` の10ファイルへ分けて
> `Protocol` で約束を表した。**これで AI 再選定の前提が揃った**——モデルを替えたとき
> 壊れる場所が1箇所に閉じ、どのモデルがどの形をどれだけ外すかが測れる。**次は AI の
> 再選定**（課題7 は「軽量LLM ＝ Gemini flash-lite・主LLM ＝ sonnet」を前提に書かれて
> おり、実物（haiku 4.5／kimi）と食い違っているので、そこから直す）。

> v0.78：**家の記録（`obsidian-memo`）が繋がり、家族ティアの1本だけを通した**
> （2026-09-04）。道具は2本で性質が正反対である——`get_house_rules`（即座・話者ゲート
> 不要）と `ask_vault_yusuke`（数十秒・Vault を全部読む・**話者ゲートが要る**）。
> 申し送りは「`_build_tool_definitions()` が個人ティアを落とす」と書いていたが、
> **その関数はこのリポジトリに存在せず**、MCP の道具はそもそも `_ACTIONS` に載っていな
> かった（＝主LLM に渡っていなかった）。**`get_house_rules` だけを載せ、
> `ask_vault_yusuke` は載せない**——存在しない道具は呼べない、が唯一の確実な守り方で
> ある。話者ゲートは **知-f** として立てた。

> v0.77：**出-d（モデルへの問い方を揃える）を立て、最初の段を実装した**（2026-09-04）。
> `backend.complete()` は文字列を返すだけで、**7箇所がそれぞれ解釈していた**——数値を
> 正規表現で拾う、集合に含まれるか見る、`yes` で始まるか見る、JSON として読む。正しく
> 扱えていた3つは「読めなかった」を記録してから落としていたが、残る4つは黙って捨てて
> いた（`.startswith("yes")` は日本語の「はい、同じです」を**偽**にする——`求めの版
> チェーン` の「同じ語は二度と調べない」性質が、モデルの言語で崩れる）。
> `core/structured_ask.py` に5つの口を置き、**取れたか取れなかったかだけを返す**。
> **AI 再選定の前に置くのは、どのモデルがどの形をどれだけ外すかを測れるようにするため**
> である。なお 出-d-い のコミット（`f8bc0`）は、番号を立てる前に書いたため
> メッセージに存在しない番号（「複-a-い」）が入っている。履歴は書き換えない。

> v0.76：**出-c を完了した**（2026-09-04）。重みを持つ5つが `ModelResource` に乗り、
> 遅延読み込み・失敗の記憶・並行制御・載せる先・先読みが1箇所へ集まった。最後の段
> （`Encoder` で符号化器を束ねる）は**実物を見て作らないと決めた**——揃っているのは
> 「何かをベクトルにする」点だけで、共通と見ていた次元合わせと正規化は符号化器の問題では
> なく記憶側の道具だった。**入出力の型から演繹せず、実物の境界から起こす**という原則から
> 出た判断で、STT・TTS に型を作らなかったのと同じである。**次は AI の再選定**（差し替えの
> 影響が1箇所に閉じた状態になった）。

> v0.75：**出-c-ろ と 出-c-は を実装した**（2026-09-04）。重みを持つ5つ（YOLO・DINOv2・
> InsightFace・faster-whisper・bge-m3）が `ModelResource` に乗り、遅延読み込み・失敗の
> 記憶・並行制御・載せる先・先読みが1箇所へ集まった。対象は当初「8つ」と書いていたが、
> 数え直すと5つだった（LLM 3つと TTS は API やサーバ越しで**重みを持たない**）。
> 移す途中で、`face.py` と `tools/stt.py` が**コメントと実装がずれていた**ことが分かった
> ——「無効化する」と書きながら失敗を記憶せず、呼ぶたびに読み直していた。**残るは
> 出-c-に**（`Encoder` で3つの符号化器を束ねる）。

> v0.74：**出-c（モデル資源の共通化）を立てた**（2026-09-03）。いま動いているモデルは8つ
> （主LLM・軽量LLM・VLM・言語の符号化器・見えの符号化器・顔・検出器・書き起こし・音声合成）
> で、そのうち3つが**同じ形を少しずつ違う書き方で**解いていた（遅延読み込み・失敗の記憶・
> 並行制御・載せる先）。型枠 `ModelResource` へまとめ、能力ごとの型は `Encoder` だけ作る。
> あわせて **`設計図` の ③-2 と [D-I内部] の食い違いを解消した**——主LLM を DIF の向こう
> （外部の機械と同列）から外し、**モデルは口でなく資源**とした（`設計図` v0.85）。
> 口は増やさない。**AI の再選定より先に置く**のは、差し替えの影響が1箇所に閉じてからの
> ほうが、モデルを替えたときに何が変わったのかを切り分けられるためである。

> v0.73：**着手順の外で進んだ2つ（16本の復元・situated V2 と MI の器）を完了表へ入れ、
> 溜まっている宿題を5件足した**（2026-09-03）。着手順の表は実機を要する課題の順序を表して
> おり、この2つは実機を要さないので行を持っていなかった。**表の「← いまここ」は 知-c の
> まま動いていない**——次の律速は実機に触れる時間である。足した宿題は、V2 と復元の実機確認、
> 日次要約の日付（`timestamp` が書いた日で対象日でない）、時間軸の索引（2表にまたがる）、
> 死んだ `self_model` の読み手3つ、中立で埋まった PAD 2941 行。

> v0.72：**051〜054 を復元し、失われた16本の復元が終わった**（2026-09-03）。051 は毎ターンの
> `self_model` を書く経路ごと撤去（読み手は本番コードに 0 件）、052〜054 はつなぎの発話を
> 記憶から外した（`_said_fillers` がプロンプトへ載せるので二重だった）。**049 だけは
> 復元しない**（ゼロベクトルを NaN に落とす応急処置で、050 が入れば要らない）。
> 本番の `schema_migrations` との差は、049 と、復元の過程で足した 055・056 の3件だけである。
> **次は実機での確認**——段1〜5・案3・049〜054 のどれも実機で動かしていない。

> v0.71：**050（PAD は未測定でありうる）を復元し、049 は復元しないと決めた**
> （2026-09-03）。評価器がゲート未満で P/Pn/Dom を気分の値で埋めていたのをやめ、未測定は
> NULL で残す。これが感情軸の母集合を半分（6433 行中 2941 行）一点に潰していた原因である。
> A（高ぶり）は機械値なので常に入る。049（索引を cosine へ）は「ゼロベクトルを NaN に
> 落とす」応急処置と読め、050 が入れば要らないので復元しない。**残るは 051〜054**
> （毎ターンの `self_model` と「つなぎ」の記録をやめる。退避表から規則が読める）。

> v0.70：**段5（視点列3つの撤去）を実装し、situated V2 が閉じた**（2026-09-02）。
> `observations` から `writer_id`／`subject_id`／`participants_json` を落とし（056）、
> 面を立てる材料は引数で渡す形にした。本文の更新は面を作り直さず `reembed_facets` で
> なぞる（**REST が足した面のベクトルが本文へ追随するようになった**）。重複判定は
> `actor` の面へ JOIN。`subject_id` は実在の 397 件の全件が既に面を持っていたので、
> 写さずに落とせた。**次は 049〜054 の復元**（049・050 はスキーマから中身が確定して
> おり、051〜054 は退避表から規則が読める・`復旧記録` v0.18）。

> v0.69：**案3（MI を出来事から面へ）を実装した**（2026-09-02）。想起の3本から
> `DISTINCT ON (o.id)` を外して面ごとに返し、面の言葉（2149 行あって 1 箇所も読まれて
> いなかった）を返すようにした。MI の `id` は面、視点3属性は落ちた。あわせて 055
> （`relation_key` の既定値）を足し、`拡散想起_統合設計` の段4 反映漏れと 042 の記述を
> 直した。**次は段5＝視点列3つの撤去**（器が決まったので、列を落とせる）。
> 実装中に、`test_event_loop.py` の待ち条件が2箇所甘く（版チェーンの版1 で抜けていた）、
> 並列実行のときだけ落ちていたのを直した。

> v0.68：**MI の器を「記憶 × 面」へ改めることを決め（案3）、049〜054 の中身を特定した**
> （2026-09-02）。MI はいま「観測1行」を指しているが、044 で `content`・時間の起点・
> 根づきの `n` が面へ移り、想起も面のベクトルで探しているので、**器だけが 044 以前の形で
> 残っていた**。表は分けたまま（`observations` ＋ `situated_memories`）、**MI の同定を
> 面（`situated_memories.id`）にし、想起は面ごとに返す**。分けたままにするのは、
> `superseded_by` が出来事の列にしかないことで「**畳んでも面は残る**」が構造的に
> 保証されており、統合するとその保証が運用の約束へ落ちるためである（版チェーンの前提）。
> 実測では、想起は必ず1視点で引くので、視点を固定すると同じ観測の面は 1.00〜1.14 枚しか
> 並ばない。段の順序は **案3（器）→ 段5（視点列3つの撤去）**とする（器が決まれば、
> MI から視点3属性を落とす判断が自動的に付く）。

> v0.67：**段4（拡張想起の関係辺を situated の面へ・共通の記憶）を実装した**（2026-09-02）。
> (B) エンティティ辺の種を `about`→`present`→`actor` の順に、母集合を `about`／`present` の
> 面に付け替えた（**母集合に `actor` は入れない**）。`fetch_perspectives` は
> `fetch_relation_persons` へ改名。新設の `shared_memory_ids` で、在席者が2人以上いるとき
> その全員が関係を持つ観測＝**共通の記憶**を候補へ足す（本番のダンプで 190 件）。
> これで `observations` の視点列3つは読み手が居なくなり、**次は段5＝視点列3つの撤去**。

> v0.66：**段3（042＝`observations.person_id` 撤去）を実装した**（2026-09-02）。
> 047 で関係の面が立ったので、設計が定めた順序——関係生成が立ってから列を落とす——の
> 条件が満たされた。所有者絞りを 12 箇所の SQL から外し、`_read_observations_by_kind` から
> person 引数を落とした。**重複判定の30秒窓だけは絞りを保ち、`writer_id` へ移した**
> （重複とは「同じ書き手が同じ内容を同じ kind で窓の内に」であって、家族の二人が同じ挨拶を
> したものは重複ではない）。`recall_self_model` と `recall_curiosities` が本番で常に空を
> 返していた不具合も、絞りごと外れて消えた。次は段4＝拡散想起の付け替えと「共通の記憶」。
> → v0.67 で実装した。

> v0.65：**段2（047＋048＝関係エッジの機械的な土台）を実装した**（2026-09-02）。
> 面の生成を「全登録人物への無差別コピー」から「関係のある人だけ」へ変えた
> （≈38,600 行 → ≈6,806 行）。**面の生成は二段**で、047 が担うのは段①＝機械
> （`actor`←`writer_id`／`present`←`participants_json`）だけ。段②＝意味役割の抽出は
> **REST 内省の仕事**として 記-a-ほ を新設した（既存の観測にもさかのぼって足す）。
> 048＝話者未解決の記録の `actor` は `__self__`。**`default` は視点ではない**ので想起は
> `__self__` へ寄せる（`viewpoint_of`）。1観測1候補への畳み込みも入れた。
> 次は段3＝042（`person_id` 撤去）。→ v0.66 で実装した。

> v0.64：**段1-に（046＝索引名の追随）を実装し、段1 が閉じた**（2026-09-02）。
> 043（`recall_count` 撤去）・044（索引を記憶にする）・045（視点項の撤去）・
> 046（索引名の追随）で、表と列の準備が済んだ。
> **次は段2＝047**（役割生成・既存行の backfill・1観測1候補の畳み込み）。そのあと
> 段3＝042（`person_id` 撤去）／段4＝拡散想起の付け替えと「共通の記憶」／
> 段5＝視点列3つの撤去。

> v0.63：**段1-は（045＝視点項の撤去）を実装した**（2026-09-02）。
> `persons.perspective_vec` と係数 `ALPHA`（0.30）と視点を育てる口（`lr=0.05`）を落とし、
> `_situated_vector(mem_vec, mu)` へ縮めた。ベクトルの差は人でなく**関係**が作る
> （047 の `relation_concept`）。視点シフト検索の絞り `s.person_id = ?` は残る。
> 残るは段1-に（046＝索引名の追随）で、段1 が閉じる。

> v0.62：**段1-ろ（044＝索引を記憶にする）を実装した**（2026-09-01）。
> `situated_embeddings` を `situated_memories` へ改名し、`content`・`last_recalled_at`・
> `groundedness_n` を面が持つようにした。`observations` からは2列が消え、取込の驚き
> `groundedness_g0` だけが残る（**パジュにとっての驚き**は出来事ごと、どの面を通って
> 思い出したかで変わる量は面ごと）。`_mark_recalled`（本番0件・`apply_verdicts` と二重）を
> 撤去した。1観測1候補への畳み込みは、役割を生成する 047（段2）で入れる。
> 残るは段1-は（045）・段1-に（046）。

> v0.61：**situated V2 の段を5つに割り、段1-い（043）を実装した**（2026-09-01）。
> 設計方針は「視点列（`writer_id`／`subject_id`／`participants_json`／`person_id`）を撤去し、
> person と MI の結びつきを situated だけが担う」。段は
> **1**（043・044・045・046＝表と列の準備）／**2**（047＝役割生成・backfill・1観測1候補の
> 畳み込み）／**3**（042＝`person_id` 撤去）／**4**（拡散想起の付け替えと「在席者2人以上と
> 関係行を持つ観測」の追加）／**5**（視点列3つの撤去・原本に無い新規）。
> 段1-い＝043 で `recall_count` を落とした（強化A の廃止・課題5 F 節）。
> 実測で分かったこと（`actor` が `writer_id` より正確・9種の役割・044 が空から始めた）は
> `復旧記録` v0.9 にある。

> v0.60：**`person_id` の列削除を記-d から situated V2（043〜047）へ移した**（2026-09-01）。
> 一度は記-d の一部として実装しコミットしたが、設計（`gap分析_移行設計` v0.6 §4・§6）が
> 「列削除は V2 で行う・既存データは所有者 person を situated へ写像する・`person_id` は
> その写像で presence/speaker のフォールバックに使う」と定めており、先に落とすと写像の材料が
> 無くなるため巻き戻した。所有者フィルタの撤去も同じ段で行う。**記-d は 039・040・041 の
> 3本で完了**とした。C-1 の申し送り（フォールバックは所有者絞りのまま残す）は、実測では
> 残す役目の母集合が 0 行だったが、撤去そのものが V2 の仕事なので判断は V2 へ送る。

> v0.59：**記-d の一覧に無かった 040（`memory_events.dedupe_key`）の扱いを記録した**
> （2026-08-31）。039・041・042 と同時に流れているが、記-d のどの版にも挙がっておらず、なぜ
> 落としたかの記録が無い。重複防止が2つあり（鍵と時間窓）、鍵が弾いていた場面は時間窓に収まる
> ため一本化した。日次要約の重複は原因が違うので別課題として残した。

> v0.58：**記-d の `scope` と `importance` を撤去した（039 の復元）**（2026-08-31）。撤去して
> 分かったことを3つ記した。`scope` には別物が3つあり完了条件の grep がそのままでは0件にならない
> こと、列を落とすときはテストの自前 SQL まで数えること（一度目の全体テストで15件が赤になり
> 11件がこれだった）、`importance` は「読むのは1箇所」だが日次減衰の書き手が生きていたことである。

> v0.57：**記-d の `unfinished_business` を撤去し（041 の復元）、環-c の取りこぼしを記録した**
> （2026-08-31）。表だけでなく `HeartbeatRuntime` ごと落ちた。書き手 `_persist_remainder` を呼ぶ
> `apply_status` が環-c で呼び出し側を失っており、本番から呼ばれるメソッドは全部で0件、
> `agent.py` は生成するだけだった。環-c の節へ「撤去し損ねたもの」として記録し、使用0件の判定は
> `getattr` 形だけでなく「生成だけされて呼ばれない器」も見落とす、と書き足した。

> v0.56：**記-d の実行時刻を日本時間で書き直した**（2026-08-30）。`schema_migrations.applied_at`
> は UTC で、05:54 は日本時間の 14時54分である。日付は変わらないが、ダンプのファイル名（JST）と
> 並べる場面で9時間ぶん逆の判断をしうるため、単位を明記した。

> v0.55：**実コードとダンプで記述を照合し、2件を訂正した**（2026-08-30）。環-e-い（OIF）は「完了」としていたが、口が建っただけで呼ぶ側を移していない。`OIF(` を生成する箇所が本番コードに0件で、記憶へは `ObservationMemory` を直接掴む5つの経路が生きている。段の目的である「1つの口へ」は未達で、付け替えをどの段に置くかは未決とした。記-d は「優先度は低い。そのうちやる」としていたが、**2026-08-03 に実行済み**である。開発機の故障でそのコードは失われ、ダンプの `schema_migrations`（039・041・042）だけが証言として残る。挙げた4つがそのまま4つとも落ちている。AIF は記述どおり生きていることを確認した（`tonic.py` が `self._aif.fire()` を通す）。

> v0.54：**環-e-ろ（AIF）の完了を記録し、現在地を 環-e-は（DIF）へ進めた**（2026-08-01）。AIF は `Firing` と `Nudge` の2つの口で、T と I が互いの中身を直接呼ぶ形を引き取った。

> v0.53：**環-e の段に AIF を足し、OIF の完了を記録した**（2026-08-01）。`設計図` ③-2 は口を4つと定めるのに段が3つしか扱っておらず、AIF が漏れていた。実装でも AIF は無く、T と I が互いの中身を直接呼んでいる（`tonic.py` → `push_affect`、`agent.py` → `nudge_current_mood`）。触る箇所が2つだけで最小なので OIF の次に置いた。環-e-い（OIF）は公開面8つ・MI 18属性で完了した。

> v0.52：**記-d へ `importance` と `unfinished_business` 表を足した**（2026-08-01）。`importance` は P-1 で役目を失った旧列で、読むのは未接続の MI 1箇所だけである。`unfinished_business` は開いた意図の旧い実装で、`heartbeat` が書く1箇所だけが呼び、本番コードに読み手が0件である（設計は開いた意図を O の記録として持つと定め、いまのループはそのとおり動いている）。`emotion` 文字列は残すと決めた。

> v0.51：**記-d（読み手の居ない列の撤去）を立てた**（2026-08-01）。実測で `observations.scope` は書くだけで誰も読んでいないこと、`person_id` は主想起が使わず旧経路 17 箇所だけが使っていることを確かめた。視点の残り3列（`writer_id`・`subject_id`・`participants_json`）は拡散想起のエンティティ辺が使うので落とさない。優先度は低く「そのうちやる」。

> v0.50：**環-e を段へ割り、環-f を分けた**（2026-08-01）。目指す形が `設計図` ③-2 として決まった。出入り口は **IIF・DIF・AIF・OIF** の4つだけで、外部は**身体・MCP-音楽・MCP-検索・主LLM**の4つへ束ねる。環-e は**挙動を変えない**範囲で、い＝OIF を作る、ろ＝DIF を作る、は＝生成器・動作器・想起を切り出す、の順に進める。挙動が変わる**待ち行列の統合（IIF）と主LLM の非同期化**は環-f へ分けた。軽量LLM と記憶は同期のまま残す（その反復の中で結論が要るため）。旧番号対応表にも両方を加えた。

> v0.49：**環-e（I の切り出し）を立てた**（2026-08-01）。設計は I を5つの実体に分け、資源ハンドラと機器 IF を外に置くが、実装では `loop/event_loop.py`（1,444 行）がループ管理・生成器・動作器・発話ゲート・O 書込を同居させ、カメラ・VLM・検索・記憶・軽量LLM・TTS を直接呼んでいる。挙動を変えず移動だけを行う課題として立て、旧番号対応表と着手順の表に加えた。**着手順の位置は未定**で、切り出しの単位が決まってから確定する。中身は `モジュール分割設計` v0.7 にある。

> v0.48：**着手順を組み替えた**（2026-08-01）。知-c（昼夜の見回りの実機確認）を先頭へ、知-d（顔と声の登録）をその次へ動かし、記-a（REST 内省）をそのあとへ置いた。REST は誰も居ないときにだけ回る機構なので人が居るあいだは実機で確かめられず、逆に 知-c と 知-d は人が居ないと進まない。実機が使える時間のほうが希少な資源なので、そちらを要する課題を前に置く。あわせて表の古い記述を3件直した：出-a は完了（TTS・STT ともローカル化・完了表へ移した）、調停の時間切れは解消済み（5.0 秒）、STT の利用枠も解消済み。溜まっている宿題に「`see` の意味づけの実機確認」を追加した。

> v0.47：**知覚スライス S1 の意味づけが成立していなかったこと**を §7 へ記録した。`extract_entities` が VLM 返答のコードフェンスでパースに失敗し、`see` の完了に何が写っていたかが載っていなかった。実機の llava:7b は同じ画像でも回ごとに包む・包まないが変わるため、症状が間欠的に見えていた。剥がす処理を `core/helpers.strip_code_fence` に一本化し、`capability_state.save_manifest` の同じ処理もそこへ寄せた。あわせて、ターン後の `SceneTracker.update` 呼び出しと `_react_to_scene_events` を撤去した（自分の発話テキストから拾ったラベルで在席欲求が動く経路であり、在/不在は S3 の YOLO が担う）。表の状態欄と S1 の説明に反映した。着手順は変えていない（記-a が現在地）。

> v0.46：**用語の分離（6概念）を反映**した。`activation`・`a`・`score` に相乗りしていた量を、日本語・英語・記号の頭文字をすべて分けた（根づき groundedness g／高ぶり arousal a／勢い dynamism d／地力 merit m／顕著性 salience s／適合度 fit f）。旧称「覚醒」「喚起」は高ぶりへ統一した。定義は `用語_略語一覧` にある。

> v0.45：**出-a の TTS を完了**（Style-Bert-VITS2・実機確認済み）。残る STT の見立てを書いた（`faster-whisper` の依存は既にある・常時集音の VAD はマイクの HVAD を踏まえて計測してから決める）。
> v0.44：**内省をツール駆動にすると決めた**（専用ループ・ツール5つ・LLM の呼び出しは 6〜10 回）。既存の `event_loop.py` に乗せないのは、発話を前提に組まれていて内省と噛み合わないためである。**調停の時間切れ（`arbiter_timeout_sec`）を自己調整の対象に加えた**（範囲 1.0〜10.0・値は LLM が判断し、集計は機械が渡す）。
> v0.43：**記-a-ろ（蒸留）の設計を決めた**（対象は前回の内省以降の O・`direction` と `activation_a0` の2段で絞る・フル LLM を1回で JSON・畳む対象は LLM が選ぶ・可逆性は保持のみ）。既存の日次要約は「終了時に作る」形だったので、起動を内省へ移し backfill だけ残すと決めた。**Config 自己調整の器を実装**（3段の優先順位・範囲の登録制・接続情報は拒否）。
> v0.42：**課題の番号を振り直した**。付けた順に増えるだけで種類も粒度もばらばらだった `#N` を、群を表す漢字1文字＋英小文字（`記-a`・`知-c`）へ改めた。数字を避けたのは設計図の「課題 N」と混ざるため、英数字（`A-1`）を避けたのは本書の「段階A」と衝突するためである。**着手順の番号（順N）は廃止**し、表の並びで示す（完了するたびに番号が意味を失うため）。§7 に「番号の付け方」と「旧番号との対応」を新設した。他資料に残る旧番号は当時の記録として書き換えない。
> v0.41：**#2（REST 内省）を4つに切り、順1（起動と骨格）を完了**した。§7 に #2 の節を新設。現在地は順7 の途中。実機で、誰も居ないときの REST 発火が内省パスへ入ることを確かめた。
> v0.40：**#12 を #12a（旧経路そのものの撤去）と #12b（廃止ストアの移管と撤去）へ割り、#12a を完了させた**。割ったのは、§3 の Phase 6 が移管を含む一方で §7 の依存表が「#14 のみ」としており、二つの記述が食い違っていたためである。§7 に #12a の結果（5段階・約 4,000 行）を新設し、Phase 6 の定義を実態へ合わせた。現在地を順7（#2 REST 内省）へ進めた。**判定の誤りを1件記録した**（`getattr` 形の見落としで `_concerns` を消し、復帰させた）。
> v0.39：**現在地を順6（#12 撤去）へ進めた**。順4（#13 深夜の時間帯倍率）と順5（小さな片付け）が完了し、順3（#15）は実機確認で欠陥が出て #17 待ちへ移った。#15 は**起点だけが動いて止まっている**（在席の立ち上がりで配られるが、配信ゲートが `PresenceSensor` を読まないため、配った直後の発話が塞がれて積み直される）。小さな片付けの表を結果へ書き換え、静的解析の赤（`ruff`・`mypy` 各16件）を緑にしたことと、死んだコード `consolidate()` の撤去を追記した。
> v0.38：**§7 の先頭に「次にやること」を新設**。表が事実と食い違ったまま（S4 完了・S5 実装済みが反映されず）で、それを見て順番を判断すると同じ取り違えが起きるため。**順9（#16 見回り）を順番から外して先に実装した**ことを明記し、溜まっている宿題（見回りの実機確認・調停の時間切れ 4.18 秒・STT の利用枠・周囲の会話を拾う件）を表にした。S4・S5 で入れたものと実測値も反映。
> v0.37：**S2・S3 の完了を反映**（定点・絶対 pan/tilt・YOLO の在/不在・動体イベント購読・在席センサの常駐・起動時の自律開始）。在席の情報源を二層（在/不在＝センサ、名前＝PMM）に分けたこと、この過程で直した4件、カメラ側の動体追尾が定点と競合することを記録。実機で確かめた事実の詳細は `設計詳細_活性_O書込_知覚在席` §3-6 へ。
> v0.36：**知覚のスライス（S1〜S5）を §7 に新設**。人物判定（誰か）を #17 へ残し、それ以外の知覚をすべて進める方針を反映した。**S1（見る・首を振るをループへ繋ぐ＝#14）を完了**。決定を記録：定点は ONVIF プリセットから始めて置きたい角度を Config で持つ、DINOv2 を採用する（新しい依存は不要）、$\widehat{S}$ の min-max 係数は実測で決める。あわせて `pyproject.toml` の `torch` が CPU 版の index を指したまま CUDA 版が入っている食い違いを記録した。
> v0.35：**§7 を着手順で組み直した**。旧 #8「在席と身元」を、顔と声の登録に依存するかどうかで **#8（在/不在の層）と #17（顔と声の登録）へ割った**。v0.32 で統合した判断を戻したことになる。統合の理由は「同じ二層を作る作業の表と裏」だったが、二層のうち**在/不在は identity を要さない**ので、登録を待たずに作れて実機で確かめられる。登録に応じられる人が揃わない状況では、統合したままだと半分も進められない。あわせて **#14（見る・首を振るをループへ繋ぐ）**、**#15（保留していた発話を配る起点）**、**#16（昼夜の見回り）** を新設し、13 項目の着手順と依存を表にした。#14 を先頭に置くのは、既定の反転（#11 段階5）で新しい系統から `see`／`look` が落ちており、いまカメラを使う経路が無いため。小さな片付けに**時間軸の式索引**を追加。重複していた小さな片付けの表と、#11 の「次にやること」の記述（段階5 まで完了済み）を整理した。
> v0.34：想起の一次絞りに**感情軸**が加わり、設計が定める軸がすべて揃ったことを反映。あわせて既存行の PAD 埋め（W1b）の実施を記録。
> v0.33：想起の軸名を**時間軸**へ改めたことを反映（`by_recency` → `by_time`）。
> v0.32：**旧 #7（顔登録 S）と旧 #8（在席系の精緻化）を #8「在席と身元」へ統合**。分けていた理由は依存（登録が済まないと精度を確かめられない）だったが、同じ二層を作る作業の表と裏で、別々に置くと片方だけ進めて確かめられない状態が続く。内訳を「土台（旧 #7）」「器（旧 #8）」「在席の判断が定まれば一緒に片付くもの」の3つに整理し、最後の節に退室の実機確認・保留を配る起点・一定時間で忘れる在席・会話経由の入退場を明記した。
> v0.31：§7 の残課題表に **#13 時間帯倍率**（静穏時間は出口を止めるだけで発火は夜も同じ頻度）を追加し、**小さな片付け**（`.gitignore` の worktree・`start-gui.sh` の扱い）の表を新設。
> v0.30：§7 の「掘り当てた不具合」に、想起の候補集合が1軸だけだった件・自分の答えだけ記録が遅れていた件・STT の聞き取り不能フィルタ・GUI の表示経路・静穏時間が人への返事まで止めていた件を追記。#8 の内訳に**起動の順序**と **`unconfirmed` のときの口調**を追加。
> v0.29：§7 に **#8（在席系の精緻化）の内訳**を新設。`_present_ctx` が在席と身元を1関数に混ぜている点（用語一覧の二層に対する乖離）、未知の在席者を表せない点、**人に合わせた発話の実機確認が未了である点**を明記した。あわせて入力の解釈（`/speaker`・`[名前]`・`/reload`・思考モード）をイベントループ分岐より前へ移し、両経路の共通の入口にした（分岐が先に return しており、イベントループ経路では `/speaker` が素の発話として LLM へ流れていた）。
> v0.28：**§7「残課題と順序（現在地）」を本文に新設**。#1〜#12 の一覧と順序は履歴（v0.27・v0.43）の文章の中にしか無く、本文を読んでも次に何をするか分からない状態だった（順序の正本として機能していなかった）。あわせて #1・#5・#10 の完了と #11 段階1・2 の完了、この過程で掘り当てた不具合（想起の取りこぼし・a0 の歪み・store の I/F・例外の扱い）を反映。次は #11 段階3。
> v0.27 改訂（順序の再構築＝感情ループ閉じ→拡散想起→致命エラー→DIF を前倒し）：Drive 起動源(1)・P1 知覚→save 視点列配線(2) を実装済み（加えて充足放電＝案Y・起動時キャッチアップ・動体検知＝案B も実装）。残りの順序を再構築した。**D の加工（P2 REST 内省・D-rel・D-mi）と知覚の精緻化（登録S・在席系・音声出力）と旧系統撤去(Phase 6)を後回し**にし、次を前へ持ってくる：**(a) 感情ループ閉じ（mood を実際に動かす・間接鎮静 M→D を効かせる・N_PAD/Nudge/発火接続・PAD 写像φ）→ (b) 拡散想起 WR（W 想起の再帰化）→ (c) 致命的エラー方針 → (d) DIF/I内部再設計（run() 制御を純イベント駆動3キュー AIF/DIF/完了 へ差し替え・反復抑止 [D-反復出力] を同梱）**。依存の事実：#5 拡散想起の前提は **Phase 2 recall（実装済み）** で REST 非依存＝いま着手可。DIF の実質ゲートは「**起動源(Drive・済)＋拡散想起(#5)でループの形が決まること**」＋方針（撤去される構造へ投資しない・本書 v0.24 原則4）であって、P2/D-rel/D-mi/撤去には hard 依存しない。よって #5 の後に DIF/loop 差し替えを置くのが正道で、後回しにした D・知覚は**でき上がった新ループ（イベント駆動 I・DIF 等）へ後段で差し込む**。D-mi は本体（畳み込み・蒸留）が REST 待ちだが、系統B の読み出し器（薄い縦切り）は REST 非依存で先行可（後回し集合の中の例外・必要なら随時）。
> v0.26 改訂（境界R 完了と、次の実装対象＝Drive 起動源を予定に落とす）：境界R（B1 core/mental_item・B2 core/helpers・B3/B3b core/parsing・brief_turn・B4 legacy/tape）を実装済み。io/ デバイスビルダー（B5）は `_init_tools` が構築フロー（9属性書き込み＋非デバイス配線）と絡み durable な境界が立ちにくいので見送り。テスト基盤は pytest-xdist（ワーカー別 DB）＋テスト DB の fsync=off で **約15分→約1分**に短縮。次の実装対象を検討で確定した＝**Drive 起動源（dynamics＝蓄積式・発火・気分変調 g_D(M)）を次にやる**。理由は依存が揃い validatable に進められるため（mood 器・drive 器・式と仮値＝発火mood §2.1-2.3 が確定・知覚/REST/D に非依存）。一方 **D の深い部分は前提が未スケジュールで詰まっている**：D-rel（situated V2 slice-3＝関係生成）は前提 **P1（知覚→本命 save の視点列配線・小〜中・いま可能）** が要り、D-mi（MI 集約＝系統A/B 蒸留）は前提 **P2（REST 内省＝蒸留の引き金・大・課題10・知覚/S 非依存でいつでも着手可）** が要る。よって順序を確定：**(1) Drive 起動源 →(2) P1（知覚→save）→(3) P2（REST 内省）→(4) D の深い部分（D-rel/D-mi）**。関連して、在席の「会話経由の入退場」（`PersonTool` を登録すれば LLM が `note_person_arrived`/`note_person_left` を呼べる・いま可能）と「一定時間で忘れる（MI 想起駆動の在席＝活性の派生ビュー）」は、後者が P1/D-rel（記憶が人に紐づく）依存なので Drive の後に置く。顔登録・声紋（S）は P1 の質を上げる後工程で D-rel の検証には必須でない（テストは PMM.person_arrived で複数在席を作れる）。Drive への入力は設計上「時間（基底 rate）＋Mood（g_D(M) が状況を集約）」で、内受容 I は drive を直接動かさず Mood 経由（発火mood §2.4-2.5）である。
> v0.25 改訂（致命的エラー時の動作方針を未検討項目として追加）：機能の前提が満たせない障害（DB 接続不可、埋め込み/評価器モデルの起動失敗、pgvector 次元不一致など）に対し、システムをどう振る舞わせるかの全体方針を検討する項目を立てる。検討する観点は、(1) 起動時失敗の扱い（落とすか degrade 起動か）、(2) 実行時の degrade と surface（どこまで動き続け・何を諦め・どう loud に残すか）、(3) ユーザーへの提示。現状は局所的 degrade のみ（棚卸し A1／A4 で例外を `logger.exception`／`logger.error` で loud 化し、`[]`／`False` を返してターンは落とさない）で、全体方針は未設計。**順序は個別設定・Config・個人登録（S）の後**（S が済んで運用の前提が揃ってから、障害時の振る舞いを設計する）。
> v0.24 改訂（順序方針のリファインメント）：Phase を直列に降りる前提を、リスクとサイズで組み直す。原則は4つ。(1) 小さく正しく先行＝挙動が大きく変わらない小機能を、正しく動く状態に先に固める。(2) 大きな挙動変化は後回し＝Drive 発火（起動源）・drive/mood の dynamics 接続・拡散想起・データモデルの深い作り替えは後ろへ。(3) リファクタリングを2つに割る＝境界R（core/store/loop/io/legacy のつなぎ目を引く・中身が変わっても残る）を先に、D（store 境界の中でのデータモデル整理）を挟み、内部R（各モジュールの中身整理）は D 確定後に置く。順序は 境界R → D → 内部R。(4) loop に触る作業（大物の機能 F と loop の R）はまとめて後回し＝loop の制御流れは起動源・拡散想起が入って初めて決まるため、形が決まらない所を先に切らない。顔登録・声紋登録・識別・設定値入力（S）は R と D の後（最後）に置く。当面 感情ループは受け身のまま（起動源を意図的に後回しにする代償として許容）。近い所の焦点は、小機能の正しさと、loop に触らない境界R（store/io/core のつなぎ目）。以下の Phase 記述はこの方針で読み替える。なお本文が「未実装/次」とする作業のうち、5軸スコアラ（r/t/e/a/p・p は slice-1/slice-2 とも実装済み）、min_score の合成床化、認識の InsightFace/ECAPA 載せ替えと GPU 実行、ToM 撤去、evaluator と store の切り出しは実装済み。
> v0.23：Phase 5 の項に、ToM のもう一方の働き（他者の心の想像）を「**W を消費する一人称 CoT**」に置き換える方針と、その **WR との相互再帰の切り分け**を追記。CoT は W の下流に置き対象を「W の文脈に出る人」で定義するので、想像⇄再帰想起の相互再帰は上流の WR が担い、CoT は先行実装しても WR 導入時に作り直しにならない。一人称崩れの止血として先行実装する（対象は当面 W の在席者＋想起 MI の人）。
> v0.22：**Phase 5 の WR 拡散想起を「W 想起の再帰化」として位置づけ、旧 ToM ツールの第二 W を吸収する決定を追記**。実機で、直接の対話相手（現話者）にまで ToM が発火し、応答が三人称の視点分析へ流れて一人称が崩れる挙動を確認した。原因は ToM の発火・出力設計にあり、記憶の汚染ではない。整理の結果、ToM が内部で独自クエリの想起を叩く部分は、本来 W 想起の再帰（拡散想起）が担うべきものだと判断した。Phase 5 の項に反映する。
> v0.21：Phase 2 の締めの分割単位を `モジュール分割設計 v0.1` として別文書に切り出し、本書からは参照する。目標構成は設計のコンポーネント名に合わせた `core/`／`store/`／`loop/`／`io/`／`legacy/`。
> v0.20：**Phase 2 の締めに「境界の切り出しと生存確認の不変条件」を新設**。`agent.py`（4,024行）と `memory.py`（2,593行）を一度に読める大きさへ分解する。2026-07-20 に見つけた3件の不具合（say ターンの永続化漏れ・感情語の飽和・時刻の9時間ずれ）の検討から、関数の長さより**端から端まで誰も確かめていなかったこと**が効いていると判断し、不変条件を先頭に置いた。ループ構造の作り替えは Phase 5 のまま前倒ししない（撤去される構造への投資を避ける）。`memory.py` の分解は v0.6 で Phase 2 へ申し送った宿題の履行、`agent.py` の副作用境界の切り出しは新設。
> v0.19：実機テストの起動コマンドを訂正。`./run.sh` は `--gui` を付けない CLI 起動で、`run_gui` が呼ばれず GUI のアイドルループが回らない（`main.py:459,500`）。普段の運用と同じ `./run-gui.sh`（`uv run familiar --gui`）に改めた。段取り自体は変えていない。
> 確定した新設計を現行コードへ落とす順序と依存を決める文書。**本書は段取り（フェーズ順序・依存・チェックポイント）の設計方針であり、各フェーズの TDD 手順・コードは承認後に別途出す。** 一項目ずつ確認しながら進める原則に従い、各フェーズ末に確認を挟む。
> v0.18：MI 集約段の設計確定（系統A・系統B・situated V2・自己認識 MI）を締めに反映。situated V2 の schema 器＝スライス1（relation_key 列・022）／スライス2（UNIQUE を relation_key 込みへ・023・upsert キー化）を Phase 1 分として実装（生成 presence のみ・挙動不変）。slice-3 以降（関係生成・person_id 削除・旧 `_remember` 撤去）は視点列を埋める知覚（[D-知覚]）依存で Phase 2 へ申し送り。集約全体の更新機構は REST 内省依存で Phase 2 寄り。
> v0.17：C-2（situated の役割整理）を実装済み（設計整理のみ・実行時の挙動不変）に更新。`situated_embeddings` の二役割（1=視点シフト検索・本人／2=在席者相関 p・他者・自分除外）を台帳へ固定し、現行で生きているのは役割1のみ・役割2＝p 軸は 5軸スコアラごと Phase 2、AGENT_SELF situated は自己の中立視点（役割1の自己スコープ）と明記。ソースコードは変更なし。節目③（在席者に応じた想起変化）は p 軸実装後へ保留。次のコードは MI 集約段。
> v0.16：C-1（person_id 所有者絞りの撤去）を実装済みに更新。situated 相関の読み出し層 `_read_observations_by_situated` を新設（第一段・未接続・テスト10件）し、`recall_day_summaries` をその層へ付け替えて所有者絞りを撤去（第二段・母集合が在席者相関へ変わる・戻り値の形は不変・既存 day_summary テスト4件を相関の意味論へ更新）。反証確認で、フォールバック二関数は主 situated 経路が0件のときだけ発火するため同じ situated 相関へ寄せると恒常的に空になると判明し、C-1 対象から外して別課題へ申し送った。次のコードは C-2（situated の役割整理）または MI 集約段。
> v0.15：B-3（PI 構築と PI→MI 拡張）を実装済みに更新。`tif.py` を新設＝`build_primitive`（M と D から PrimitiveMentalItem を構築）と `expand_to_mental`（PI→MI 拡張）の純関数。emotion に MoodPAD、drive に AiDrivers を流用し A-1 器は無変更。実際の発火とループには未接続で外部挙動不変。DB 不使用でマイグレーションなし。新規テスト3件。Nudge と N_PAD と発火接続は後続段。段階B の Phase 1 スライス（B-1／B-2／B-3）が揃い、次のコードは段階C の C-1。
> v0.14：B-2（drive（5欲求）レジスタ）を器のみ実装済みに更新。`drive_register.py` を新設＝5欲求（SEEKING／REST／BOND／SAFETY／ESTEEM）の器 AiDrivers（各軸 [0,1]・静止0.0）、agent_state（state_key drive5）への load_drives／save_drives。器と永続化のみで蓄積と放電と mood 変調（dynamics）は未実装。生きた15欲求 DesireSystem（agent_state["desires"]）にも as_coalition にも未接続で外部挙動不変。新規マイグレーションなし。新規テスト5件。dynamics と PI.drive surface と旧15→新5 移行は後続段。次のコードは B-3（PI 構築と MI 拡張）。
> v0.13：B-1（mood（PAD）レジスタの器）を実装済みに更新。`mood_register.py` を新設＝4軸 PAD の器 MoodPAD、各軸を M_rest=(0.5,0.5,0.5,0.5) へ半減期600秒で収束させる純関数 decay_to_rest、agent_state（state_key mood_pad）への load_mood／save_mood。emotion→PAD 写像 φ（課題11k・未確定）にも既存 mood にも未接続で外部挙動不変。新規マイグレーションなし（agent_state の新キー1つ）。新規テスト7件。次のコードは B-2（drive レジスタ）。
> v0.12：B-1 の平静を全軸0.5中立化へ修正（旧記載 (P=0,Pn=0,A=0,Dom=0.5) → M_rest=(0.5,0.5,0.5,0.5)・課題5 と発火/mood 別紙に整合）。あわせて B-1 を薄い縦切りに明記＝PAD レジスタの器と M_rest への指数収束（HL_M=600秒）と agent_state 永続化だけを先に作り、emotion→PAD 写像 φ（課題11k・写像値 未確定）の接続は後続とする。既存の単一ラベル mood と AffectiveState は撤去せず残す（外部挙動不変）。
> v0.11：A-3 の Phase 1 残務を決定と記述で確定（コード変更なし）。観測列 recall_count・last_recalled_at（017）を調査し、旧 recall の `_compute_final_score` と `_mark_recalled` の中だけで使われることを grep で確認。対応づけを確定＝recall_count は新しさ（t 軸）の若返り回数（半減期倍化）、last_recalled_at は若返りの起点リセットで、activation の n（評価由来）とは別系統・想起回数からは引き継がない。更新トリガは機械想起からフルLLM 参照申告へ移す。二列は旧 recall スコアリングに閉じるため Phase 1 は現状維持（外部挙動不変）とし、再編は 5軸スコアラの Phase 2 で行う。よって A-3 の Phase 1 残務は完了し、次のコードは段階B の B-1。詳細は活性の別紙 v0.2 §4。
> v0.10：A-3 の最初の一歩 A-3-1（活性の (a0,n) 保存形式）を実装済みに更新。マイグレーション 2026-07-02-021 で observations に activation_a0（REAL・既定1.0）と activation_n（INTEGER・既定0）を追加、importance を NULL 防御後に activation_a0 へ簡易移行（importance 列は残す）。memory.py に導出関数 _derive_activation（既定 step=0.33・値は課題5）を新設。導出値は想起スコアへ未接続で recall は従来どおり importance を読むため外部挙動は不変。新規テスト7件（純導出5・マイグレーション DB2）。recall_count・last_recalled_at の役割再編、想起スコアへの接続、n 増減の評価機構は A-3 の残り（Phase 2）へ申し送り。次の一歩は A-3 の残り。
> v0.9：A-2 を調査完了として記録。観測を読み出すコードが返していた属性（memory_id／summary／emotion／timestamp／date／time／direction／kind／source_kind／image_path／importance／superseded_by／検索メタ）を洗い、器（MentalItem）への対応表を作成。当初想定との食い違い（挙げた旧属性名が observations カラムに無い・date/time は 016 で削除済み・A-2 の対象は DB カラムでなく読み出しコードが返す属性）を記録。recall_count／last_recalled_at は A-3 へ、視点4列は段階 C へ申し送り。次の一歩は A-3（activation の (a0,n) 導出）。
> v0.8：A-1 を実装済みに更新（器 `PrimitiveMentalItem`／`MentalItem` と変換関数 `_row_to_mental_item` を導入・`recall_self_model` で器を組み立てる経路を通す・返り値は従来のまま・アクセス層無変更・新規3テスト＋既存115件・全体テスト通過）。次の一歩は A-2（MI 属性最小化の準備＝調査）。
> v0.7：Phase 1 節に詳細ステップ（薄い縦切り A-1〜D-2）を織り込み、**TUI 実機テスト（`./run.sh`）の必要タイミングを各ステップに明文化**（節目①〜⑤の一覧表つき）。読み出し層寄せ4本は段階 A-0（完了）として位置づけ。Phase 1 のことは本文書に一元化（別文書を作らない）。最初の一歩は A-1（observations を MI として読む・スキーマ変更なし）。各ステップは予定であり実装が進むごとに進捗を追記。
> v0.6：Phase 1 の読み出し層寄せを締め（4本目＝`recent_feelings`・層を `str | tuple` へ初拡張・後方互換・全テスト通過）。単純 kind 絞りの読み出しは層に集約。残る複雑な recall（別テーブル・日付演算・複合条件）は層に寄せず Phase 2 で「取り出しメソッド＋5軸スコアラ」へ分解する方針を確定（渡し方＝専用メソッドで構造化値・LIKE は situated 近傍へ）。データモデル本体は未着手。
> v0.5：Phase 1 の3本目を実装済みとして記録（`recall_day_summaries` を層経由へ・在席者スコープ `self._person_id` でも層無変更のまま機能・層の再利用性を3パターンで実証・追加テスト4件＋既存97件非回帰・全体テスト通過）。次は kind 単純絞りでない recall（semantic_facts・on_this_day 等）で層適用の可否を別途検討。
> v0.4：Phase 1 の次の一本を実装済みとして記録（`recall_self_model` を層経由へ・emotion 込み経路で層の再利用性を実証・層コード無変更・追加テスト4件＋既存97件非回帰・全体テスト通過）。次の一本は `recall_day_summaries` 等。データモデル本体は未着手。
> v0.3：Phase 1 の最初の一本を実装済みとして記録（読み出しアクセス層の実体化＝`_read_observations_by_kind` 新設・`recall_curiosities` を層経由へ・スキーマ変更なし・外部挙動不変・全テスト pass）。次の一本は emotion 込みの `recall_self_model` の層寄せ。データモデル本体は未着手。
> v0.2：未決4点を確定（Phase 1 から着手・薄い縦切り・スコアはフラグ排他切替・min_score は Phase 2 後）。Phase 0 は実機完了。
