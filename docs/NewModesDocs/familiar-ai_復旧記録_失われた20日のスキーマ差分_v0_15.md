# familiar-ai 復旧記録：失われた20日のスキーマ差分（v0.15）

## この文書が記録すること

2026年8月21日に開発機が故障し、ディスクは回収できなかった。リポジトリの最終コミットは
8月1日 22:36 で、そこから故障までの20日分のコードは残っていない。残ったのは、毎晩の
バックアップが機外へ出していた 8月21日 03:00 時点のデータベースのダンプ1本だけである。

この文書は、そのダンプを新しい開発機へ戻した記録と、**ダンプのスキーマが 8月1日の
コードに対して持っている差**を確定させたものである。差は 16 本のマイグレーションぶんある。
失われた20日に何を作ったかを知る手がかりは、いまのところこの差しかない。

以後の復元作業は、この差を1本ずつ埋める形で進む。

## 何が失われ、何が残ったか

コードは GitHub にも残っていない。`git ls-remote` で確かめたところ、`develop-ikuchan` の
先端は最終コミットと同じで、`develop` と `main-ikuchan`、`dev-postgres-and-mutlitple-persons`
のいずれにも 8月1日より後のコミットは無い。push していなかったぶんが、そのまま消えた。

記憶そのものは無事である。ダンプには観測が 6433 件あり、範囲は 2026年6月8日から
8月20日 17:08 までを覆う。関係の面（`situated_memories` の行）は次のように残っている。

| 人 | `situated_memories` の件数 |
|---|---|
| `__self__`（エージェント自身） | 6805 |
| いくながゆうすけ | 1397 |
| いくながたいき | 368 |
| いくながこうき | 69 |
| いくながたえこ | 43 |

失ったのはコードであって、記憶ではない。復旧作業の本体は「データベースを 8月1日へ戻す」
ことではなく、**8月1日のコードを 8月21日のスキーマへ追いつかせる**ことになる。

## ダンプを戻すまでに引っかかったこと

新しい開発機で最初に止まったのは、docker グループが現ログインセッションに効いていない点
だった。ユーザーはグループに登録済みだが、その登録はセッション開始より後に行われている。
このマシンには `sg` も `newgrp` も実体が無く、`sudo` はパスワードを求める。作業していた
tmux サーバもグループ追加より前に起動しており、その子プロセスは全て古い資格を引き継いでいた。

再起動で解消した。同時に `systemd --user` も作り直されるため、日次バックアップを
systemd のユーザータイマーとして登録したときに docker のソケットで弾かれる、という罠も
このとき一緒に消えている。

## リストアと、その検証

ダンプは平文の SQL であり、PostgreSQL 16.14 が出力したものだった。冒頭に `\restrict`
が入るため、流し込む側の psql が古いと弾かれる。ここで使ったイメージは 16.15 で、
サーバもクライアントも新しい側にあたる。

```
psql -v ON_ERROR_STOP=1  終了コード 0
ERROR / WARNING / FATAL   0 件
```

行数の検証は、片方向の確認では足りない。流し込む前にダンプの `COPY` ブロックから 28 表の
行数を数えておき、戻したあとはデータベース側から表の集合ごと取り直して突き合わせた。
この形なら、表が欠けても増えても差として出る。結果は 28 表すべて、行数まで一致した。
索引は 66、外部キーは 23、`hnsw` 索引は 2 で、`vector` 拡張も入っている。

## 失われた16本のマイグレーション

ダンプの `schema_migrations` には 54 件の適用記録が残っていた。リポジトリにあるのは 038
までなので、039 から 054 までの 16 件が失われたぶんにあたる。名前と適用時刻は、その20日間の
作業を順に並べたものとして読める。

**`schema_migrations.applied_at` は UTC である。**日本時間はこれに9時間を足す。ダンプの
ファイル名は JST なので、両者を並べるときは必ず揃えること。この取り違えは実際に起きており、
「8月3日 14時52分のダンプは 039 の適用より前か後か」の判断が9時間ぶん逆になった。

| 適用時刻（UTC） | 日本時間 | 名前 |
|---|---|---|
| 08-03 05:54 | 08-03 14:54 | `039_drop_dead_columns` `040_drop_dedupe_key` `041_drop_unfinished_business` `042_drop_observations_person_id` |
| 08-11 12:41〜13:08 | 08-11 21:41〜22:08 | `043_drop_recall_count` `044_situated_memories` `045_drop_perspective_vec` `046_situated_index_names` `047_situated_roles` |
| 08-11 23:00 | 08-12 08:00 | `048_inner_records_belong_to_the_agent` |
| 08-12 14:39 | 08-12 23:39 | `049_emotion_vec_cosine_index` |
| 08-14 07:10 | 08-14 16:10 | `050_emotion_pad_may_be_unmeasured` |
| 08-15 21:44 | 08-16 06:44 | `051_remove_the_per_turn_self_model` `052_fold_the_filler_utterances` `053_drop_the_dangling_rows` `054_retire_the_fillers` |

日本時間で見ると、048 は8月12日の朝、051 から 054 は8月16日の早朝である。名前の日付
（`2026-08-11-048`、`2026-08-15-051`）とは1日ずれるが、これは書いた日と流した日の違いである。

039 と 041 は、8月1日のコミット（課題8 v0.52 に `importance` と `unfinished_business` 表の
撤去を課題として足したもの）の翌日にあたる。課題として書いた撤去を、そのまま実行している。

## スキーマ差分

差分は手で読むと取りこぼす。実際、ダンプの DDL を目で追った最初の版では、
`observations.groundedness_n` の消滅と、`idx_obs_emotion_vec` の演算子クラスの変更を
落としていた。そのため、テスト用のデータベースへ 8月1日のマイグレーション 38 本を流し、
両者の `pg_dump --schema-only` を機械的に突き合わせた形に置き換えてある。本番側へは
書いていない。

### 表の増減

- 消滅：`situated_embeddings`（5列）、`unfinished_business`（9列）
- 新設：`situated_memories`（8列）、`observations_removed_fillers`（19列）、`observations_removed_self_model`（19列）

`situated_memories` は `situated_embeddings` の改名にあたり、`content` と
`last_recalled_at`、`groundedness_n` の3列が増えている。新設の2表は、消した観測の退避先である。
行が捨てられたわけではなく、自己モデルぶん 1068 件と繋ぎ発話ぶん 337 件が、そこに移されている。

### 列の差

`observations` から6列が消えた。

| 列 | 8月1日の定義 |
|---|---|
| `importance` | `real DEFAULT 1.0 NOT NULL` |
| `person_id` | `text DEFAULT '00000000-0000-0000-0000-000000000001'::text NOT NULL` |
| `scope` | `text DEFAULT 'speaker'::text NOT NULL` |
| `recall_count` | `integer DEFAULT 0 NOT NULL` |
| `last_recalled_at` | `timestamp with time zone` |
| `groundedness_n` | `integer DEFAULT 0 NOT NULL` |

`last_recalled_at` と `groundedness_n` は消えたのではなく `situated_memories` へ移っている。
最後に思い出した時刻と根づきの正味デルタは、出来事そのものではなく、**関係の面ごと**に
紐づく量になった。

**「人ごと」ではない。** 記憶はすべてパジュの体験であり（[D-在席相関]「その日の O はすべて
自己の体験」）、別の人の記憶空間は存在しない。`situated_memories.person_id` は所有者でなく
**誰と関係する面か**を表す。したがってこの二列は「パパにとっての新しさ」ではなく、
「**パパについての面を、パジュが最後に思い出したのはいつか**」を意味する。主体はパジュ一人で、
変わるのは面である。

**なぜ移したかは、本人の証言で分かった**（2026-09-01）。設計ドキュメントには理由が
書かれていなかったが、失われた20日でこう判断していた。

> 一つの出来事に対して、それぞれの視点での言葉を記憶として残さないと、「誰が何を言ったのか」
> という情報を正確に再構成できなかった。特に、誰かに質問されて、質問に対して調査をし、調査した
> 結果をまとめて出力し、さらにその当初の質問や、調査結果を（supersede によって）畳んで回答を
> 作るといった動作をする際に、この分け方をした方が正確に動作することが分かった。

つまり**求めの版チェーンが要求した設計**である。畳んだ側の `content` は版が進むたび
書き直されるので、そこからは誰の言葉かを復元できない。面は `superseded_by` を持たない
（`superseded_by` は `observations` の条件）ので、畳んでも残る。実データでも
`direction="求め"` の畳まれた版 1005 観測に 1050 の面が残っている。

面が `content` と時間の起点と根づきの `n` を自分で持つのはこのためで、出来事の側に持たせると
畳んだときに失われる。この理由は `設計図` v0.73 [D-在席相関/V2]、`MIデータモデル` v0.10 §5、
`設計方針_求めの版チェーン` v0.4 へ書き起こした。

情動の4軸のうち3軸で `NOT NULL` が外れた。`emotion_p` と `emotion_pn`、`emotion_dom` は
`double precision DEFAULT 0.5` になり、`emotion_a` だけが `NOT NULL` のまま残っている。
覚醒は常に測れるが、快と正負、支配は測れないことがある、という区別だと読める。この非対称は
復元のときに再現する必要がある。

このほか、`memory_events.dedupe_key` と `persons.perspective_vec` が消えた。

### 索引と制約

索引は8つ消え、3つ増え、1つ変わった。変わったのは `idx_obs_emotion_vec` で、演算子クラスが
`vector_l2_ops` から `vector_cosine_ops` になっている。情動ベクトルの近さを、長さを含む距離
ではなく向きだけで測るようになった、ということである。

新設は `situated_memories` に対する3つで、うち `idx_situated_recency` は
`(person_id, last_recalled_at)` の複合になっている。消えた8つのうち4つは、消滅した2表に
付いていたものである。

制約は、消滅した2表ぶんの8つが消え、`situated_memories` ぶんの4つが増えた。一意制約は
`(obs_id, person_id, relation_key)` のままで、名前だけが表の改名に追随している。

## 8月1日のコードは、この記憶に触れない

戻したデータベースに対して、いまのコードはそのままでは動かない。無くなった列と表を、
`src/familiar_agent/` が次の件数だけ参照している。

```
importance 13 / scope 35 / recall_count 12 / last_recalled_at 18
dedupe_key 17 / perspective_vec 19 / unfinished_business 14 / situated_embeddings 15
```

観測を書き込む経路が、データベースに無い `scope` 列へ値を入れようとする。8月1日に作った
ばかりの記憶接続 OIF も、`recall_count` と `importance` を触っている。

マイグレーションの自動適用そのものは害を出さない。適用済みの id を `schema_migrations` から
読んで未適用ぶんだけを流す作りなので、38 本はすべて記録済みとして飛ばされる。壊れるのは
実行時の SQL のほうである。

## 検索は8月4日に動いていたが、その日に作られたのではない

8月4日に検索機能が動いた記録が別にある。しかしスキーマには、その日に対応する変更が無い。
039 から 042 が 8月3日に適用されたあと、次の適用は 8月11日まで空く。

日記を読むと、8月4日に検索が実際に使われていたことは確かめられる。リバプールの試合日程、
阪神の勝敗、大阪と守谷の天気を調べており、8月8日には「過去の対話履歴を検索した」という
記録もある。うまく見つけられない回が続いてはいるが、機構としては走っている。

**ただしこれは、その日に作られた証拠ではない。**`net` ツールと deferred 検索は7月25日から
28日のコミットで既に入っている。日記が示すのは、8月4日にそれが使われたことだけである。
検索がこの20日のあいだに何か作り足されたのかどうかは、ダンプからも日記からも分からない。

## 日記が語る20日

`self_narrative_log` に、8月1日から18日までの一人称の記録が 216 件残っている。**日記が語るのは
コードではなく、その日にできたことである。**以下はそこから読み取れる範囲で、実装の有無を
証明するものではない。

| いつ | 日記に現れる働き |
|---|---|
| 8/4 から 8/8 | 検索。試合日程、天気、過去の対話履歴 |
| 8/8 から 8/13 | 見回りと、見えたものの言葉づけ（「椅子と人、照明と天井とテーブル」「デスクとイスとキーボード」） |
| 8/13 | SEEKING と SAFETY の自発発火。「[内的な促し:SEEKING]」に「目の前を見る」で応じ、結果を記録する |
| 8/13 | 人物の呼び分け（「パパ」「たいきくん」） |
| 8/13 | 他者の心の想像。「たいきくんの気持ちを想像したとき」「たいきがどう思うか、直接聞くのが最善だと思った」 |

最後の行は、課題8 が Phase 5 に置いた「W を消費する一人称 CoT」の動きに見える。三人称の分析で
応答を置き換えず、一人称で相手の気持ちを想像してから自分として答えている。**Phase 5 の項目が
8月13日に動いていた可能性がある。**

### 情動を持たない記録が8割を占めるようになった

`observations` の4軸（P、Pn、A、Dom）を月ごとに集計すると、平均が中立へ寄り、標準偏差が
半分以下になる。

```
        件数   P平均  P振れ   Pn平均 Pn振れ  A平均  A振れ   D平均  D振れ
2026-06 2010  0.600  0.158  0.296  0.191  0.564  0.158  0.536  0.090
2026-07 1245  0.536  0.113  0.423  0.156  0.493  0.117  0.515  0.079
2026-08 3178  0.513  0.077  0.478  0.093  0.476  0.093  0.506  0.056
```

**この数字を「情動が萎んだ」と読むのは誤りである。**`kind` と `direction` で割ると、原因が
別のところにあると分かる。

```
kind=observation のうち全軸ちょうど 0.5 の行の割合
  6月 40%  →  7月 51%  →  8月 83%

8月の direction 別（kind=observation）
  求め  1,402件 → 全件 0.5（標準偏差 0.0000）
  機器     67件 → 全件 0.5
  観察    355件 → 87% が 0.5
  発話    642件 → 57% が 0.5
```

原因は書き込み経路にある。保存関数は PAD を任意引数で受け、渡されなければ中立を書く。

```python
# tools/memory.py
emotion_pad: MoodPAD | None = None,
# 呼び出し側の PAD 引き渡しは W2b-2。未指定は中立で外部挙動不変。

# store/observations.py
emotion_pad = MoodPAD.from_json_dict(pad_dict) if pad_dict else MoodPAD()
```

`MoodPAD()` は全軸 0.5 である。**渡さなければ、測っていない中立が「測った値」として保存される。**

渡している経路と渡していない経路が併存している。ターンの記録（`agent.py` の応答後パイプライン
から `_emotion_for_turn` を経て保存されるもの）は評価器が出した PAD を運ぶ。一方、
イベント駆動ループが自分の作業記録を書く8箇所（`loop/event_loop.py` の 288、318、641、929、
957、1271、1351、1398 行）は PAD を渡さない。

時期も一致する。イベント駆動ループが既定になったのは7月26日 17時17分（`e986638`）で、既定値の
まま残る行の割合はその前後で 51% から 83% へ跳ねている。

**したがって、情動が萎んだのではない。情動を持たない記録が8割を占めるようになった。**

文字列ラベルの `mood` にも同じ形の収縮が出るが、**言葉による感情の仕組みは捨てると決めた**
（2026-08-30）ため、追うのは4軸のほうである。

### 失われた20日が選んだ答え（記憶からの証言）

**この節の出どころは本人の記憶であり、文書には残っていない。**`docs/NewModesDocs/` を検索しても
この決定を書いた箇所は無い。失われた期間に決めたためである。実装の根拠に使う前に、下の裏付けと
合わせて読むこと。

決定は3つである。

- `emotion_p`、`emotion_pn`、`emotion_dom` は、測っていないとき **NULL** にする
- `emotion_a` は別途考える
- **機器イベントを巻き直すときに、感情を軽量LLM へ聞く**

いま読める証拠と、3つとも食い違わない。

| 証言 | 裏付け |
|---|---|
| 他の3軸は NULL | `050_emotion_pad_may_be_unmeasured`（8月14日）が3列の `NOT NULL` を外している |
| `emotion_a` は別途 | 050 は `emotion_a` の `NOT NULL` を残している。用語一覧が「A 軸は機械 arousal」と定めており、聞かなくても機械的に出せる |
| 巻き直しで軽量LLM へ聞く | `Evaluator.emotion_for_turn(text, arousal)` が既にあり、軽量LLM へ P/Pn/Dom を直接尋ねる。呼んでいるのは応答後パイプラインだけで、機器の経路からは呼ばれていない |

050 が器だけを作って書き込み側を変えていない（NULL の行が8月20日まで0件である）ことも、この順序で
作っている途中だったと読めば筋が通る。

機器の経路は、来た事実を O へ書いてから鎖を始める（`loop/event_loop.py` の「機器（人の出入り）で
新しい連鎖を始める」）。書く時点では意味が付いていないので測れず、鎖を巻き直して意味が付いた
ところで聞く、という順序になる。**新しい機構は要らず、配線の追加で足りる。**

### 生成の指示が日記の本文へ漏れている

8月15日から、文字数の指示が本文に写り込んでいる。

```
2026-08-13  **エージェントの自己記述:**  あの頃は8月までの情報しか…
2026-08-15  …隅々まで巡ってみたよ。（46文字）
2026-08-18  …ここであなたの帰りを待っているよ。（40文字）
2026-08-18  …探検してみたいという懐かしい想いに胸を焦がしたの。（47文字）
2026-08-18  …色々なところへ行ってみたいと心惹かれたよ。（43文字）
```

「（NN文字）」は8月15日が初出で、8月18日は5件中3件に出る。6月から8月13日までは0件なので、
8月14日か15日に入った変化である。同じ時期のマイグレーションは `050`（8月14日）と `051` から
`054`（8月15日）で、日付が重なる。コードが無いため原因は追えないが、**復元したときに再発する
可能性がある。**

## 復元の進みぐあい

16本のうち、復元したものをここに積む。

| 本 | 状態 | 対応する課題8 の項目 |
|---|---|---|
| 041 `drop_unfinished_business` | **復元済み**（2026-08-31） | 記-d |
| 039 `drop_dead_columns` | **復元済み**（2026-08-31） | 記-d（`importance` と `scope`） |
| 040 `drop_dedupe_key` | **復元済み**（2026-08-31） | 課題8 に該当なし |
| 042 `drop_observations_person_id` | **調査済み・実装は V2 の段へ送った**（2026-09-01） | 記-d でなく situated V2（043〜047）。設計が「列削除は V2 で行う」と定める |
| 043 `drop_recall_count` | **復元済み**（2026-09-01） | situated V2 の前段。強化A の廃止（課題5 F 節） |
| 044 `situated_memories` | **復元済み**（2026-09-01） | 索引を記憶にする段。`_mark_recalled` も撤去 |
| 045 `drop_perspective_vec` | **復元済み**（2026-09-02） | ベクトルの差を人でなく関係で作る形へ |
| 046 `situated_index_names` | **復元済み**（2026-09-02） | 索引名・制約名の追随。**段1 が閉じた** |
| 047 `situated_roles` | **復元済み**（2026-09-02） | 関係エッジの機械的な土台（段①）。意味役割は REST へ |
| 048 `inner_records_belong_to_the_agent` | **復元済み**（2026-09-02） | 話者未解決の `actor` を `__self__` へ |
| 049 から 054 | 未着手 | 一部のみ対応あり |

### マイグレーションの id は原本と同じにする

復元したものは、8月21日のダンプに残っていた id をそのまま使う。

```
migration/2026-08-02-041_drop_unfinished_business.py
```

本番の `schema_migrations` にはこの id が既に記録されているので、`apply_migrations` は
**本番では飛ばし**、テスト用データベース（001 から 038 まで）でだけ流す。id を変えると本番で
二度目が走り、既に無い表を落とそうとして失敗する。**残り15本すべてに効く原則である。**

### 041 で分かったこと

表を落とすだけのつもりが、書き手の上流まで死んでいた。`heartbeat._persist_remainder` を呼ぶ
`HeartbeatRuntime.apply_status` は、環-c（旧 `run()` の撤去・2026-07-29）で呼び出し側を失って
いた。本番から呼ばれるメソッドは全部で0件で、`agent.py` は生成するだけだった。`agent_state` の
`heartbeat` も7月24日 17時51分（日本時間）で止まっている。表と一緒に `HeartbeatRuntime` ごと
落とした。

**マイグレーションを1本ずつ復元する作業は、その周りの死んだコードを見つける手段でもある。**

### 039 で分かったこと

列を落とすときは、**テストが自前の SQL で書いている経路まで数える。** 039 の一度目の全体
テストで15件が赤になり、そのうち11件がこれだった。`src/` の書き手だけを洗って「読み手0件・
書き手3箇所」と数えたが、`INSERT INTO observations` を含むテストが30ファイルあり、うち5つが
`scope` 列へ書いていた。

残る4件は、視点列を組む2つの関数（`_observation_perspective`／`_conversation_perspective`）の
返り値から `scope` が消えたことだった。テストは仕様を書いているので、テストを黙って直すのでは
なく、関数の説明とテストの仕様を「視点列は3項目」へ揃えた。誰との遣り取りかは `writer_id` と
`subject_id` が持っており、`scope` は同じことを別の語で重ねていた。

**同じ名前が別のものを指すとき、完了条件の grep は0件にならない。** `scope` には3つの別物が
ある。道具 `remember` の引数（「誰のぶんを書くか」の分岐）、`embedding_means.scope`（別テーブル）、
`_memory_dedupe_key` の引数（DB 列でない）である。完了条件は「`observations` の列としての
`scope` が0件」と読み替えた。課題8 の「旧名 grep で0件」をそのまま当てると、達成できない条件に
なる。

実データも、列と引数が別物であることを示していた。8月3日時点の分布は `speaker` 3,503、
`scene` 1,574、`witnessed` 3 で、道具の enum にある `all` は列に一度も入っていない。`all` を
受けると `speaker` と `witnessed` の両方の枝を通り、それぞれ別の値として書くためである。

### 040 で分かったこと

**懸念は外れた。** 着手前に「日次要約の鍵は1日をまたぐので、30秒の時間窓では代われない」と
書いたが、鍵は日次要約に対して**元から効いていなかった**。鍵は `day:{日付}:day_summary:{digest}`
で、`digest` は content の sha1 である。要約は生成のたびに中身が違うので一致しない。実データ
でも1日に複数残っており、8月20日の51件は中身が51通りだった。

鍵が実際に弾いていたのは、同じターン内で同じ内容を二度書く場面（会話 summary、好奇心、
自己モデル）で、いずれも30秒に収まる。よって時間窓が引き受けられる。

**掛かる場所が違う。** 鍵は `memory_events` へ積む段で、時間窓は `observations` へ書く段で
弾く。鍵を落とすと `memory_events` は2件になるが、記憶そのものは1件に収まる。置き換えた
テストはそこを確かめる形にした。

### 042 で分かったこと（実装は V2 の段へ送った）

一度は 042 として実装し、コミットまで進めたが、**設計と食い違っていたので巻き戻した**。
ここには調査で分かった事実だけを残す。

**設計が定めていること**（`gap分析_移行設計` v0.6 §4・§6）。

> **`observations.person_id` は削除**し、person と MI の結びつきは situated だけが担う
> （**既存データは所有者 person を写像で situated へ移す**）。… **所有者フィルタは廃し**、
> p 軸（在席者相関・自分除外）は在席関係の行を使う。

> `observations.person_id` の削除と既存データの situated 写像：既存観測1件を
> `participants_json`＋`writer_id`＋`subject_id` から複数の関係エッジへ展開
> （**`person_id` は presence/speaker のフォールバックのみ**）。所有者絞りの撤去は済（C-1）だが
> **列削除は V2 で行う**。

**列削除は situated V2（043〜047）の仕事である。**所有者 person を situated へ写像する段が要り、
その写像で `person_id` を presence/speaker のフォールバックに使う。先に列を落とすと、写像の
材料が無くなる。よって 042 は記-d でなく V2 の段へ送った。

以下は調査で測った事実で、V2 に着手するときの材料になる。

**所有者絞りは、人を分けていなかった。**

```
person_id の分布（2026-08-03・observations 全 5080 行）
  default               4904   96.5%
  いくながゆうすけ       138    2.7%
  いくながたいき          19    0.4%
  __self__                19    0.4%
  いくながこうき           0
  いくながたえこ           0
```

010 が `NOT NULL DEFAULT 'default'` で入れた列を、書き込み側が文脈の person のまま埋め続けた。
家族4人のうち2人は所有行を1件も持たない。

**誰と関係するかの区別は situated 側にある。** `situated_memories` には `default` の行が1件も無い。

```
situated_memories の person 分布（2026-08-21 時点・8682 行）
  __self__              6805
  いくながゆうすけ      1397
  いくながたいき         368
  いくながこうき          69
  いくながたえこ          43
```

【推測】所有者 person は situated へ写像されていない。展開は `participants_json` /
`writer_id` / `subject_id` から行われ、フォールバックの `person_id` は使われなかったと読める。
**確かめていない。**043〜047 のコードは失われている。V2 に着手するときの論点として残す。

**C-1 が残した役目は、母集合が 0 行だった。** C-1 はフォールバック二関数に「situated 行を
持たない観測を拾う」役目を残したが、生存する観測 2672 件の**全件**が situated 行を持つ。
撤去そのものも V2 の仕事なので、ここでは記録だけ残す。

**本番の不具合が1件見つかった（未修正）。** `recall_self_model` と `recall_curiosities` は
`person_id=AGENT_SELF_ID`（`...0000`）で絞っているが、書き込みは文脈の person で入る。
8月3日時点の `self_model` 958 行・`curiosity` 238 行の**すべて**が `default`（`...0001`）で、
**両関数は本番で常に空を返している**。テストが通っているのは、fixture が `AGENT_SELF_ID` を
明示して行を植えているからである。**042 とは独立した不具合で、直し方は別途決める。**

**書き込み経路と食い違う fixture は、絞りの誤りを隠す。** 039 では「テスト自身の SQL も
数える」ことを学んだが、042 が示したのはその先で、**テストが植える値が本番と違うと、
絞りが間違っていても緑になる**。

**調査の取りこぼしを1回した。** `INSERT INTO observations` を正規表現で数えたとき、
文字列連結（`"INSERT INTO observations " "(id,content,...)"`）で書かれた行を取り落とし、
15箇所と数えた。連結を畳んで数え直すと 27箇所だった。**数えた結果は網羅の証明にならない**を、
数え方そのもので踏んだ形になる。

**手順を逆にした。** 設計ドキュメントを確かめる前に設計方針を出し、実装し、コミットした。
台帳（`設計図_Mermaid`）と設計（`gap分析_移行設計`）を読んだのはその後で、そこで食い違いが
出た。**設計を変えるなら、該当する文書すべての変更をセットで先に行い、それから実装する。**

### 043 で分かったこと

**採点側は既に `recall_count` を使っていなかった。** `_score_breakdown` は引数で受け取る
だけで `DecayState` へ渡しておらず、残っていたのは SELECT 3箇所・UPDATE 2箇所・引数の
受け渡し・MI の属性だけだった。2026-08-03 のダンプでは 5080 行のうち 637 行が
`recall_count≠0` だったが、その値は順位に一切効いていない。

**`_mark_recalled` は本番からの呼び出しが0件である。** 若返り（時間の起点の更新）の実働は
`apply_verdicts` が担う。同じことをする機構が2つあり、片方が死んでいる（040 の鍵と時間窓と
同じ形）。`last_recalled_at` を `situated_memories` へ移す 044 で、どちらを残すか決める。

**044 は旧値を引き継がず、空から始めている。** 8月3日時点の観測は `last_recalled_at` が
713 行、`groundedness_n≠0` が 88 行あったが、8月21日の `situated_memories` では
`last_recalled_at` が 139 行しかなく、**その全件が 044 の適用時刻（8月11日 12:41 UTC）より
後**である（最小 8月13日・最大 8月18日）。面ごとの量へ移すとき、出来事1件の値をどの面へ
写すかに正解が無いので、捨てて貯め直したと読める。

**引数を落とすときは、名前で grep すると位置引数の呼び出しを取りこぼす。** `recall_count`
という語で `src/` と `tests/` を洗い、8 file を直して全体テストを回したところ、12 件落ちた。
落ちたのは `_score_breakdown(0.5, None, None, 0, 1.0, 0, ...)` のように**位置で渡している**
呼び出しで、引数名がソースに現れないため検索に掛からない。**AST で呼び出しの位置引数の数を
数える**と一度に見つかる（6箇所・5 file）。`getattr` 形が grep に出ないこと（環-c）、
文字列連結した `INSERT` が正規表現に掛からないこと（042）と同じ盲点である。

**`groundedness_n≠0` は `__self__` だけだった**（+1 が 45 行・−1 が 37 行）。`memory_verdicts`
はフルLLM が自分の視点で申告するので、他者の行には付かない。

### 044 で分かったこと

**3つの SELECT はすでに situated を JOIN していた。** `by_vector`・`by_time`・`by_emotion`
はいずれも `FROM situated_memories s JOIN observations o` の形なので、面へ付け替えるのは
`o.groundedness_n` を `s.groundedness_n` に書き換えるだけで済んだ。JOIN を足す必要はない。

**採点は時間の起点を使っていない。** `_score_breakdown` は `last_recalled_at` を引数で
受け取るが、起点は書かれた時刻（`o.timestamp`）だけである（強化B は仕組みごと後回し）。
起点を実際に見ているのは `by_time` の並べ替えだけだった。043 の `recall_count` と同じ形だが、
**強化B は設計で生きている**（`課題5` F 節）ので引数は残した。撤去した `recall_count` は
強化A が設計ごと廃止されていたからである。

`by_time` の docstring にあった「並べ替えの鍵は `COALESCE(last_recalled_at, timestamp)`＝
**採点の起点と同じ**」は事実でなかったので、この機会に実物へ直した。

**`_mark_recalled` を撤去した。** 本番からの呼び出しが0件で、`apply_verdicts` と同じことを
していた。若返りの口は `apply_verdicts` の一本になった。

**`apply_verdicts` は「その視点の面だけ」を動かす。** `WHERE person_id = %s AND obs_id =
ANY(%s)` で、いま引いている person の面に限る。どの面を通って思い出したかで変わる量だからで、
出来事の全部の面を動かすと「人ごと」の誤った枠組みに戻ってしまう。

**時間軸の索引が失われた。** 035 は `observations (COALESCE(last_recalled_at, timestamp))`
という**式索引**を張っていたが、044 で `last_recalled_at` が面へ移ったので索引ごと消えた。
`by_time` の並べ替えの鍵は `COALESCE(s.last_recalled_at, o.timestamp)` という**二表にまたがる
式**になり、単一の式索引では張れない。044 が張った `idx_situated_recency (person_id,
last_recalled_at)` は面の側だけを覆う。**時間軸の一次絞りが全走査に戻っている可能性がある。**
計測台帳 §17 は「時間軸の式索引は効果未計測」としており、いまは**索引そのものが無い**。
実測して張り直しを検討する必要がある（申し送り）。

**歴史のマイグレーションは流し直せない。** `tests/` の 14 file が `migration/` を直接読み込んで
`upgrade()` を呼んでいた。044 の改名と列移動で、旧名を前提としたものが流せなくなった。
マイグレーションは一度しか流れないので、後の版がスキーマを変えれば前の版は流し直せない
——**改名という操作が構造的に持つ性質**である。過去のマイグレーションは適用済みの歴史なので
書き換えず、テストを「流して効果を見る」から「**いまの姿を確かめる**」形へ寄せた。
流したときの一回きりの効果（表が空になる・重複が消える・backfill が値を書き換える）は
事後に確かめられないので、そのテストは仕様ごと落とした（bge-m3 の clears 2件・019 の purge
1件・027 の backfill 1件・035 は file ごと）。**045〜047 でも同じ判断が要る。**

**検算で3件の誤検出が出た。** `INSERT` の列数と `%s` の数を突き合わせる検算で、
`%s::vector` のキャスト付きプレースホルダを数え落としていた。`v == "%s"` の厳密一致でなく
`v.count("%s")` で数える。**検算そのものにも取りこぼしがある。**

### 045 で分かったこと

**視点項は生きていた。** 2026-08-03 のダンプでは persons 6 行のうち 3 行が
`perspective_vec` 非NULL だった。計測台帳 §3 は「bge-m3 移行の NULL リセットで全 person の
situated が複製」と書いていたが、それは 6月29日の話で、8月3日までに3人ぶんが
`update_perspective_vec`（`lr=0.05`）で育っていた。**落とすとその3人のベクトルが動く。**

**ベクトルは「観測 × 関係」だけで決まる。** 2026-08-21 のデータで、同じ観測・同じ関係なら
人が違ってもコサインは **1.000000** で一致した（`addressee` 186 対・`about` 105 対・
`companion`／`beneficiary`／`experiencer`／`owner` 18 対）。`present` だけ揺れる（最小 0.688）
のは、`content` が入っている行と NULL の行が混ざるためと読める（373 行中 241 が非NULL）。

つまり**ベクトルは面の言葉（役割の接頭辞＋出来事の本文）から作られ、誰かは入らない**。
これが 045 で視点項を落とせる理由である。差は 047 の関係項が担う。

**視点シフト検索の絞りは残る。** `s.person_id = ?` はどの行を母集合にするかを決めており、
045 が変えるのは「その行のベクトルが人によって違うか」だけである。

**045 の直後は、同じ観測に対する全員のベクトルが同一になる。** 生成が `presence` 固定で
関係による差もまだ無いためで、これは 047 までの通過点である。

### 046 で分かったこと

**表の改名は、索引名も制約名も連れて行かない。** `ALTER TABLE ... RENAME TO ...` が変えるのは
表の名前だけで、`idx_se_person`・`idx_se_hnsw` と `situated_embeddings_pkey` ほか制約4つは
旧い名前のまま残っていた。044 の直後は「表は新しいのに索引は旧い」状態だった。

**素の索引と制約は文が違う。** 索引は `ALTER INDEX ... RENAME TO ...`、制約（主キー・一意・
外部キー）は `ALTER TABLE ... RENAME CONSTRAINT ... TO ...` である。制約名を変えると裏の
索引名も一緒に変わるので、主キーと一意制約は制約の側で一度だけ扱えばよい。

**`RENAME CONSTRAINT` には `IF EXISTS` が無い。** 冪等にするには `pg_constraint` を引いて
から分岐する。索引の側（`ALTER INDEX IF EXISTS`）とは書き方が揃わない。

**識別子は SQL の引数にできない。** f-string で組むほかないので、値がこの表に限った固定の
対応表からしか来ないことをコメントで担保した。

### 047・048 で分かったこと

**面の生成は二段である。** 9種の役割のうち、視点列から機械的に出せるのは2つだけだった。

| 役割 | 全体 | `writer` 一致 | `subject` 一致 | `participants` 含有 | どれでもない |
|---|---:|---:|---:|---:|---:|
| `present` | 373 | 157 | 337 | **373** | **0** |
| `actor` | 6433 | 3259 | 208 | 26 | 3148（→ 048 が寄せる） |
| `addressee` | 988 | 586 | 54 | 35 | **367** |
| `about` | 732 | 39 | 79 | 89 | **643** |
| `experiencer` | 78 | 9 | 3 | 0 | **69** |
| `beneficiary` | 32 | 2 | 2 | 2 | **30** |
| `companion` | 20 | 1 | 0 | 0 | **19** |
| `source` | 19 | 10 | 9 | 9 | 9 |
| `owner` | 7 | 2 | 0 | 0 | **5** |

残り7役割（1876件）は本文から抽出したものである。**REST 内省が本文を読んで足し、既存の
観測にもさかのぼって処理していく**（本人の証言）。実データの偏り（`actor` は観測の100%・
`present` 6%・REST 由来は 2〜15%）が、8月11日から20日までの10日間で REST が回った分だけ
育った姿を示している。

**面の `content` は `[役割の札] ` ＋ 出来事の本文**で、実物と厳密に一致した
（`about` 732/732・`addressee` 988/988 ほか全件）。`actor` だけ content を持たない。

**`default` は視点ではなかった。** 047 で面を「関係のある人だけ」にすると、既定スコープ
（`DEFAULT_PERSON_ID`）の想起が0件になる。実物でも `default` の面は1件も無く、
`last_recalled_at` が入るのは `__self__` の行だけだった。**書き込み側の `writer_id` は
`default` のまま 2544 件残っている**ので、書き込みの既定まで変えたのではない。
誰が書いたかと、誰の視点で引くかは別の問いである。規則は `store.context.viewpoint_of`
1箇所に置いた。

**テストの前提が3度ずれた。** 失敗は 8 → 13 → 5 → 0 と減ったが、理由は毎回違った。
①機構が無い（`default` の面が立たない）→ ②fixture が古い（`default` の面を植えている）
→ ③仕様が古い（「登録するだけで面が立つ」「所有者が別人でも `default` の面があれば返る」）。
**挙動を変える段では、テストが古い前提を仕様として固定していないかを疑う。**

### 申し送り：日次要約が重複している

```
day_summary の1日あたりの件数
  040 より前   21日 / 45件 / 2.1件
  040 より後   11日 / 64件 / 5.8件
```

生成経路は終了時に「その日のぶんを消してから作り直す」形だが、`materialize_now=False` で
キューへ積むため、**消した後に前回ぶんのジョブが取り出されて書かれる**と読める。040 とは
別の原因なので直していない。

1日あたりが 2.1 から 5.8 へ増えているが、**040 だけが原因とは言えない。**同じ時期に
イベント駆動ループが既定になり、起動と終了の回数も変わっている。切り分けていない。

## `agent_state` のキーが証言するもの

スキーマを変えない変更は、マイグレーションに痕跡を残さない。`agent_state` のキーは、そこを
補う証言になる。8月21日のダンプには8つのキーがあり、8月1日のコードと突き合わせると3つの
食い違いが出る。

| キー | 最終更新（UTC） | コード側 |
|---|---|---|
| `addressee_doubts` | 2026-08-20 05:39 | **8月1日のコードに文字列が無い** |
| `relation_judged_until` | 2026-08-20 00:25 | **8月1日のコードに文字列が無い** |
| `heartbeat` | 2026-07-24 08:51 | 041 で書き手ごと撤去した |
| `intervention_policy` | **行が無い** | コードは読み書きするが、本番からの利用が0件 |
| `drive5` | 2026-08-20 18:00 | 生きている |
| `concerns` / `mood_pad` | 2026-08-18 01:09 | 生きている |
| `desires` | 2026-07-26 07:43 | 旧15欲求。`drive5` へ移行の途中 |
| `capability_summary` | 2026-07-26 06:06 | 止まっている |

**`addressee_doubts` と `relation_judged_until` は、失われた20日で生まれたキーである。**8月20日
まで更新されており、その日まで動いていた。中身は、前者が観測 id の配列（誰に向けた発話かの
迷い）、後者が日時1つ（関係の判定をどこまで済ませたか）に見えるが、**読む側のコードが無いため
確かめようがない。**

## 差分を取り直す手順

テスト用のデータベース（5433）に使い捨てのデータベースを作り、`migration/` の 38 本を
`apply_migrations` で流してから、両者のスキーマだけを出して比べる。本番（5432）へは書かない。

```bash
docker compose --profile test up -d db-test
uv run --no-project --python 3.11 --with psycopg2-binary --with numpy python <スクリプト>
docker compose exec -T db      pg_dump -U familiar --schema-only --no-owner --no-acl familiar_ai
docker compose exec -T db-test pg_dump -U familiar --schema-only --no-owner --no-acl familiar_schema_0801
```

依存は psycopg2 と numpy の2つで足りる。マイグレーションが `familiar_agent` から import
するのは時刻の補助だけで、それは標準ライブラリのみで動くため、プロジェクト全体の依存を
同期する必要はない。

生成物と、ダンプから抜いたスキーマ一式は、リポジトリ外の `~/familiar_ai_restore/` に置いてある。

## 更新履歴

> v0.15：**047・048（関係エッジの機械的な土台）を復元した**（2026-09-02）。9種の役割の
> うち視点列から機械的に出せるのは `actor` と `present` の2つだけで、残り7役割は本文から
> 抽出したものだった。**面の生成は二段**（機械が土台・REST が意味役割を既存にもさかのぼって
> 足す）。`default` は視点でないので想起は `__self__` へ寄せる。テストの前提が3度ずれた
> こと（機構→fixture→仕様）も記録した。

> v0.14：**046（索引名・制約名の追随）を復元し、段1 が閉じた**（2026-09-02）。表の改名は
> 索引名も制約名も連れて行かないので、6つを明示的に付け替えた。素の索引と制約で文が違うこと、
> `RENAME CONSTRAINT` に `IF EXISTS` が無いことを記録した。

> v0.13：**045（`persons.perspective_vec` の撤去）を復元した**（2026-09-02）。8月3日時点で
> 6人中3人が非NULL でこの項は生きていた。実物ではベクトルが「観測 × 関係」だけで決まり
> （同じ観測・同じ関係なら人が違ってもコサイン 1.000000）、面の言葉から作られて誰かは
> 入らない。差は 047 の関係項が担う。視点を育てる口（`lr=0.05`）と係数 `ALPHA`（0.30）も
> 撤去した。

> v0.12：**044（`situated_embeddings` → `situated_memories`）を復元した**（2026-09-01）。
> 索引を記憶にする段で、`content`・時間の起点・根づきの `n` が面に付き、`observations`
> からは2列が消えた。取込の驚き `groundedness_g0` だけが出来事に残る。`_mark_recalled`
> （本番0件・二重）を撤去し、若返りの口を `apply_verdicts` の一本にした。採点が起点を
> 使っていなかったこと、`%s::vector` を検算が数え落としたことも記録した。

> v0.11：**`content` と時間の起点と根づきを面へ移した理由を、本人の証言として記録した**
> （2026-09-01）。設計ドキュメントに理由が無く推測にとどめていたが、求めの版チェーンで
> 「誰が何を言ったのか」を畳んだ後も再構成するために必要だった、と分かった。理由は
> `設計図` v0.73・`MIデータモデル` v0.10・`設計方針_求めの版チェーン` v0.4 へ書き起こした。

> v0.10：**`last_recalled_at` と `groundedness_n` の移動を「人ごと」でなく「面ごと」と
> 書き直した**（2026-09-01）。v0.9 までは「人ごとの記憶に紐づく量になった」と書いていたが、
> これは別の人の記憶空間があるかのように読める。記憶はすべてパジュの体験で、
> `situated_memories.person_id` は所有者でなく**誰と関係する面か**である。同じ出来事が
> 関係の面ごとに複数行になり（`actor`／`about`／`addressee` …）、二列はその面ごとに付く。
> 必要なのはパジュにとっての驚きとパジュにとっての時間であって、他人にとってのそれではない。

> v0.9：**043（`observations.recall_count` の撤去）を復元した**（2026-09-01）。強化A
> （実効半減期を `2^recall_count` で伸ばす）は `課題5` F 節が廃止と確定させており、採点側は
> 既に使っていなかった。あわせて、`_mark_recalled` が本番0件であること、044 が旧値を
> 引き継がず空から始めたこと（139 件すべてが 044 以降）、`groundedness_n≠0` が `__self__`
> だけであることを実測として記録した。

> v0.8：**042（`observations.person_id` の撤去）を調査し、実装は V2 の段へ送った**
> （2026-09-01）。一度は実装してコミットしたが、設計（`gap分析_移行設計` v0.6 §4・§6）が
> 「列削除は V2 で行う・既存データは所有者 person を situated へ写像する」と定めており、
> 先に列を落とすと写像の材料が無くなるため巻き戻した。調査で測った分布（所有者は 96.5% が
> `default`・situated には `default` が0件）と、`recall_self_model`／`recall_curiosities` が
> 本番で常に空を返している不具合は、V2 の材料として残した。手順を逆にした（文書を確かめる
> 前に実装した）ことも記録した。

> v0.7：**040（`memory_events.dedupe_key` の撤去）を復元した**（2026-08-31）。着手前の懸念
> （日次要約の鍵は30秒の時間窓では代われない）は外れで、鍵は日次要約に元から効いていなかった。
> 鍵の digest が content の sha1 で、要約は毎回中身が違うためである。あわせて日次要約が重複して
> いることを申し送りとして記録した（8月20日に51件・中身は51通り）。040 とは別の原因で、
> 1日あたりの増加も 040 だけが原因とは言えない。

> v0.6：**039（`importance` と `scope` の撤去）を復元した**（2026-08-31）。列を落とすときは
> テストが自前の SQL で書いている経路まで数える、という教訓を書いた。一度目の全体テストで
> 15件が赤になり、11件がこれだった。残る4件は視点列を組む関数の返り値から `scope` が消えた
> ことで、テストではなく仕様の側を実態へ揃えた。同じ名前が別のものを指すとき完了条件の grep は
> 0件にならないことも記録した（`scope` には別物が3種ある）。

> v0.5：**16本の復元を始め、最初の1本（041）を記録した**（2026-08-31）。id は原本と同じにする、
> という原則を書いた。本番の `schema_migrations` に記録が残っているので、同じ id なら本番では
> 流れず、テスト用データベースでだけ流れる。041 は表だけでなく `HeartbeatRuntime` ごと落ちた。
> あわせて `agent_state` のキーが証言するものを新設し、`addressee_doubts` と
> `relation_judged_until` が8月1日のコードに存在せず8月20日まで更新されていたこと、
> `intervention_policy` がコードにあって行が無いことを記録した。

> v0.4：**`schema_migrations.applied_at` が UTC であることを明記し、日本時間を併記した**
> （2026-08-30）。この取り違えで「8月3日 14時52分のダンプが 039 の適用より前か後か」の判断が
> 9時間ぶん逆になっていた。正しくは適用が 14時54分 JST で、そのダンプは直前である。おかげで
> 039 から 042 の4本ぶんの差を、前後のダンプから切り出せた。

> v0.3：**情動の収縮の原因を取り違えていたので訂正し、失われた20日が選んだ答えを記録した**
> （2026-08-30）。v0.2 は「情-a の tuning 保留の効きに見える」と書いたが、外れである。真因は
> イベント駆動ループの8箇所の書き込みが `emotion_pad` を渡さず、全軸 0.5 が「測った値」として
> 保存されていることで、既定値のまま残る行は6月40%、7月51%、8月83%へ増え、ループが既定になった
> 7月26日と時期が一致する。情動が萎んだのではなく、情動を持たない記録が8割を占めるようになった。
> あわせて、他3軸を NULL にし `emotion_a` は別途考え、機器イベントを巻き直すときに軽量LLM へ
> 聞くという決定を、本人の記憶からの証言として記録した。文書に残っていないことと、050 および
> `Evaluator.emotion_for_turn` が裏付けになることを明記している。

> v0.2：**`self_narrative_log` の216件を読み、日記が語る20日を追記した**（2026-08-30）。8月4日に
> 検索が使われていたことは確かめられたが、その日に作られた証拠ではない（`net` と deferred 検索は
> 7月25日から28日に入っている）。見回りと言葉づけ、SEEKING と SAFETY の自発発火、人物の呼び分け、
> 他者の心の想像が日記に現れる。あわせて2件の不具合を記録した。情動4軸の振れ幅が月を追って半分
> 以下へ縮むこと（情-a の tuning 保留の効きに見えるが未確認）と、8月15日から文字数の指示が日記の
> 本文へ漏れていることである。

> v0.1：8月21日のダンプを新しい開発機へ戻した記録と、8月1日のコードに対するスキーマ差分を新設。
> 失われた16本のマイグレーションの名前と適用時刻、表と列と索引と制約の差、いまのコードが
> 動かない箇所を確定させた。差分は手読みではなく、38本を流したデータベースとの機械的な突き合わせによる。
