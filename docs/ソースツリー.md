# familiar-ai ソースツリー

`src/familiar_agent/` のモジュール構成。**この一覧は実物から生成している**——各 module の
docstring の1行目を説明に使うので、file が増減すれば作り直すだけでよい（説明そのものは
コードの側にある）。最新はリポジトリを直接見る。

（2026-09-10 生成）

```text
src/familiar_agent/
├── _i18n.py                   # Locale detection and string translations for familiar-ai.
├── _ui_helpers.py             # Shared UI utilities for TUI, GUI, and REPL.
├── agent.py                   # Core agent loop - ReAct pattern with real-world tools.
├── bootstrap.py               # Startup/bootstrap helpers for familiar-ai.
├── camera_discovery.py        # Network camera discovery helpers and CLI entrypoint.
├── capability_state.py        # Capability manifest loader and AI self-understanding storage.
├── coalition.py               # ワークスペースへの候補（Coalition）の器
├── concern_engine.py          # Lightweight active concerns for autobiographical self continuity
├── config.py                  # Configuration management.
├── config_overrides.py        # Config の自己調整の器（記-a）
├── db.py                      # PostgreSQL connection management.
├── db_migrations.py           # PostgreSQL migration runner.
├── desires.py                 # Desire system - autonomous motivations for the embodied agent.
├── diagnostics.py             # GUI diagnostics and connection-test helpers.
├── diffuse_store.py           # 拡散想起 (B) エンティティ辺の DB 読み（dumb・LLM フリー）
├── drive_register.py          # New-5 drive register (SEEKING, REST, BOND, SAFETY, ESTEEM).
├── emotion_pad.py             # PAD↔感情ラベルの正本と PAD→ラベル逆引き（書き込み PAD 化 W2a）
├── errors.py                  # 致命的な起動エラー（#10）。DB・埋め込み等、これ無しでは動けない依存の失敗を表す
├── event_bus.py               # Event Bus — canonical event representation and JSONL logging.
├── exploration.py             # ExplorationTracker — ICL-driven novelty-aware exploration.
├── gui.py                     # PySide6 GUI for familiar-ai — Soft Daylight theme.
├── intervention_policy.py     # Intervention Policy — neighbor-core Layer D.
├── main.py                    # CLI REPL for familiar-ai.
├── mcp_client.py              # MCP (Model Context Protocol) client manager.
├── memory_worker.py           # Background worker for durable memory operations.
├── mood_register.py           # PAD (Pleasure/Pain, Arousal, Dominance) mood register.
├── person_memory_manager.py   # Person-aware memory routing layer.
├── poses.py                   # 定点：見回り・在席マップ・norm が共有する N 個の向き
├── prediction.py              # Prediction Error Engine — Friston-inspired free-energy signal.
├── presence_map.py            # 定点別 presence マップ：在/不在の層
├── presence_sensor.py         # 在/不在を測る器：撮って、どの定点かを決めて、人が居るかを数える
├── realtime_stt_session.py    # Realtime STT session manager — shared between REPL and TUI.
├── reflect.py                 # Self-reflection loop — Kokone reads her own session log and rewr
├── relationship.py            # Relationship tracker — persistent companion relationship metadat
├── routines.py                # Routine and schedule helpers for quiet hours and continuation fl
├── scene.py                   # World model — persistent scene entity tracking and change detect
├── self_narrative.py          # Self-narrative — persistent first-person diary of Kokone's sessi
├── settings_schema.py         # Single source of truth for familiar-ai setup/settings fields.
├── setup.py                   # Setup and first-run configuration helpers for familiar-ai.
├── silence_state.py           # 「黙っていて」と頼まれた状態
├── sqlite_migrations.py       # Simple timestamped SQLite migration runner.
├── tif.py                     # PI construction and PI->MI expansion (Phase 1 B-3, construction 
├── time_decay.py              # Pure time-decay value object. No DB, no env, no settings — calcu
├── tui.py                     # Textual TUI for familiar-ai.
├── visual_norm.py             # 定点ごとの「見えの普通」と、そこからの隔たり
├── voice_guard.py             # Voice-loop guard for TTS -> realtime STT echo suppression.
├── backends/
│   ├── anthropic.py               # AnthropicBackend（出-d-は）。Anthropic の公式 SDK を使う
│   ├── cli.py                     # CLIBackend（出-d-は）。コマンド行の呼び出しを使う
│   ├── factory.py                 # どのバックエンドを使うかを決める（出-d-は）
│   ├── gemini.py                  # GeminiBackend（出-d-は）。Gemini を使う
│   ├── glm.py                     # GLMBackend（出-d-は）。GLM（智譜）を使う
│   ├── kimi.py                    # KimiBackend（出-d-は）。Kimi（Moonshot）を使う
│   ├── openai_compat.py           # OpenAICompatibleBackend（出-d-は）。OpenAI 互換の API を使う
│   ├── shared.py                  # どのバックエンドも使う下請け（出-d-は）
│   └── types.py                   # バックエンドがやりとりする形（出-d-は）
├── core/
│   ├── context_parts.py           # 文脈を組む口（出-e）。モデルへ**何を与えるか**の形を揃える
│   ├── diffuse.py                 # 拡散想起 (B) エンティティ辺の純ロジック（LLM フリー・機械的）
│   ├── drive_autonomy.py          # Drive 発火 → 自発ターンの選択・ゲート（[D-行動選択]・純関数・Slice 2b）
│   ├── drive_dynamics.py          # Drive 起動源の dynamics（蓄積・気分変調・発火・放電）＝純関数
│   ├── drive_satisfaction.py      # 充足放電ルート（案Y・純関数）：ターン完了時の軽量LLM充足判定で drive を全放電
│   ├── helpers.py                 # agent.py から切り出した module レベルの純関数（loop 非依存・境界R B2）
│   ├── llm_protocol.py            # バックエンドが守る約束（出-d-は）
│   ├── mental_item.py             # MI（心的アイテム）の器と、観測行から器を組み立てる変換
│   ├── model_resource.py          # モデル資源（MR）。重みを持つものが従う型枠（出-c）
│   ├── parsing.py                 # ME.md / FAMILY.md / 話者接頭辞の純粋パーサ（loop 非依存・境界R B3）
│   └── structured_ask.py          # 軽量LLM へ「形のある答え」を求める口
├── io/
│   ├── aif.py                     # 自律機構接続 AIF：T と I をつなぐ唯一の口（`設計図` ③-2）
│   ├── dif.py                     # 外部機器接続 DIF：I と外の機械をつなぐ唯一の口（`設計図` ③-2）
│   └── oif.py                     # 記憶接続 OIF：記憶ストア O との唯一の出入り口（`設計方針_OIF` v0.1）
├── legacy/
│   ├── semantic_layer.py          # 撤去予定の意味・信念層と明示リンク（Phase 6 で撤去）
│   └── tape.py                    # TAPE-inspired planning for familiar-ai (arxiv:2602.19633).
├── loop/
│   ├── arbiter.py                 # 調停器（ARB）：パジュが次に何をするかを自分で決め、反復の出し方を3つへ振り分ける
│   ├── coherence.py               # 整合チェックへ渡す事実を組む（出-f）
│   ├── evaluator.py               # 評価器 — 軽量LLM（utility backend）を使うターン評価の集合（W2b-2）
│   ├── event_loop.py              # イベント駆動ループ（#11 段階1）：I（情報処理機構）の最小縦切り
│   ├── generator.py               # 生成器 GEN：反復ごとに、主LLM へ渡す材料を組む（環-e-に）
│   ├── history.py                 # 会話履歴スライスの読み取り専用フラット化
│   ├── prompt.py                  # イベント駆動ループ（#11 段階1）の system プロンプト（案B・クリーン最小）
│   ├── request.py                 # 求め RQ：1つの求めが始まってから閉じるまでの状態（環-e-に・に-5-に）
│   ├── rest.py                    # REST 内省の1パス（#2・順1＝骨格）
│   ├── tonic.py                   # T（自律機構 Tonic）の常駐タスク：時計を持つ唯一の側
│   └── workspace.py               # 作業記憶 W：反復ごとに組み、反復ごとに捨てる（環-e-に・に-5-に-2）
├── recognition/
│   ├── embedding_store.py         # 人ごとの認識埋め込みの保存と、cosine 最大＋しきい値の純判定
│   ├── face.py                    # Face-based person recognition (InsightFace / ArcFace).
│   ├── motion_events.py           # カメラ側の動体イベントを購読する（ONVIF PullPoint）
│   ├── person_detector.py         # 人が居るかの判定（YOLO）。在/不在の層で、誰かは問わない
│   ├── presence_watcher.py        # Background camera poller for passive presence detection.
│   ├── visual_encoder.py          # 見えの埋め込み（DINOv2）。定点ごとの「普通」を作るための視覚エンコーダ
│   └── voice.py                   # Voice-based speaker identification (ECAPA-TDNN / speechbrain).
├── store/
│   ├── clock.py                   # 時刻の生成を一箇所に集める
│   ├── context.py                 # 層が共有する文脈
│   ├── db_compat.py               # PostgreSQL と sqlite3 の接続の差を吸収する薄いラッパ
│   ├── embedding.py               # 埋め込みモデルと、ベクトルの符号化・正規化・次元合わせ
│   ├── jobs.py                    # 非同期の書き込みキュー（`memory_events` と `memory_jobs`）
│   ├── observations.py            # O（観測）の読み出し層
│   ├── persons.py                 # 人物レジストリ（`persons`）
│   ├── pose_norms.py              # 定点ごとの「見えの普通」の保存層（`pose_norms`）
│   ├── relations.py               # MI 間の関係（`relations` と `relation_members`）
│   └── situated.py                # 視点ベクトルと situated 行の作成・保存
└── tools/
    ├── camera.py                  # Camera tool - the eyes and neck of the embodied agent.
    ├── coding.py                  # Coding tools — Read/Edit/Glob/Grep/Bash for agent-driven develop
    ├── deferred_fetch.py          # Deferred fetch tool: fire-and-forget URL fetch, result injected 
    ├── deferred_search.py         # Deferred search tool: fire-and-forget search, result injected on
    ├── local_stt.py               # 常時集音をローカルで行う（出-a）
    ├── memory.py                  # Observation and emotional memory for the embodied agent.
    ├── mic.py                     # Microphone capture — streams PCM 16 kHz 16-bit mono to an async 
    ├── mobility.py                # Mobility tool - legs of the embodied agent (robot vacuum).
    ├── pending_speech_store.py    # pending_speech ストア
    ├── person.py                  # Person identification and presence management tools.
    ├── realtime_stt.py            # Realtime Speech-to-Text using ElevenLabs WebSocket API (Scribe v
    ├── stt.py                     # Speech-to-Text tool using ElevenLabs Scribe API.
    └── tts.py                     # TTS tool - voice of the embodied agent.
```

`locales/` は翻訳の資源（`.json`）で、module ではないので載せていない。
