# familiar-ai 開発ガイド（CLAUDE.md）

## この文書の位置づけ

- この file は毎セッション読み込まれる。**常に効く判断規則だけ**を置く。
  手順の詳細は `.claude/rules/`、設計の正本は `docs/NewModesDocs/` にある。
- 読み手はコーディングエージェントである。人間向けの解説文書ではない。
- 目的は、familiar-ai への変更を、**人間の承認を挟みながら、実物の事実に基づき、
  再現可能な手順で**進めることである。速さは目的ではない。

## プロジェクト概要

familiar-ai は、家庭で家族が使う、身体を持つ伴侶エージェントである。複数の家族が
一つのエージェントを共有し、各人が独立した記憶空間、関係状態、視点ベクトル
（`perspective_vec`）を持つ。話者は顔認識または声紋で識別し、記憶の読み書きを
自動でその人の文脈へ振り分ける。

構成は次のとおり。

- ReAct ツールループ
- PostgreSQL 記憶（pgvector）と人ごとの `situated_embeddings`
- 予測とワークスペースの層
- 関係、評価、社会方針、欲求調整の明示的な状態層
- capability manifest と定期的な自己理解の更新
- カメラ、移動、TTS、STT、GUI、MCP の任意統合

バックエンドは特定実装に依存しない。Anthropic を含むが、唯一の実行経路ではない。

## ソースツリー

モジュール構成は `docs/ソースツリー.md` にある（最新はリポジトリの `src/familiar_agent/` を直接見る）。

## 実行時アーキテクチャ

`agent.py` の現在のターンの流れは次のとおり。

1. ユーザー入力とツール・シーン文脈の取り込み
2. 内受容の収集
3. 予測状態の読み出し
4. 記憶、作業記憶、開いているエピソードの活性化
5. 暫定の関係証拠の更新
6. 感情の評価
7. 社会方針の選択
8. 欲求の調整
9. ワークスペース競合の実行
10. ReAct ループの実行
11. 応答のメタゲート
12. ターン後のトレースとメンタル状態スナップショットの永続化

このターン駆動の流れとは別に、GUI のアイドルループ（`gui.py` の
`_process_queue()`）が、`should_deliver_deferred_result()` が真を返したとき、
別の「遅延配信ターン」を発火する。このターンは完了した `search_deferred` /
`fetch_deferred` の結果を、通常のユーザー入力の合図を介さず先んじて配信する。
在席、静穏時間、社会文脈でゲートするが、検索がユーザー起点で、かつユーザーが
直近30分以内に活動していれば静穏時間は迂回する。

アイドルの DMN サイクル（ワークスペースの勝者なし）では、`capabilities.yaml`
から自己理解を定期的に更新する。

以前のプロンプトのみの社会ロジックは、もはや全体像ではない。決定論的な状態層が、
生入力と応答計画のあいだに入っている。

## 永続化

すべての状態は **PostgreSQL**（`DATABASE_URL` 環境変数）に保存する。SQLite も
JSON ファイルも使わない。

主なテーブル:

| テーブル | 内容 |
|---|---|
| `observations` | 生の記憶と埋め込み |
| `obs_embeddings` | バイナリの埋め込みベクトル |
| `situated_embeddings` | 人ごとの pgvector 埋め込み |
| `episodes` / `episode_memories` | まとめた記憶エピソード |
| `memory_salience` | 想起の顕著性追跡（撤去予定） |
| `semantic_facts` | 抽出した事実 |
| `behavior_policies` | 抽出した行動規則 |
| `memory_revisions` | 編集履歴 |
| `memory_events` / `memory_jobs` | 非同期ジョブキュー |
| `memory_links` | 連想リンク |
| `unfinished_business` | 開いた懸案 |
| `relationship_state` | 人ごとの関係データ |
| `persons` | 既知の人物レジストリ |
| `mental_state_log` | 追記専用のメンタル状態スナップショット |
| `self_narrative_log` | 一人称のセッション日記 |
| `agent_state` | キーバリュー：desires, heartbeat, concerns, intervention_policy, capability_summary, mood_pad, drive5 |

スキーマ変更は `migration/` 以下のタイムスタンプ付きファイルを通す。

## データベース

| DB | 用途 | ポート |
|---|---|---|
| `familiar_ai` | 本番 | 5432（Docker） |
| `familiar_test` | テスト | 5433（Docker `--profile test`） |

## 開発ルール

- Python 3.10 以上
- 非同期を基本とする
- **保存バックエンドは PostgreSQL のみ。SQLite も JSON/JSONL も使わない**
- 巨大なプロンプト塊より、決定論的なロジックと型付き dataclass を優先する
- 生の内受容・身体指標を、通常のユーザー向けテキストへ漏らさない
- スキーマ変更ごとにマイグレーションを追加する

## 判断の優先順位

規則どうしが衝突したときは、上から順に優先する。下位のために上位を崩さない。

1. **不可逆な損害を出さない**（本番 DB のデータ、コミット履歴、既存の記憶）
2. **人間の承認**。確信を、確認を省く理由にしない。「承認されるに決まっている」と
   感じたときこそ立ち止まる合図とする
3. **事実の正確さ**。実物のソースと実行中の挙動を確認してから語る。【事実】と
   【推測】を分け、事実だけを断定する
4. **上記「開発ルール」の遵守**。これらはすでに全体に効いている不変条件である
5. **設計ドキュメントとの整合**（`docs/NewModesDocs/`）。ただしこれらは
   **未実装の決定を含むことがある**。予定であって、確定した規範ではない
6. **検証の緑**（ruff / mypy / 全体テスト）
7. **速さと変更量の少なさ** ← 最下位。他を犠牲にして急がない

衝突の扱い方:

- **設計ドキュメントの記述が開発ルールに反して見える** → 実装せず、どちらを直すかを
  問う。設計側が新しい正解である可能性もあるため、黙ってどちらかへ倒さない。
- **設計ドキュメントと実コードが食い違う** → 実コードを【事実】として報告し、
  どちらを直すかを問う。勝手にどちらかへ寄せない。
- **要求された変更が開発ルールに反する**（例：一時ファイルに JSON で保存したい）
  → 実装せず、理由と代替案を出して確認を取る。
- **テストを通すために設計を曲げたくなった** → 曲げずに止めて報告する。

## 権限

| 区分 | 対象 |
|---|---|
| 読み取り自由 | リポジトリ全体、`docs/`、`.claude/rules/`、テスト DB（5433） |
| 承認なしで変更可 | 承認済みの実装計画の範囲内での `src/`・`tests/`・`migration/` の編集。下記の判定を満たす file への**追記** |
| 承認が必要 | 実装計画の範囲を超える変更、`docs/` 配下の**新規 file 作成と改名**、設計・計画文書の改訂、追記自由な file であっても既存記述の削除・書き換え、この CLAUDE.md と `.claude/rules/` の変更、依存の追加・更新、コード側の改名・撤去、マイグレーションの適用 |
| 禁止 | 本番 DB（`familiar_ai`、5432）への書き込みと破壊的操作、`git push --force`、履歴の書き換え、feature ブランチと PR の作成、無人での長時間自走 |

**追記自由の判定**（file 名で決める。次をすべて満たすときのみ）

1. `docs/NewModesDocs/` 直下にある
2. 名前に「進捗」または「ログ」を含む
3. 名前に「設計」「計画」「仕様」「課題」「ユースケース」「データモデル」「チケット」
   「規範」「一覧」を含**まない**

判断がつかない名前は承認必須とする。新規作成と改名は、名前に関わらず承認を得てから
行う（名前が権限を決めるため、自分で名前を作る・変えることを許すと判定が骨抜きになる）。

## 作業ループ

詳しい手順は `.claude/rules/作業手順.md` にある。ここには常に効く骨格だけを置く。

1. **現状を確認する** — 実物のソースと、実行中の挙動を読む
2. **該当する設計ドキュメントを読む**（後述の表を使う）
3. **設計方針を出す** — 原因と対応案の形。承認を得る。
   *この段では実装方針もコードも TDD 手順も出さない*
4. **実装計画（TDD の改造方針）を書く** — 承認を得る。
   設計に未決を残したまま、この段へ進まない
5. **RED** — テストを書き、**正しい理由で落ちる**のを目で見る。
   成り立つ場合に見えるものと、間違っていれば見えるはずのものを対で確認する
6. **GREEN** — 一項目ずつ実装する。大きな変更を一度にやらない
7. **検査** — 次節のチェックリストを実行する
8. **不合格なら原因の工程へ戻る**（3 または 5 へ）
9. **合格したらコミットし、報告する**

一項目ごとに人間の確認を挟む。仮置きの値が必要になったら、決めずに質問し、
回答を得てから進む。

## 完了条件（コミット前チェックリスト）

`develop-ikuchan` へコミットする前に、次をすべて満たす。

```bash
uv run ruff check .
uv run --group dev mypy src/familiar_agent
./scripts/run_tests.sh -m "<開発内容を短く（日本語可）>"
```

- [ ] `ruff` が緑
- [ ] `mypy` が緑
- [ ] `./scripts/run_tests.sh` が緑
- [ ] 保存は PostgreSQL のみ（SQLite・JSON・JSONL を増やしていない）
- [ ] スキーマ変更に対応するマイグレーションがある
- [ ] 生の内受容・身体指標がユーザー向けテキストへ漏れていない
- [ ] 改名・撤去なら、旧名の `grep` が **0件**。
      数え上げたリストで代えない。除外するなら理由を1件ずつ明示する
- [ ] 書いたコードに対応する設計が `docs/NewModesDocs/` にある。設計の無いコードを入れない
      （設計だけが先にある状態は正常である。実装が設計に追いついていないことは欠陥ではない）
- [ ] この file に未実装の予定を書いていない。ここの記述は「いま動いている実物の説明」として
      読まれるので、予定を現在形で混ぜると、次のセッションがそれを実装済みと信じ、
      無い機構を前提にコードを書く。予定は `docs/NewModesDocs/` に版番号つきで置く
- [ ] テスト実行中に対象外のファイルを編集していない

`ruff` と `mypy` は自分で回す。**全体テストも自分で回す。** `./scripts/run_tests.sh` は
テスト DB コンテナ（ポート 5433）を起動し、それに対して pytest 一式を回し、緑なら `-m` の
メッセージで `git add -A` してコミットし、終了時にコンテナを止める。`uv run pytest -q` を
直接使わない（多くのテストが実 PostgreSQL 接続を要する）。テストは時間がかかるので
**バックグラウンド実行**してよい（`run_in_background`）。完了通知で結果を確認し、緑なら
コミット済み、赤なら失敗内容から直す。バックグラウンド実行中に別のファイルを編集すると、
緑時の `git add -A` に巻き込まれてコミットがずれる。

## 反復条件と停止条件

- 不合格なら、原因を特定してから、該当する工程へ戻る。症状だけを潰さない。
- 修正の前後で何が変わったかを記録する。
- **同じテストが同じ理由で 2回続けて落ちたら、止めて報告する。** 未解決点と、
  必要な人間判断を示す。
  - 「同じ理由」は、落ちたテスト名とエラーの種類（例外の型と発生箇所）が前回と
    一致すること。別のテストが落ち始めた場合や例外の種類が変わった場合は、
    カウンタを 0 に戻す。前進しているあいだは止まらない。
  - **カウンタを戻したときは、前の失敗と何が変わったかを1行残す。** 記録のない
    リセットを重ねれば、上限は事実上なくなる。
  - この上限は「止まって報告する」の合図であり、作業の失敗ではない。方針の承認を
    得て、そこから再開できる。
- 次のいずれかに当たったら、その場で止めて質問する。
  - 仮置きの値を決める必要が出た
  - 設計ドキュメントと実コードが食い違った
  - 変更が「権限」表の承認必須・禁止に触れる
  - 承認された設計方針の範囲を超える変更が必要になった

## 最終報告の形式

1. **【事実】** — 実行したコマンドとその結果、読んだファイルと行、変更したファイル
2. **【推測】** — 事実から導いた解釈。断定しない
3. **残る不確実性** — 確認できなかったこと
4. **次に承認が必要なこと**

## 参照情報（作業前に読む文書）

設計と計画の正本は、日本語の設計ドキュメントで、すべて `docs/NewModesDocs/` に置く。
**関連コードを触る前に該当ドキュメントを読む。** 新しい設計・計画ドキュメントは
`docs/NewModesDocs/` に版番号つきで書く（作成前に承認を得る）。これらを `@import` で
全部取り込まない（毎セッション読み込まれて重い）。必要なものを都度読む。

| 触る対象 | 先に読む |
|---|---|
| mood、drive、評価器、想起 | `感情ループ全体像`（起動源は Drive の時間蓄積と発火。W→mood→想起はその下流）、`設計詳細_発火_mood` |
| 活性、O 書込、知覚在席 | `設計詳細_活性_O書込_知覚在席` |
| store と I/F、アーキテクチャ全体 | `設計図_Mermaid`（「store と I/F」節が実装済み台帳） |
| パラメータ値 | `課題5_パラメータ仮案`、`計測_設定値根拠台帳` |
| 段階・順序・現在地 | `課題8_段取り設計`（計画の正本）、`直近の進め方と進捗` |
| 用語・略語 | `用語_略語一覧` |
| 記憶データモデル | `MIデータモデル`、`gap分析_移行設計`、`bge-m3移行チケット`、`課題7` |
| 仕様の意図 | ユースケース① 〜 ⑥ |
| `src/`・`migration/` の py | `.claude/rules/コード規約.md` |
| `docs/NewModesDocs/` の md | `.claude/rules/文章規範.md` |
| 出力の形式全般 | `.claude/rules/作業手順.md`（常時適用） |

設計ドキュメントは版番号で管理し、**未実装の決定を含むことがある**。実装済みかどうかは
`設計図_Mermaid` の台帳と実コードで確かめる。

## Git ワークフロー

- `develop-ikuchan` に直接コミットする（feature ブランチも PR も作らない）。
- コミットは Conventional Commits。説明は日本語でよい。

例:

```text
feat(memory): 想起にエピソード圧縮を追加
fix(agent): 生の内受容の漏れをゲート
docs: 技術アーキテクチャ資料を更新
```

## ルールの所在（`.claude/rules/`）

CLAUDE.md には常時効く要点だけを置き、詳細はここに切り出す。

- `作業手順.md` — 出力に関する指示（調査、設計方針、改造方針、ファイル運用、図の体裁）。
  常時適用。
- `文章規範.md` — 日本語技術文書の文章規範。`docs/NewModesDocs/` の md を書く・直すときに従う。
- `コード規約.md` — ログ方針と、Superpowers / PostgreSQL / pgvector / 並行処理スキルの
  使い分け。`src/` と `migration/` の py を触るときに従う。
